{
  "name": "ascii-roguelike",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check:arch": "node tools/check-architecture.js",
    "build": "npm run check:arch && NODE_ENV=production vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist --no-history"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.0.0"
  }
}
