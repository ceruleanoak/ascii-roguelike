# ADR Backlog — Candidate Decisions

Architecturally significant decisions that **lack an ADR**. AI appends candidates here when it
notices a gap; you prioritize and write them (the *why* is yours to author). When an ADR is
written, remove the row and add it to the index in `README.md`.

This is a prioritization queue, not a to-do mandate — some candidates may resolve to "not worth
an ADR." Drop those rows. Only hard-to-reverse / architecture-shaping decisions belong here, not
routine choices.

**Priority:** P1 = load-bearing, would cost the most to reconstruct the reasoning for later · P2 = significant but lower urgency.

| Priority | Candidate decision | Where it lives today | Surfaced |
|----------|--------------------|-----------------------|----------|
| P1 | **No persistence — full reset on death/refresh.** No localStorage/sessionStorage/IndexedDB; `PersistenceSystem` permanently disabled. The "save file" is mental. | CLAUDE.md "Critical Technical Constraints"; `PersistenceSystem.js` | 2026-06-13 (seed) |
| P2 | **Every consumable use is now a throw — a uniform windup/flight delay before the effect resolves, replacing instant activation for self-buffs (heal, speed, shield, stoneskin, regen, damageBuff, block, cleanse, invuln, waterImmunity, float, auto_dodge, arrowRefill, manaSlot, maxhp, panic_blind).** `ConsumableTriggerSystem.checkTriggerCondition` only gates whether a consumable CAN fire; the actual mutation now lives in the new `applyEffect()`, run by `InventorySystem._executeWindupEffect` once the throw animation lands (`THROW_DURATION = 0.45s`). Gates (HP thresholds, liquid/bow preconditions) are still evaluated at throw-start, not landing, so a precondition that changes mid-flight (e.g. leaving liquid) is not re-checked — same trade-off the pre-existing offensive windup items (bomb, poison flask, etc.) already made. The throw always lands back on the player (no aim yet); `windup.targetX/targetY` already exist (from Jolt Jar) as the extension point for a future aimed throw. Decide: is a flat 0.45s delay on every potion the permanent balance model (a player at 1 HP now has a half-second of vulnerability before a heal lands), and should self-buff throws eventually support aiming/facing-direction toss instead of always landing on the thrower. | `src/systems/ConsumableTriggerSystem.js` (`THROW_DURATION`, `checkTriggerCondition`, `applyEffect`); `src/systems/InventorySystem.js` (`_triggerConsumable`, `_executeWindupEffect`); `src/rendering/state/ExploreRenderer.js` (windup arc/spin render) | 2026-07-10 |
| P1 | **main.js is orchestration-only; behavior routes to systems.** Enforced budgets via `tools/check-architecture.js`; net-new behavior gets a system, not an inline branch. **Extended 2026-07-25 — the ratchet now has a carve-out: libraries are not budgeted.** `src/entities/Item.js` was dropped from `arch-budgets.json` and the rule generalized in the checker header + CLAUDE.md: a file that is a *catalogue* (grows with the content it holds) is exempt, while orchestrators, systems, renderers, and entities that own live state stay budgeted. The reasoning: capping a catalogue only pushes new content somewhere less obvious, and the drift the ratchet exists to catch — behavior defaulting into a file with no business owning it — isn't the failure mode a catalogue has. The open question is the boundary: nothing enforces the library/behavior-owner distinction, it is a judgment call made per file at the moment someone would add a budget, and Item.js is a mixed case (a factory that both catalogues attack patterns *and* generates live hitbox objects). Decide whether "library" needs a stated test (or a marker in the file/config) rather than a per-session reading, and whether other currently-budgeted files qualify. | `tools/check-architecture.js` (header Rules); `tools/arch-budgets.json`; CLAUDE.md "Code Placement Procedure" rule 5 | 2026-06-13 (seed) |
| P2 | **Weapon timing in "double-seconds" (WEAPON_TIMER_RATE = 2).** Data values are ÷2 effective seconds; resolved bug #88 tuned everything against this. | CLAUDE.md "Architectural Compromises"; `PHYSICS.WEAPON_TIMER_RATE` | 2026-06-13 (seed) |
| P2 | **Plane predicate centralization (PlaneSystem) over per-frame interior guards.** Combat/vision/pickup/collision route through one predicate; ad-hoc `inHut`/`inMaze`/`inDungeon` checks are the layer-leak anti-pattern. | `PlaneSystem`; CLAUDE.md anti-patterns; bug #107 | 2026-06-13 (seed) |
| P2 | **Enemy behavior via Mechanic composition, not subclassing/branching.** New behaviors are `entities/enemyMechanics/` files selected by data. | CLAUDE.md "Entity Size Norms"; `src/entities/enemyMechanics/` | 2026-06-13 (seed) |
| P2 | **Crafted intermediates require dual registration in `ITEMS` *and* `INGREDIENTS`.** A material that is both a recipe result (type `INGREDIENT`) and a pool ingredient must be entered in both maps; display lookups resolve via `INGREDIENTS[char]` only, so a char in `ITEMS` but absent from `INGREDIENTS` silently renders as "Unknown" in the crafting menu (no validation). Decide: single source of truth / a derive-or-validate step vs. keep the manual parity convention. | `src/data/items.js` (`ITEMS` + `INGREDIENTS`); `isItem`/`isIngredient`; `main.js` getIngredientData (`INGREDIENTS[char] \|\| {name:'Unknown'}`); bug #114 | 2026-06-19 |
| P2 | **Multi-hut-per-room breaks the `room.hut` singular assumption.** Settlement (`'S'`) rooms hold `room.huts[]`; every other hut-bearing room type still uses singular `room.hut`. `HutSystem._findNearbyHut()` normalizes both shapes into one lookup loop rather than the codebase committing to one shape. Decide: migrate everything to `huts[]` vs. keep the dual-shape normalization permanently. | `src/systems/RoomGenerator.js` (`generateHutRoom` vs `generateSettlementRoom`); `src/systems/HutSystem.js` (`_findNearbyHut`) | 2026-06-20 |
| P2 | **Parameterized hut interior sizing breaks the hardcoded-10×10 assumption.** `HutSystem.generateHutInterior()` now takes an optional `{cols, rows}` override (Alchemy Hut uses 12×12); the three call sites that used to gate on strict `gridCols === INTERIOR_COLS` now use a looser `isHutFloor(floor) = floor.gridCols <= 12` threshold, mirroring a heuristic `HutInteriorOverlay.js` already used independently. Decide: formalize a shared hut-floor-size contract vs. keep the two files' thresholds in sync by convention. | `src/systems/HutSystem.js` (`INTERIOR_COLS`, `isHutFloor`); `src/rendering/ui/HutInteriorOverlay.js` (`gridCols > 12` check) | 2026-06-20 |
| P1 | **Carried ingredients now auto-bank on per-character death instead of being lost.** `_switchToCharacterAtRest()` pushes `player.inventory` into `inventorySystem.restInventory` before `player.reset()` wipes it, removing the "walk back to REST or lose your loot" risk for ingredients specifically (weapons/armor/consumables still follow existing death rules; full game over via `_resetRunToRest`/`handleGameOver()` is untouched and still wipes everything). This narrows the documented "EXPLORE: Inventory lost on death" rule and trades away part of the risk/reward loop the discrete "banking" step created. Decide: is per-character-death survival the intended scope, or should full game over also spare ingredients (or should the old lose-on-death behavior come back)? | `src/main.js` `_switchToCharacterAtRest()`; CLAUDE.md "Game States" (EXPLORE bullet); `src/systems/InventorySystem.js` `bankLoot()`/`handleGameOver()` | 2026-06-30 |
| P2 | **Player damage now defaults to knockback unless a site opts out.** `PhysicsSystem.applyDamageKnockback(player, result, sourceX, sourceY, force = PHYSICS.DEFAULT_DAMAGE_KNOCKBACK)` is a no-op on dodge/block/immune/invuln and otherwise pushes the player away from the damage source — added at each `player.takeDamage()` call site that lacked its own tuned knockback (regular/rock projectiles, frog tongue, mimic bite, maze ghost contact, trap explosion, lightning strike). Melee/charge/boulder/leap-landing sites keep their existing custom forces. Three sites were left with no knockback at all (sap contact — enemy position-locks to the player every frame, so a push is instantly undone; wet/electric field damage; burn DoT) because they fire every frame/tick rather than as a discrete hit. Decide: is "knockback by default, opt out only for continuous/locked sources" the permanent contract, and should the exemption list be enumerated somewhere durable instead of living only as inline comments. | `src/game/GameConfig.js` (`PHYSICS.DEFAULT_DAMAGE_KNOCKBACK`); `src/systems/PhysicsSystem.js` (`applyDamageKnockback`); call sites in `CombatSystem.js`, `MazeSystem.js`, `TrapSystem.js`, `LightningStrikeSystem.js`, `MimicMechanic.js` | 2026-07-03 |
| P2 | **Trap charges-per-room now derived from inventory quantity, not a static constant.** All trap types (`items.js` `charges` field) previously reset to a fixed per-type value every room; `Player.resetTrapsForNewRoom()` now sums copies held across the equipped quick slot + `itemChest` + `pendingChestDeposits` by `char`. Charges are a per-room cap only — deploying a trap does not permanently deplete the crafted count (resets to the same total next room). Decide: is "charges = held quantity" the permanent trap model, and should the now-unused fixed `charges` constants in `items.js`/`Item.js` fallback be removed or repurposed as a floor/cap. | `src/systems/TrapSystem.js` (`resetTrapsForNewRoom`, `countHeldTraps` — moved out of `Player.js` 2026-07-25); `src/entities/Item.js` (`charges` init fallback); `src/data/items.js` (per-trap `charges` field) | 2026-06-20 |
| P1 | **Manual (SPACE) consumable trigger now bypasses auto-trigger's emergency gates; auto = last resort, manual = tactics.** Keys 4-8 arm `player.selectedConsumableIndex` (persists like `activeSlotIndex`); SPACE fires the armed slot via `ConsumableTriggerSystem.checkTriggerCondition(..., manual=true)`, which skips HP/proximity/count gates per-effect but still respects one-shot/cooldown/windup state and true physical preconditions (liquid contact for waterImmunity/float, empty bow slot for arrowRefill). Proximity/count-only offensive items (Bomb, Cursed Skull, Poison Flask, Venom Vial, Jolt Jar, Firecracker, Steam Vial, Battle Elixir) lost auto-trigger entirely (`manualOnly: true`); HP-gated items kept auto-trigger but thresholds tightened (~×0.6, floor 0.12). Decide: is per-effect dual-mode gating in one switch the permanent pattern as the consumable roster grows, or should manual/auto fully diverge into separate functions per item; also whether the ×0.6/floor-0.12 tightening formula should be codified as a constant rather than restated per item. | `src/systems/ConsumableTriggerSystem.js` (`checkTriggerCondition`, `manualTrigger`, `fireSelected`, `selectSlot`); `src/entities/Player.js` (`selectedConsumableIndex`); `src/main.js` (keys 4-8, `handleSpacePress`); `src/data/items.js` (`manualOnly`, `autoTriggerHP`/`autoTrigger` fields) | 2026-07-03 |
| P2 | **`data.pacifist` designation added to `Enemy` — decide if it should stay a flag or become a real class split.** Bug (2026-07-06): Moose/Rabbit (`Enemy` instances via `HuntingSystem`'s `dataOverride`, reusing `Enemy` for physics/vision/pathing/mechanic-composition) inherited the generic aggro/chase/attack state machine, which ran every frame and re-set `targetVelocity` toward the player before `GameAnimalMechanic.update()` (called later in the same method) could override it to flee — read as the moose charging the player before fleeing. Fixed by adding `data.pacifist: true` (set on `MOOSE`/`RABBIT`) checked near the top of `Enemy.update()`: skips the entire aggro/chase/attack pipeline and delegates movement fully to the enemy's own mechanic (`GameAnimalMechanic`). This is a flag-gated early return, not a class split — every future generic `Enemy` behavior added above the guard still risks leaking into pacifist enemies if someone forgets to check the flag first. Decide: keep `pacifist` as the permanent designation (cheap, reuses `Enemy` infra) vs. extract a shared base (movement + vision + physics-registration) that both `Enemy` and a lightweight `Wildlife` class extend, with `Wildlife` never wired into the combat state machine at all. | `src/entities/Enemy.js` (`update()`, `data.pacifist` guard); `src/data/enemies.js` (`MOOSE`, `RABBIT`); `src/entities/enemyMechanics/GameAnimalMechanic.js`; `src/systems/HuntingSystem.js` | 2026-07-06 |
| P2 | **No registry tracks (char, color) uniqueness across enemies/items — collisions are found ad hoc.** Surfaced when picking chars for the Moose/Rabbit game-animal encounter: `'M'` and `'r'` were already claimed by Giant Slime and Rat respectively in the `ENEMIES` registry (safe to reuse here only because Moose/Rabbit are spawned via `dataOverride`, bypassing the `ENEMIES[char]` lookup — see `Enemy.js` constructor), and confirming "no collision" meant grepping the whole data file by hand. The Character Encoding Rule (CLAUDE.md) constrains *which* characters a tier may use (letter/digit for raw ingredients, Unicode symbol for crafted items, ASCII for enemies) but nothing enforces that a given (char, color) pair is unique or flags a clash at authoring time. Decide: build a lint/build-time check (extending `tools/check-architecture.js` or a sibling script) that fails on duplicate (char, color) pairs within a tier, vs. accept ad hoc grepping as the permanent process. | `src/data/enemies.js` (`ENEMIES`, `MOOSE`, `RABBIT`); `src/entities/Enemy.js` (constructor `dataOverride \|\| ENEMIES[char]`); CLAUDE.md "Character Encoding Rule" | 2026-07-05 |
| P2 | **First enemy with a true hidden/intangible state, a vision-duration-gated telegraphed instant-hit beam attack, and an armor-piercing damage concept.** The cyan mini-boss Sniper (`enemies.js` char `'1'`) introduces several patterns with no prior precedent: (1) `enemy.sniperHidden` is a dedicated flag (not `plane`-based) requiring explicit skips in render (`ExploreRenderer.renderEnemy`), collision (`CombatSystem.js` two enemy-loop skips), and minimap indicators (`OffscreenEnemyIndicators.js`) — every future intangible-enemy state must remember to touch the same three call sites, since nothing centralizes "is this enemy currently interactable"; (2) the FSM (`idle→hiding→hidden→tracking→aiming→telegraph→firing→cooldown`, `SniperMechanic.js`) is fully suspend-driven (`updateActive` short-circuits `Enemy.update()` per the `LeapAttackMechanic` pattern) rather than layering on the standard aggro/chase/attack state machine, and vision-lock duration (2s then 1s, reset on any occlusion or range change) gates progression rather than distance/cooldown as every other enemy attack does; (3) "armor-piercing" reuses the existing `isImpact: true` block-bypass flag (Miner precedent) — there is still no real player armor/defense-reduction system for a future weapon or enemy to actually pierce, so this is a semantic reuse, not a new mechanic; (4) the fired beam deliberately ignores background-object occlusion (only walls block it) while the *aim/vision-lock* is occluded by the same objects — a same-mechanic split between "aiming" and "firing" collision rules with no prior example to generalize from. Decide: should hidden/intangible-enemy state be centralized into a single predicate (mirroring `PlaneSystem`'s role for plane 0/1) before a second such enemy is built, and is the vision-lock-duration-gates-attack pattern worth extracting into a reusable mechanic base for future ranged enemies. | `src/entities/enemyMechanics/SniperMechanic.js`; `src/entities/Enemy.js` (suspend dispatch); `src/systems/CombatSystem.js` (`consumeResult` dispatch, `sniperHidden` skips); `src/rendering/effects/SniperEffects.js`; `src/data/enemies.js` (`sniperMechanic` config, `BOSS_ENCOUNTERS.sniper`) | 2026-07-17 |
| P1 | **Telegraph shape vocabulary — warning shape decoupled from damage shape, owned by one shared geometry module.** Enemy melee telegraphs are gaining a shape vocabulary (`rect`/`cone`/`ring`/`circle` + timed damage pulses) via a `telegraph: {warnShape, hitShape, pulses}` data block and a pure-geometry module `src/game/Telegraph.js` consumed by three renderer/resolver surfaces at once: CombatSystem (hit resolution), ExploreRenderer (drawing), and the enemy-editor sandbox (which previously hand-mirrored CombatSystem's windup state machine and silently diverged). Decisions baked in: warn ≠ hit shape is a deliberate readability stance (warning aids anticipation, not a 1:1 outline); each pulse re-arms the attack by clearing `hasHit`, so a pulse is a separate damage burst tested against the player's i-frames (there is no per-pulse `attackId` — `attackId` is the player→enemy dedupe field and plays no part on this side); the whole melee list runs on the enemy double-second clock, stepped with `deltaTime * ENEMY_TIMER_RATE` to match `Enemy.update()`, with real-second durations converted at the `Enemy.convertToEnemyAttack()` boundary (resolved bug #150 — the seam was live and silently doubling every authored delay); absent `telegraph` data preserves byte-identical legacy single-rect behavior for all shipped enemies. Hard to reverse once enemies are authored against the shape schema. Cross-ref the Sniper row (2026-07-17) — its vision-gated beam telegraph predates and sits outside this vocabulary. **Extended 2026-07-24 — the animation layer is a combat authority, not a renderer.** `src/game/TelegraphAnimation.js` adds named shape presets (`basic`/`verticalSlice`/`horizontalSliceThin`/`horizontalSliceThick`/`ring`) and an animation catalog, authored as `telegraph: {shape, animation, beatDamage}`. The decision taken: **the animation declares the beats** — a named animation owns the attack's rhythm and its beats compile into the damage pulse list (`compilePulses`), so choosing `doubleSweep` over `sweep` changes how many times the attack damages the player, not merely how it looks; `pulses` and `animation` are mutually exclusive at authoring time (animation wins, console.error on both). Alternative rejected: pulses authored independently with the animation purely decorative — rejected because it lets drawn choreography and damage timing drift apart silently, the exact divergence class the shared module exists to prevent. Consequence to confront: retiming or adding an animation is a **balance** change reviewable as such, not art. **Extended 2026-07-25 — Telegraphs are drawn in pixel space, not on the character grid.** The first implementation masked a cell rasterization, which can only ever move a whole cell at a time and read as blocks stuttering across the shape; it was rejected on sight. The replacement splits the visual in two — a still filled area for the tell (it says *where*) and a ~3px stroke sweeping through it for the strike (it says *now*) — both drawn with raw canvas calls via `drawTelegraph(ctx, attack)`, following `BossRenderer.drawChargeCone`. The decision worth ratifying is the precedent, not the code: **combat cues are pixel-space graphics in a game whose entities are all glyphs.** That buys precision and fluidity the grid cannot express, and costs the visual coherence of an ASCII game — every future cue now faces the same fork. Downstream effects already taken: shape presets are sized for gameplay alone (the ~1.25-cell raster floor they were built around is gone, since any size now fills faithfully); animations are differentiated by axis, direction and **easing** rather than by windup choreography, which collapsed several catalog entries into near-twins (`slap`/`sweep`, `vertical`/`thrust`) that survive only on their speed curves. **Extended 2026-07-24 — three forks taken in one pass, each reversible in isolation but load-bearing together.** (1) *Areas and Sizes.* The shape presets became **Areas** (`telegraph.area`) and the three basic ones (`box`/`circle`/`trapezoid`) gained **Sizes**, with `small` — one cell, the original windup box — as the default and `big` (the AoE) requiring an explicit ask; `basic` was retired rather than aliased, so there is exactly one name per region. The decision inside the decision: **the default warning is the smallest one that is still a complete instruction.** An enemy that simply swings at you threatens one cell; anything wider is an authored claim about that enemy. (2) *No Area is ever outlined.* The warned region is fill-only, blinking between two solid values, so **the strike stroke is the only line a Telegraph draws** and cannot be confused with a boundary. Costs crispness at the edge; buys an unambiguous reading of which line hurts. (3) *The tell and the strike never share the screen.* At release the Area disappears and only the strike draws — and nothing at all draws between the beats of a multi-hit attack. This is a readability stance with a balance consequence: `doubleSweep`'s pause is now silent, so its second hit arrives with no standing warning. **The fork worth ratifying separately: `telegraph.attackShape` puts a character back into a pixel-space cue.** An authored glyph (Unifont, one cell, rotated to face the swing) replaces the hairline and rides the same `strikeAnchors` path. It reopens the pixel-space-vs-glyph question settled directly above, deliberately: the *path* stays continuous geometry and only the *mark* becomes a character, which is the narrowest form the reunion could take. Alternative rejected: authoring the strike as a cell-space glyph sequence — that is the rasterization already rejected on sight. Consequence to confront: an Attack Shape is a combat cue, so it is exempt from the two-tier Character Encoding Rule, and nothing prevents authoring one that collides visually with an item or enemy char. **Extended 2026-07-25 — the strike is the hitbox; the Area only names the danger zone.** Damage is now resolved against the travelling mark's own geometry, swept across the frame's slice of the beat (`Telegraph.strikeHitsBox` sub-samples `[beatElapsedPrev, beatElapsed]` and pads the player's box by the mark's half-thickness), rather than by containment in the hit shape. The decision: **what the player watches move is what damages them** — an Area tells you which ground to leave, and standing on it costs nothing until the mark arrives. Three things follow, each a consequence rather than a separate choice. (a) A multi-beat animation only damages once per pass that actually crosses the player, so `doubleSweep` is two dodgeable events rather than a shape that bills twice; the beats stay a balance decision but no longer a guaranteed one. (b) An attack must stay armed for the *whole* pass instead of being tested on one frame — a ~3px mark tested once would essentially never connect — so arming duration became the Telegraph module's call (`retireAfterTest`), latching one hit per beat at the moment of contact. (c) An Attack Shape must **stretch to span its mark**, since glyph and hitbox are now the same object and a one-cell character in the middle of a strike that damages end to end would be a picture of the wrong thing. `blink` is carved out explicitly: with no travelling mark, its hit shape still damages by containment on release. Alternative rejected: keep containment damage and treat the strike as decoration over it — that is precisely the watch-vs-connect drift the shared module exists to prevent, and it is what the previous behaviour was. Two consequences to confront: this makes **every telegraphed enemy melee attack materially easier to dodge**, unmeasured, so it is a roster-wide balance change riding on a readability decision; and hit resolution now reads pixel constants (`STRIKE_WIDTH` 3px, `GLYPH_HALF` 0.35 cell), so the pixel-space precedent above has reached the damage model, not just the drawing. | `src/game/Telegraph.js`; `src/game/TelegraphAnimation.js`; `src/systems/CombatSystem.js` (enemy-melee loop); `src/rendering/state/ExploreRenderer.js`; `tools/enemy-editor/src/sandbox.js`; `tools/enemy-editor/src/telegraph.js`; `src/data/enemies.js` (Rat/Boar/Slime); GLOSSARY.md "Telegraph" / "Area" / "Telegraph Animation" / "Attack Shape" | 2026-07-22 |
| P2 | **`Enemy.takeDamage` gains a damage-source identity contract.** Signature extends to `takeDamage(amount, attackId, source)` where `source` carries `{weaponSubtype, isBlade, isBullet}`, threaded from CombatSystem's projectile and melee resolution where `attack.weaponSubtype` is in scope. First consumer: chip armor (`ArmorMechanic`) discriminating whips (never chip — the hard counter). This makes weapon identity available at the damage sink for all future resist/counter mechanics — a contract every future damage call site must honor (or pass null and silently opt out of source-aware mechanics). Cross-ref the Sniper row's note that "armor-piercing" was semantic reuse with no real armor to pierce — chip armor is now that real system on the enemy side. | `src/entities/Enemy.js` (`takeDamage`); `src/systems/CombatSystem.js` (projectile + melee damage sites); `src/entities/enemyMechanics/ArmorMechanic.js` | 2026-07-22 |
| P2 | **Enemy editor is the enemy-authoring home; schema sync is a same-commit obligation.** `tools/enemy-editor/` (schema-driven form + live sandbox stepping the real `Enemy.js` + codegen to `enemies.js` literals + git-tracked draft JSONs in `templates/`) is being adopted as the canonical authoring/testing path for enemy work, and a written same-commit sync rule in the editor README. The decision: hand-maintained schema, vs. generating the schema from the mechanics themselves (rejected for now — mechanics don't declare their field metadata). The editor previously drifted invisibly because nothing referenced it. **Open weakness the ADR should confront:** the countermeasure is currently a README sentence, not a check — no build-time drift guard exists in `tools/check-architecture.js` comparing `schema.js` mechanic gates against `enemyMechanics/*.js` `isEnabled` gates and `enemies.js` usage, and the drift is already real (the `telegraph` block shipped with no way to author it in `schema.js`/`codegen.js`). Either build the guard or accept manual sync explicitly. | `tools/enemy-editor/` (README, `src/schema.js`); `tools/check-architecture.js`; CLAUDE.md "Dev & Debug Tools" | 2026-07-22 |
| P2 | **Fractional-threshold reveal (Sinkhole) — first "majority of tracked sub-objects" gate.** `room.sinkholes[].cutSet`/`adjacentGrass` reveals a concealed Sinkhole once `cutSet.size / adjacentGrass.length > 0.5` of the grass tiles touching it have been cut, checked inline at the `cutGrass` effect site. Every prior reveal precedent (`fairySpawned`, the beast-lurking-in-grass roll) is a one-shot boolean or single roll, not a ratio across a tracked set. Decide whether this ad hoc counter is the template for future "reveal-by-majority" mechanics or should be generalized into a reusable tracker. | `src/systems/SinkholeSystem.js`; `src/systems/InteractionSystem.js` (`cutGrass` branch); `src/systems/RoomGenerator.js` (`seedSinkholes`) | 2026-07-19 |
| P2 | **First mid-run cross-zone transition triggered by in-room player action (not REST/cheat-menu), and first room generator branching its plane-1 layout via an external one-shot flag rather than the room's own template/zone data.** The Sinkhole shortcut dives the player from a green `G` room's plane-1 cave into a freshly generated yellow `U` room mid-EXPLORE (`SinkholeSystem._crossToYellow`, mirroring `handleZoneTeleport`/`handleRoomWarp` but triggered by grass-cutting instead of a cheat/exit). `RoomGenerator.generateUndergroundRoom` checks a one-shot `this._forceSinkholeArrival` flag (set by `SinkholeSystem`, cleared on read) to decide whether to inject a guaranteed lake+river-trail — the first case of generation-time behavior gated by caller-set instance state rather than `room.zone`/`exitLetter` alone. This is explicitly phase one of a larger feature (enemy population for both sides is deferred). Decide whether flag-and-clear is the durable pattern for future shortcut-style cross-zone features, or whether they need a more formal handshake mechanism. | `src/systems/SinkholeSystem.js`; `src/systems/RoomGenerator.js` (`generateUndergroundRoom`, `_forceSinkholeArrival`); `src/systems/roomFeatures.js` (`injectSinkholeLake`) | 2026-07-19 |
| P2 | **Letter-room generators now branch per-zone internally, and the branch's state/logic lives in a phased, reversible, exit-locking hazard system.** Red-zone Ascent (`A`) rooms are structurally different from every other zone's Ascent room: `RoomGenerator.generateAscentRoom()` gates on `room.zone === 'red'` mid-function to seed a mud floor (`roomFeatures.js` `seedMoltenAscentCycle`), and a new per-frame system (`LavaAscentSystem`) mutates `BackgroundObject`s in place through a `fillingFloor → fillingSlopes → drainingSlopes → drainingFloor → complete` cycle, holding `room.exitsLocked` for its full duration (extending the existing `inAquifer`-style inline guard in `main.js`'s generic enemy-clear unlock). This is the first "same template letter, different mechanic per zone" room — `generateCalderaRoom` is a full replacement of the `C` room, not a same-letter variant — and the first hazard system with its own phase state machine on `room.*`. Decide: is zone-conditional branching inside a shared letter-room generator (vs. a fully separate per-zone generator function) the intended pattern for future letter+zone combos, and should the phased/reversible/exit-locking hazard shape (`LavaAscentSystem`) become a reusable base for similar mechanics in other zones. | `src/systems/RoomGenerator.js` (`generateAscentRoom`); `src/systems/roomFeatures.js` (`seedMoltenAscentCycle`); `src/systems/LavaAscentSystem.js`; `src/main.js` (`ascentLavaActive` guard) | 2026-07-14 |
| P1 | **Weapon tier results are now gated on run state — the fairy fountain's Attunement decides which upgrades exist.** `FountainSystem._findNextTierChar` used to be a pure lookup into `WEAPON_TIERS` (pick a random char from the next rung); it now filters that rung by `weaponElement(getItemData(c))` against `room.fountain.attunement`, so an unattuned pool returns only non-elemental candidates and a gem offering *adds* its element to the pool. Three things follow. (1) `WEAPON_TIERS` stops being a static description of the upgrade graph and becomes an input to a run-state-dependent one — a rung that is entirely elemental (the whip family's tier 2) now means "unreachable without a gem" rather than "guaranteed elemental upgrade", which silently re-values every existing tier table nobody authored with a gate in mind. (2) The refusal path is reused for the gate: an empty filtered rung returns `null`, which already meant "fairy hands the weapon back, `upgradeUsed` not consumed" — so a locked upgrade is indistinguishable in play from a top-tier weapon, with no cue explaining why. That is deliberate under the non-instructive-UI rule, but it means the gate is only learnable by noticing the water changed colour and trying again. (3) The fountain is now the sole source of elemental weapon tiers, so any future non-fountain path to the same weapons (a recipe, a drop table, a shop) reopens the gate without touching this code. Decide: is run-state filtering of a data table the durable shape for progression gates, or should gated tiers be expressed in the data itself (a `requires` field on the item) so the gate is visible where the weapon is authored. | `src/systems/FountainSystem.js` (`_findNextTierChar`, `_attune`, `ATTUNEMENT_COLORS`); `src/data/items.js` (`TREASURE_OFFERINGS`, `weaponElement`, `WEAPON_TIERS`); `src/systems/TreasureOfferingSystem.js`; GLOSSARY.md "Attunement" (pending) | 2026-07-25 |
| P1 | **One ingredient pile — the banked/carried inventory split is gone.** Ingredients lived in two arrays: `InventorySystem.restInventory` (banked, REST) and `Player.inventory` (carried, EXPLORE), with each feature picking a pool by hand. The player entity no longer stores ingredients at all; `InventorySystem.ingredients` is the only pile, reached through `game.getIngredients()` / `hasIngredient()` / `countIngredient()` / `addIngredient()` / `removeIngredient()`, and every consumer was converted in one pass (cauldron, press, bridge worker, errands, wise fellow, mana conversion, crafting menu, Tab overlay, death ledger, fountain offering). The split had no mechanical stake behind it — `handleGameOver()` wipes both, so "banked" was never safer than "carried" — but it silently halved the visible inventory for anything that read the wrong pool, and the dual-pool hand-rolls that grew up to paper over it (`ErrandSystem`, `RidgeSystem`, `InteractionSystem` each had their own carried-then-banked fallback) drifted independently. Two things follow. (1) There is no longer any act of banking for ingredients: walking back to REST transfers nothing, so the risk/reward of the return trip now applies only to weapons, armor and consumables. This supersedes the 2026-06-30 row above, whose decision (auto-bank carried ingredients on per-character death) is moot once there is one pile — that row is a candidate to drop. (2) `game.ingredients` (loose ingredient entities on the room floor) and the pile are now one accessor apart on the same object, distinguished only by comments. Decide: is a single pile with no banking step the permanent inventory model, and should the REST/EXPLORE distinction survive anywhere in inventory at all. | `src/systems/InventorySystem.js` (`ingredients` + accessors, `bankLoot`); `src/entities/Player.js` (ingredient array removed); `src/main.js` (accessor block); GLOSSARY.md "Inventory" (pending) | 2026-07-26 |
| P1 | **Enemy behavior is a closed State spine with per-enemy data variants.** Every `Enemy` walks one named, closed set of states (proposed: Dormant / Alert / Approach / Anticipate / Strike / Recover / Search / Withdraw); an enemy's data declares which of them it uses and how, and a state it does not declare is skipped, with the runner walking the transition table forward until it finds a declared one so a skip never dead-ends. Extending the set is an authorial act (glossary + code), never authorable per-enemy in data — free-form per-enemy state graphs were considered and rejected. This draws the line the existing "Enemy behavior via Mechanic composition" row (2026-06-13) leaves open: what is a State and what is a Mechanic. The answer taken: a State owns the enemy's phase and its per-frame movement; a Mechanic is an overlay that claims a state or none, declared as `claims: [...]` / `overlay: true` rather than as source-line position. Five mechanics currently run private FSMs the core cannot see (`SniperMechanic` declares nine states in a comment block and never writes `enemy.state`, so `ExploreRenderer` and `TrailMechanic` read stale values; `ChargeMechanic` force-rewrites `enemy.state = 'chase'` at three sites), and ten each re-implement the same `{windupActive, windupTimer, cooldown}` ladder. Note the precedent this reopens: a parallel meta-state machine bolted alongside the core was built and deleted in `baba4f0` because it leaked flags and needed orphan indicators. The claim here is that the concept was right and its *position* was wrong — a spine state cannot leak, because the runner owns `enter`/`exit` and there is exactly one current state. Decide: is the closed-spine-plus-data-variants model the permanent shape of enemy behavior, and is `mechanic:` on a state the only escape hatch (vs. admitting wildcard or ad-hoc states later). **Update 2026-08-01:** a wildcard state now exists in code — `flee` leads both `FALLBACK.approach` and `FALLBACK.search` in `EnemyStateMachine.js`, so an enemy declaring `flee` but omitting `approach`/`search` gets it instead of hunting (trap goblin is the first user). Implemented at the user's explicit request; the open question above (is this the sanctioned general pattern, or a one-off) is unchanged. **Update 2026-08-02:** Bomb (`'6'`, `RipenMechanic`) is the wildcard's second user (flees, declaring neither `approach` nor `search`), and its growth-stage-3 archetype flip is also the *second* use of retroactively mutating `stateMachine.declared` at runtime to make a state transition resolve somewhere new — the first being `Enemy.js`'s `equipWeapon()` for `declared.recover`. Both call sites reach directly into `.declared` rather than through any method `EnemyStateMachine` exposes, and both had to independently discover (by reading `resolve()`/`FALLBACK`) that a bare `transition()` call silently no-ops if the target isn't declared yet. Two data points now support the same fix: a real `declare(id, cfg)` API on `EnemyStateMachine` would make "this enemy can newly become X" a first-class, discoverable operation instead of a pattern each caller has to reinvent by touching a private-looking field. **Update 2026-08-02 (2):** the mutation pattern above shipped a real bug, not just a discoverability wart — `stateDefaults.js`'s `statesFor(data)` was handing out `data.states` *by direct reference* for any re-authored enemy, so `stateMachine.declared` was the literal shared `ENEMIES['6']` catalogue object, not a per-instance copy. One Bomb reaching growth-3 and mutating `declared.approach`/`.search` therefore corrupted every future Bomb spawned that page session — freshly spawned Bombs came in already `enraged`-chasing instead of fleeing, reproduced via cheat-menu respawn. Fixed by shallow-cloning in `statesFor` (`{ ...data.states }`). This is exactly the failure mode a real `declare(id, cfg)` API would have prevented by construction (it would clone or own its target rather than callers reaching into a shared field directly) — raises the fix from "nice API" to "closes a live cross-instance data-corruption bug." **Update 2026-08-02 (3):** the wildcard chain grew two more links — `lookback` and `useTrap` are now real declared States (`enemyStates/lookback.js`, `enemyStates/useTrap.js`), promoted out of what used to be flags and a timer block living inside `flee.js` itself, at the user's explicit request ("flee needs a separately supported statefulness on the same spine: idle > alert > flee > lookback (looping with flee) > use trap > idle"). `FALLBACK.lookback` and `FALLBACK.useTrap` both lead with `'flee'` if undeclared, but the two are not symmetric: `useTrap` undeclared is a safe degrade (Lookback's fallback lands on a State genuinely different from Lookback's own current one — Bomb's case, glances back forever, never traps), while `lookback` undeclared is not (Flee's fallback lands back on Flee's own already-current state, which `transition()` treats as a no-op, so the glance-back would silently never fire at all). Deliberately not engine-guarded — same category of decision as the wildcard state itself, left to the data author plus an enemy-editor warning (`fleeNotes` in `tools/enemy-editor/src/schema.js`) rather than a runtime assertion. The Trap Goblin's `useTrap.to` resolves to `'alert'` rather than the shared default of `'withdraw'`, unique to it per the user's own callout — it settles back to idle after laying its trap, not disengagement. This is now the wildcard chain's third and fourth link (after the two `flee` update entries above), continuing to grow the case for the `declare(id, cfg)` API named in the 2026-08-02 update rather than resolving it. **Update 2026-08-02 (4):** the direct `useTrap -> alert` resolution above turned out to be a real bug, not just a narrative choice — reopening the door on the very same tick the retreat ended let a target that was merely still visible (not chasing) retrigger the whole flee episode indefinitely, contradicting the user's explicit correction that "the pattern ENDS, never repeats... only in the case of active pursuit... should more than one trap ever actually happen." Trap Goblin's `useTrap.to` now resolves to `'withdraw'` (the shared default) after all — but this is not adopting "disengagement" as a distinct narrative beat; `withdraw`'s only job here is to be a real, non-zero elapsed gap (0.7s) before anything re-checks for the target, since `alert` alone has no such gap without a configured `duration`. Whether a State should be allowed to declare "I need a minimum dwell before my `next()` can fire" as a first-class thing (rather than borrowing a neighboring State's duration for it) is a second, smaller design question this raises alongside `declare(id, cfg)`. Also new this update: `src/entities/enemyPursuit.js`, a `requirePursuit` config flag (`armPursuitGate`/`isPursuing`) that any State can opt into via `machine.configFor()` — the second generic per-State config flag after `duration`. It exists because resetting `lastKnownPosition`/`hadVisualContact` on `useTrap.exit()` (needed so the goblin can still notice a target it isn't facing) reopens Alert's re-engage doors on raw visibility/proximity alone, which can't tell "still nearby" from "still chasing." Notable implementation trap for whoever designs the eventual generic API: the gate cannot compare current distance-to-target against a raw entry-distance snapshot, because States that hold it armed (`alert`'s wander, `withdraw`'s backing-off) move the enemy itself every frame, and the enemy's own motion contaminates a raw-distance comparison as much as the target's does — confirmed by a headless test where a purely random wander step toward a stationary target read as "pursuing." The working fix anchors the snapshot to the enemy's position at arm time and measures only the target's own displacement from that fixed point. | `src/entities/Enemy.js` (the six-way `if/else if` ladder this replaces); `src/entities/EnemyStateMachine.js` + `src/entities/enemyStates/` (planned); `src/entities/enemyMechanics/`; `src/entities/enemyMechanics/RipenMechanic.js`; `src/entities/enemyMechanics/TrapLayerMechanic.js`; plan `~/.claude/plans/what-is-needed-a-streamed-stallman.md`, `~/.claude/plans/humble-zooming-meerkat.md` | 2026-07-27 |
| P2 | **Movement archetypes become per-State verbs; `movementStyle` survives only as an authoring preset.** `data.movementStyle` (`chaser`/`keeper`/`kiter`/`jumper`/`ambusher`) is an enemy-level type today, dispatched from one branch of the state ladder into five hardcoded methods with un-tunable multipliers (0.6× strafe, 0.8× ring, 1.2× dive, 0.4× windup, 0.5× back-off, 0.3× wander). It becomes six verbs — `close`, `hold`, `orbit`, `back`, `still`, `wander` — each usable in *any* state with its own `speed` multiplier and optional band, so an enemy can orbit while approaching, close while striking, and back off while recovering. Jumping stops being an archetype and becomes a modifier on a verb (`hop: {interval, impulse, arc}`), attachable to whichever state should carry it. The open question is what happens to `movementStyle` itself: it is retained as a preset that expands to a default `states` block, which is precisely the mechanism that makes the migration behavior-identical for all 56 enemies — but a preset that is expanded at author time and then discarded, versus one resolved at runtime on every enemy forever, are different decisions with different failure modes (the first makes the data self-describing but loses the shorthand; the second keeps two sources of truth for the same behavior). Decide: does `movementStyle` survive in `enemies.js` after migration, and is the six-verb vocabulary closed the way the State set is. | `src/entities/enemyMovement.js`; `src/data/enemies.js` (`movementStyle` / `movementConfig`); `src/data/stateDefaults.js` (planned); `tools/enemy-editor/src/schema.js` (movement archetype section) | 2026-07-27 |
| P2 | **Recover is a closed six-variant set, and the player-lock/grab pair generalized from one boss to any enemy.** `enemyStates/recover.js` fixes the Chase-state waggle (the legacy melee back-off was a bare distance flip with no minimum-commit timer, re-triggering itself for the whole cooldown) by giving Recover a real timer with six named variants (`retreat`/`stationary`/`jumpBack`/`knockback`/`lockPlayer`/`hide`), selected per-enemy via `data.recover.variant` and defaulted to `retreat` for chaser/keeper archetypes in `stateDefaults.js`. Two of the six reuse existing single-purpose mechanisms rather than inventing new plumbing: `hide` reuses the rabbit Burrow's `plane = 1` (`GameAnimalMechanic.js`), and `lockPlayer` reuses `player.grabbed`/`grabbedBy` (`Player.js`), previously exclusive to GooHead's boss-only grab. Generalizing the grab pair required two follow-on fixes with no prior need to exist: `BossSystem._checkGrabEscape` now guards `typeof head.releaseGrab === 'function'` before calling boss-only escape methods on a `grabbedBy` that might not be a GooHead, and `EnemySpawnSystem.handleEnemyDeath` now clears `grabbedBy` if the dying enemy was holding it — without this a regular (far more killable than a boss) enemy dying mid-`lockPlayer` would leave the player permanently rooted, since no other death path touched the field. Decide: is the six-variant set closed the way the State set itself is (row above, 2026-07-27), and is `grabbed`/`grabbedBy` now a general-purpose "player movement lock" primitive any future mechanic may claim, or should GooHead's struggle/escape behavior and Recover's plain-timer lock diverge into separate fields now that a second writer exists. | `src/entities/enemyStates/recover.js`; `src/data/stateDefaults.js` (`data.recover`); `src/entities/enemyMovement.js` (`lungeBack` verb); `src/systems/BossSystem.js` (`_checkGrabEscape`); `src/systems/EnemySpawnSystem.js` (`handleEnemyDeath`); `src/data/enemies.js` (Rat/Frog/Troll/Steam Specter) | 2026-07-31 |
| P2 | **Mechanic precedence is a declared priority, not source-line order.** Which of an enemy's mechanics wins a frame is currently encoded entirely as the order of ten hand-written early returns in `Enemy.update()`, several of which return `{suspend: true}` to short-circuit everything below them. Nothing states the intent, so the ordering cannot be read, reviewed, or changed without re-reading the method — and a new mechanic's position in the file silently decides whether it can be overridden. The proposal is a numeric `priority` per mechanic, sorted once by the runner, with the initial numbers chosen to reproduce today's source-line order exactly and a comment saying so: the ordering becomes *visible and editable* before it becomes *different*. The decision worth ratifying is not the mechanism but the commitment — that precedence is data an author sets, which means a mechanic's authority over a frame becomes a tunable rather than an artifact of where it was pasted. Decide: is a flat numeric priority the right expression (vs. explicit `before:`/`after:` relations, or a claims-based resolution where two mechanics claiming the same State is an authoring error), and should conflicting claims fail loudly at load rather than resolving silently. | `src/entities/Enemy.js` (`update()` early-return chain); `src/entities/enemyMechanics/` (all 23) | 2026-07-27 |
| P2 | **`enemy.displayChar` decouples rendered glyph from data key — glyph now means "family," color means "tier."** Plague Rat (`enemies.js` key `'P'`) is the first enemy whose rendered character differs from its own `char`/dict key: `Enemy.js` constructor sets `this.displayChar = this.data.displayChar ?? char`, and `ExploreRenderer`'s default enemy-render branch draws `displayChar` instead of `char` (the one call site shared by both surface and interior-PiP rendering). Plague Rat sets `displayChar: 'r'` and a distinct `color`, so it draws as a green-tinted rat rather than claiming a new letter — the explicit design intent (user's own framing): "Player needs to associate enemy type with the character, a mild memory reward," i.e. glyph is a recognition cue for *family*, color is the cue for *danger tier within that family*, and the two must not both change on a tougher variant. All 124 other `enemy.char` usages (identity checks like `enemy.char === 'M'`, Bomb/Bat-specific render branches) deliberately still key on `char`, not `displayChar` — the dict key stays the unique identity, only the drawn glyph aliases. Decide: is this the sanctioned pattern for every future "tougher variant of X" enemy (vs. a depth-gated stat buff on the same entry, which was explicitly rejected here in favor of a separate entry), and should the Character Encoding Rule's two-tier table (CLAUDE.md) be extended to state the glyph-vs-color convention explicitly rather than leaving it discoverable only from this one call site. | `src/entities/Enemy.js` (constructor `displayChar` field); `src/rendering/state/ExploreRenderer.js` (default enemy-render branch); `src/data/enemies.js` (`'P'` Plague Rat, `displayChar: 'r'`); CLAUDE.md "Character Encoding Rule" | 2026-08-02 |
| P2 | **`strike.bands[]` can now override `damage`/`knockback`/`duration` (implemented 2026-08-01) — open question is whether that's the right permanent shape.** Originally: `band.attack` read as a per-band attack selector but nothing consumed it, so every band produced byte-identical damage and only differed in windup/movement. Fixed by wiring `damage`/`knockback`/`duration` overrides into `Enemy.createMeleeAttack()`/`createWindupAttackVisual()` via `this.stateMachine.band`, landed on Giant Slime as a single always-picked band (no behavior change — see its `states.strike` comment). Still undecided: whether a band should also be able to swap `attack` to a genuinely different shape (e.g. a `telegraph` block, or dispatching to a different attack type entirely) rather than just scaling numbers on the same melee shape — Giant Slime's real near/far idiom (bite vs. `leapAttack`) still can't be expressed as two bands, since Leap is a separate 3-phase Mechanic (airborne arc + shockwave) outside the Strike state (2026-07-27 spine row, §4). Decide whether that gap is worth closing or `leapAttack`-style Mechanics are the permanent answer for shape-changing attacks, with bands staying number-only. | `src/entities/enemyStates/strike.js` (`bands`, `thresholds()`); `src/entities/Enemy.js` (`createMeleeAttack`, `createWindupAttackVisual`, `stateMachine.band`); `src/data/enemies.js` (Giant Slime `'M'`, first real usage) | 2026-08-01 |
| P2 | **First bespoke, multi-part miniboss with a dedicated per-instance coordinating system, distinct from the existing single/formation `BOSS_ENCOUNTERS` spawn model.** Red zone's Centipede (`zones.js` `bossPool: ['centipede']`) is spawned through a new branch in `RoomGenerator`'s miniboss dispatch that calls `CentipedeSystem.spawn()` instead of the generic `spawnBossEncounter()`, using a custom-generated arena template (`stampCentipedeArena` in `roomFeatures.js` — fixed-grid reflectors/rocks/trees/destructible-rocks only) rather than the existing `ZONE_BOSS_TEMPLATES` mechanism (which only runs pre-generation, for true zone bosses). Chain state (`room.centipedeChains[]`: head+body units, a per-chain trail buffer for follow-the-leader movement, `pendingTurn`) lives on the room, not on `game` or on any single `Enemy` instance — the first miniboss where the boss *is* a coordinated multi-entity system (independently-hittable parts that split into new heads on a body hit) rather than one `Enemy` instance, however tough. `CentipedeSystem.js`/`CentipedeUnit.js` are deliberately new, unbudgeted files, kept out of the (already near-ceiling) `main.js`/`RoomGenerator.js` headroom by touching each with only a one-line hook. A real crash was caught in this build during Playwright verification, not static review: `CentipedeUnit.update()` originally returned `undefined`, and `EnemyUpdateSystem`'s canonical per-frame loop unconditionally reads `updateResult.justAggrod` on whatever every `room.enemies` member's `update()` returns — any bespoke `Enemy` subclass that skips this contract freezes the whole game (no try/catch around the render loop) the instant it spawns. Decide: is a bespoke per-miniboss coordinating system + room-scoped state the template for future multi-part minibosses (and should the "every `room.enemies` member must return a truthy update result" contract be documented/asserted somewhere central instead of being a trap each new subclass can fall into), or should the `BOSS_ENCOUNTERS` data model grow a generalized "chain/swarm" encounter type instead of each one reinventing a system file. | `src/systems/CentipedeSystem.js`; `src/entities/CentipedeUnit.js`; `src/systems/RoomGenerator.js` (miniboss dispatch branch); `src/data/zones.js` (`bossPool`); `src/systems/roomFeatures.js` (`stampCentipedeArena`); `src/systems/EnemyUpdateSystem.js` (`justAggrod` contract) | 2026-08-04 |
| P2 | **Dynamic, per-instance, provenance-dependent item color — a starter potion's color/purity is now fixed at brew time and persists to the true potion it becomes, replacing a static per-recipe `ITEMS[char].color`.** `applyPotionModifierColor(item, starterChar)` (`src/data/alchemy.js`) stamps both `item.potionModifier` and `item.color` from a lookup table (`POTION_STARTER_MODIFIERS`/`POTION_MODIFIER_COLORS`), called from two independent commit paths that previously diverged (`AlchemySystem._commitTrue`'s Cauldron path already stamped `potionModifier` but never touched `.color`; `CraftingSystem.claimCraftedItem`'s REST Combine Station path stamped neither). This is the first case of an item's *appearance* — not just its identity/name — being determined by what produced it rather than by its own recipe entry; every prior `Item.js` construction reads `this.color = this.data.color` once and never revisits it. Decide: is per-instance provenance-dependent appearance now sanctioned generally (a template other systems, e.g. weapon tinting by element, could reuse), or is potion purity a one-off special case that doesn't need a named pattern. | `src/data/alchemy.js` (`applyPotionModifierColor`, `POTION_STARTER_MODIFIERS`, `POTION_MODIFIER_COLORS`); `src/systems/AlchemySystem.js` (`_commitTrue`); `src/systems/CraftingSystem.js` (`claimCraftedItem`); `src/entities/Item.js` (`this.color` construction-time read) | 2026-08-08 |
| P2 | **Dungeon floors are now a branching destination graph, not a linear index chain — and the floor count has no explicit cap.** The 6-floor dungeon rework replaced the old `floorIndex + 1`/`- 1` arithmetic with `floor.descents[] = [{id, row, col, obj, active, locked, destination}]` (`destination: {kind:'numbered', floorIndex} | {kind:'side', key}`) plus `floor.ascendTo`, resolved via `DungeonSystem._destinationOf()`/`sameDestination()`/`getFloorByDest()`. This is what makes the Branch floor's 3-way fork (Trap Room / Companion Gate, plus the Corridor floor's own Whip Trial fork) and the Companion Gate's own onward path to the Pyramid representable at all. Two side effects: (1) side rooms are stored as string-keyed properties on the `game.dungeonFloors` array (`.whipTrial`/`.trapRoom`/`.companionGate`) rather than numeric indices — relies on `InteriorManager.reset()` replacing the whole array reference to clear them, not an explicit delete; (2) there is no `MAX_FLOOR_INDEX` constant — the Pyramid (floor index 3) is terminal only because its own `descents` is `[]`, so a real Floor 5/6 (blocked on user-authored boss/legendary-weapon content, not implemented this build) would be added as a new descent rather than a raised ceiling. Decide: is the destination-graph model (vs. a flatter "floors are always linear, branches are a special case") the permanent shape for future interior systems with more than one path, and is string-keyed-properties-on-an-array an acceptable standing pattern for side rooms or does it want a dedicated `game.dungeonSideRooms` map. | `src/systems/DungeonSystem.js` (`ensureFloorGenerated`, `getFloorByDest`, `_destinationOf`, `sameDestination`, `_ascend`); `src/systems/DungeonFloorGenerator.js`; `src/systems/DungeonPuzzleSystem.js`; `src/data/dungeonFloorTemplates.js` (footprint contract); plan `~/.claude/plans/need-a-dungeon-layout-parallel-haven.md` | 2026-08-13 |
| P2 | **Compass item ships with only its Dungeon-scoped "beeps when treasure is present" behavior — the full Explore-mode directional/room-letter pointer is explicit descoped future work.** User's own clarification during the dungeon rework: "The Compass mechanic I mentioned is for the normal Explore, not the dungeon. In the Dungeon, it just beeps." `ITEMS['⌖']` (`effect: 'compassBeep', passive: true`) is currently consumed by exactly one call site, `DungeonPuzzleSystem.updateCompassBeep()` — a throttled proximity poll against `floor.items`/`floor.ingredients`, unrelated to the described Explore behavior (points toward the nearest unexplored direction; 3 consecutive follows guarantee a room-type choice). Decide: does the eventual Explore mechanic reuse `effect: 'compassBeep'` as one passive effect with two contexts (dungeon vs. surface), or fork into a second `ITEMS` effect/data field so the two behaviors don't have to keep sharing one name as they diverge. | `src/data/items.js` (`'⌖'` Compass entry); `src/systems/DungeonPuzzleSystem.js` (`updateCompassBeep`) | 2026-08-13 |
| P2 | **The Whip Trial's hook-post pull generalized into a standing traversal mechanic reachable from ordinary Explore/Dungeon/Maze terrain, not just its dedicated puzzle room.** A whip strike landing on a Stump (`'ŋ'`) or Tree (`'Y'`) background object now grabs and reels the player in exactly like the two purpose-built hook posts in the Whip Trial did — the trigger in `CombatSystem.js`'s melee-vs-object resolution and the trigger in `DungeonPuzzleSystem._updateWhipTrial` both just set `player.hookedByWhip = {targetX, targetY}`; the actual traversal (reel-in speed, arrival snap, i-frames for the duration) was extracted out of `DungeonPuzzleSystem` into `PhysicsSystem.updateEntity` as a shared early-return so every trigger source behaves identically. This is the first case of a mechanic taught inside a one-off dungeon tutorial room becoming a general-purpose interaction with ordinary world terrain — Stumps and Trees are common, unauthored scenery (`RoomGenerator`/`roomFeatures` spawn pools), so any existing or future room that happens to place one near a gap or ledge now implicitly offers whip-traversal across it, without a level designer opting in per-room the way the Whip Trial's geometry did deliberately. Decide: is "ordinary scenery objects double as traversal anchors for a specific weapon family" the sanctioned shape for teaching-room mechanics graduating into the general world (a template other Weapon Trials could reuse — e.g. a future weapon's trick working on some other common prop), or should general-terrain grapple points be opt-in per room/object (a flag) rather than blanket by char, so level design can control where the shortcut exists. | `src/systems/PhysicsSystem.js` (`updateEntity`'s `hookedByWhip` block, `WHIP_PULL_SPEED`); `src/systems/CombatSystem.js` (Stump/Tree whip trigger in the melee-vs-object loop); `src/systems/DungeonPuzzleSystem.js` (`_updateWhipTrial`, original hook-post trigger); `src/entities/Player.js` (`hookedByWhip` field, movement lock in `updateInput`) | 2026-08-18 |
| P2 | **Second instance of a custom-drawn canvas sprite standing in for a font glyph, first applied to a multi-stage destructible object.** The new red-zone destructible rock (bg object char `'9'`, `data.customSprite: 'fracturedRock'`) follows the precedent set by deflector triangles (`ExploreRenderer._drawDeflectorTriangle`, hand-built `beginPath/moveTo/lineTo/fill`) rather than `fillText` — but where deflectors are one static shape, this object tracks `BackgroundObject.damageStage` (0-4, derived from `hp`/`maxHp`) and `drawFracturedRock(ctx, cx, cy, stage, color)` (`src/rendering/sprites/fracturedRockSprite.js`) renders a different hand-authored polygon per stage as it takes damage. `obj.char` stays `'9'` for identity/collision/drop-table lookups the whole time — only the drawn sprite changes, the same "char is identity, not necessarily what's drawn" split as the Plague Rat `displayChar` row above, expressed through geometry instead of a font-glyph swap. Decide: is "custom canvas sprite, dispatched via a `data.customSprite` discriminator, with a per-stage geometry table for destructibles" now the sanctioned pattern for any future non-font visual (vs. each one hand-rolling its own dispatch key), and should `ExploreRenderer`'s render-branch dispatch (`boulderDeflector` / `customSprite === 'fracturedRock'` / fallback `fillText`) be generalized into a single sprite-registry lookup before a third case adds a third hardcoded branch. | `src/game/GameConfig.js` (`'9'` Fractured Rock entry); `src/entities/BackgroundObject.js` (`damageStage`); `src/rendering/state/ExploreRenderer.js` (render dispatch); `src/rendering/sprites/fracturedRockSprite.js` | 2026-08-04 |
| P2 | **New "held, not equipped" narrative-key classification — a key item can now exist as a boolean flag with no backing `Item`/inventory slot at all, surfaced through a dedicated 4th inventory-overlay tier.** Two keys now follow this shape: the dungeon Skull Key (run-scoped `game.dungeonKeyObtainedThisRun`, set on last-skull-clear, never a quick-slot item) and the K-room Vault Key (room-scoped `room.vaultInfo.keyHeld`, migrated off its former `Item`/`AFFINITY_POOLS` weapon-slot representation this session). Both are declared in a new `KEY_ITEM_DEFS` registry (`InventoryOverlay.js`) — `{char, name, held: (game) => boolean}` — rendered in a new KEY ITEMS section alongside the existing TREASURE/COMPONENTS/MATERIALS tiers, with boolean-held items skipping the "xN" count suffix. This is the first inventory concept with no physical representation anywhere in `game.player.inventory`/quick slots — existence is entirely a predicate function evaluated at render time. Decide: is a `held` predicate on a static registry the standing pattern for all future single-use narrative keys (vs. e.g. a real lightweight `Item` subtype that simply refuses equip-slot placement), and should run-scoped vs. room-scoped flag placement be a documented rule of thumb (tied to the interior/backtrack-persistence semantics already documented for `dungeonKeyObtainedThisRun`-style flags) rather than a per-key judgment call. | `src/rendering/ui/InventoryOverlay.js` (`KEY_ITEM_DEFS`, `_renderCategorySection`'s `nameOverrides`); `src/systems/RoomGenerator.js` (`vaultInfo.keyHeld`); `src/systems/InteractionSystem.js` (`canUnlockVault`/`unlockVault`/`dropsKey` branch); `src/data/items.js` (`AFFINITY_POOLS` exclusion) | 2026-08-18 |
| P2 | **First template-driven puzzle-room authoring pathway, generalized off one hardcoded precedent — a generic trigger state machine now backs both a hand-authored dungeon side room and any future editor-built one.** Every existing dungeon side room (Whip Trial, Companion Gate, Trap Room) is a bespoke function in `DungeonFloorGenerator.js` — hardcoded grid, hardcoded fixture placement, hardcoded per-room update logic in `DungeonPuzzleSystem.js`. This adds a second, parallel authoring path: a JSON template (`grid` + `triggers[]`, one file per room under `src/data/dungeon/puzzleTemplates/`, loaded via static import in `dungeonPuzzleTemplates.js` mirroring the existing floor-template pattern) consumed by a new generic `DungeonFloorGenerator.generatePuzzleRoom(templateName, ...)` and a new generic `DungeonPuzzleSystem._updatePuzzleRoom()`/`_advanceTrigger()` runtime loop, authored via a new third mode (`'puzzle'`) in `tools/dungeon-editor/`. Two triggers kinds were made deliberately distinct rather than one type with a mode flag — strike-based **Switch** (weapon-attack triggered, reuses the `puzzleSignal`/`glitterHit` contract) and occupancy-based **Panel** (walk-on triggered) — each independently configurable as `permanent` (never reverts once struck/stepped-on) or `timed` (reverts `neutralizeSeconds` after the last trigger event), a vocabulary with no prior precedent in the codebase (the Whip Trial's switches were refactored onto this same generic `activation`/`neutralizeSeconds`/`_advanceTrigger` machinery in the same pass, replacing its bespoke `recentlyStruck`/`_struckTimer` fields — so the hardcoded room and the new generic path now share one runtime implementation, not two). This build is deliberately **infrastructure only**: verified via a headless scratch harness (`tools/debug/`, deleted after use), the generator/editor/schema work end-to-end, but no editor-built template is wired into the reachable `descents[]` floor graph (2026-08-13 row above) — that remains a future content decision. Decide: is grid+triggers JSON (vs. some other template shape) the durable authoring contract for future side rooms generally, should the existing hardcoded rooms (Whip Trial/Companion Gate/Trap Room) eventually migrate onto template files for consistency or stay bespoke as "the tutorial precedent," and is Switch/Panel as two closed, separately-typed trigger kinds (vs. a single kind with a `walkOn: boolean` field) the pattern later trigger kinds (e.g. a timed pressure plate requiring N simultaneous occupants) should follow. **Update 2026-08-22:** Whip Trial has now migrated onto this pathway, resolving the first half of the "Decide" above — `generateWhipTrial()` and `_updateWhipTrial()` are deleted; its exact geometry (chasm, hook posts, pedestal, both switches) now lives as `src/data/dungeon/puzzleTemplates/whip_trial.json`, generated and updated by the same generic `generatePuzzleRoom()`/`_updatePuzzleRoom()` this row introduced — it is, mechanically, just the pool's second template. Corridor's North descent now rolls `pickRandomPuzzleTemplateName()` once per dungeon visit (weighted, mirroring `dungeonFloorTemplates.js`'s own pool pattern) instead of hardcoding `'whipTrial'`; the picked name is itself the `game.dungeonFloors` storage key, extending the pre-existing "key = content identity" convention (`DungeonSystem._destinationOf` now special-cases `roomKind === 'puzzleRoom'` to read `floor.templateName`, since every pool entry now shares that one `roomKind`). This also grew the schema past what this row originally scoped: a `weight` field, a `'G'` grid char for impassable Gap cells (void render with directional edge glyphs at the walkable boundary), `hookPosts[]` (manually placed, not auto-derived from Gap), `torches[]` (maze-parity ignite-on-proximity-while-wielding-Torch fixtures — a new `PuzzleTorch` class reusing `MazeTorch`'s exported visual constants rather than importing the class itself, per the Interior System Pattern), and a `pedestal` marker generalizing what was Whip-Trial-exclusive weapon-tutorial furniture into an opt-in any template can set — the editor's Puzzle palette and `validatePuzzleTemplate` grew matching tools for all four. Companion Gate and Trap Room remain bespoke — the second half of the "Decide" (should they migrate too, and is grid+triggers+fixtures JSON the durable authoring shape generally) is still open. | `tools/dungeon-editor/` (`'puzzle'` mode, `index.html`, `main.js` validation, `preload.js`); `src/data/dungeonPuzzleTemplates.js`; `src/data/dungeon/puzzleTemplates/*.json`; `src/systems/DungeonFloorGenerator.js` (`generatePuzzleRoom`, `PuzzleTorch`); `src/systems/DungeonPuzzleSystem.js` (`_updatePuzzleRoom`, `_advanceTrigger`, `_within`); `src/systems/DungeonSystem.js` (`_destinationOf`); `src/systems/MazeSystem.js` (`TORCH_INTERACT_RADIUS` export); `src/rendering/ui/HutInteriorOverlay.js` (gap/torch rendering); GLOSSARY.md "Puzzle Room" / "Switch" / "Panel" / "Trigger" / "Activation" / "Neutralize" / "Gap" / "Torch" (fixture) / "Hook Post" (pending) | 2026-08-21 |
| P2 | **First non-flat coin price ladder, and the game's first three-lane payment-substitution model (ingredient / coin / treasure) for a single purchase.** Every existing coin-spend prior to this (Weapons Master, Camp NPC, Fisherman, Well) is a flat 1 coin, occasionally 2 — a constant, not a formula. The Settlement Shopkeeper hut introduces `src/data/shopPricing.js`'s `BASE_COIN`/`COIN_PER_TIER` tables (`baseCoins = BASE_COIN[role] + T * COIN_PER_TIER[role]`, T derived from weapon `data.tier` or, for armor/consumables with no tier field, a new reverse-lookup `getIngredientRarity()` against `AFFINITY_POOLS`), plus a `coinFloor` that ingredient-toggling can shrink the price toward but never past. Layered on top: each shop listing offers three independent ways to pay for the same item — toggle padded ingredients (worse than the item's real 2-ingredient recipe, an explicit "worse than crafting" tax) to discount the coin price, pay the full (possibly-discounted) coin price outright, or hand over one specific Treasure item as a full-price substitute that bypasses coins/ingredients entirely (`ShopSystem._toggleLane`/`isCostMet`/`_confirmPurchase`). No prior NPC interaction in the codebase lets a single purchase be completed by more than one payment shape, and none has a price that scales off item rarity rather than being hardcoded. Decide: is the tier-derived scaling formula (`BASE_COIN`/`COIN_PER_TIER`/`coinFloor`) the intended permanent shape for coin pricing generally (a template future paid interactions should reuse rather than reinventing their own flat constant), and is the three-lane ingredient/coin/treasure substitution model specific to the Shop's "worse-than-crafting sink" framing or a pattern other future purchases (e.g. a second shop, a black-market hut) should inherit as-is. | `src/data/shopPricing.js` (`BASE_COIN`, `COIN_PER_TIER`, `getIngredientRarity`, `rollShopStock`); `src/systems/ShopSystem.js` (`_toggleLane`, `isCostMet`, `getRemainingCoinPrice`, `_confirmPurchase`); `src/entities/Shopkeeper.js` | 2026-08-21 |
