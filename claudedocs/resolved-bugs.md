# Resolved Bugs

Archive of fixed bugs from `known-bugs.md`. Migrated here on resolution so the active bug list stays focused on open work. Newest first.

---

## P1 — Confirmed Broken (Resolved)

| # | Bug | Source | Resolution |
|---|-----|--------|------------|
| 235 | **No hut could be entered at all — `generateHutInterior` threw `ReferenceError` on every single hut** — the spawn-occupancy helper `cellOccupied()` closes over `npcs`, but `const npcs = []` was declared ~80 lines further down, below the interior-decor loop that calls the helper. A `const` binding is in the temporal dead zone until its declaration executes, so the first `cellOccupied()` call threw `Cannot access 'npcs' before initialization`. `decorCount` is `1 + rand(0..2)` and never zero, so that call always happened, for every `hutKind`: `_enterHut()` aborted mid-transition and SPACE at a hut door silently did nothing. Introduced by the NPC-exclusion radius added to `cellOccupied` in 70255d2 (frog hut). | user 2026-09-02 ("Why can i no longer enter huts?") | ✅ fixed — 2026-09-02 — `enemies`/`npcs` hoisted to the top of `generateHutInterior`, above the helper that reads them, with a comment naming the TDZ so they don't drift back down to their point of use. |
| 236 | **A defeated-or-abandoned Hoardmaw could survive the run reset and stay on the vault floor's enemy roster** — `DungeonBossSystem` had no teardown: `reset()` dropped its own references but left the body in `floor.enemies`, and dying inside the vault never routes through `DungeonSystem._exitDungeon`, so `resetRunState()` (death/title) left the encounter half-live. The cached floor object outlives a reset, so a re-entered vault could present a stale boss. | self-spotted 2026-09-02 (audit of uncommitted Ascent/Hoardmaw work) | ✅ fixed — 2026-09-02 — the system now tracks `vaultFloor` and has `_teardownEncounter()` / `_leaveFloorRoster(maw)`; `reset()` and `resetRunState()` both tear down, and `_defeat()` leaves the roster through the same helper. |
| 237 | **Enemy projectiles were destroyed on red-zone deflector triangles instead of being bent by them** — in `CombatSystem`'s enemy-projectile loop the background-object collision pass ran BEFORE `_tryDeflectorRedirect(proj)`, so a shot that hit a deflector resolved as a wall impact and died. The player-projectile loop already runs the redirect first; the two paths had silently diverged. | self-spotted 2026-09-02 (audit of uncommitted Ascent/Hoardmaw work) | ✅ fixed — 2026-09-02 — the redirect check moved above the collision loop (matching the player path), and `boulderDeflector` objects are skipped inside the loop so a grazing hit can't consume the shot either. |
| 238 | **The cyan Ascent's ice ring thawed on frame one — the room was open water before the player ever saw ice** — `seedFrozenAscentCycle` (and `IceAscentSystem._advanceRefreeze`) called `setWaterState('frozen', 0)`. `BackgroundObject.update` ticks `waterStateTimer` down and reverts to `'normal'` the moment it reaches zero, so a 0-second freeze is a no-op; the whole `stable` phase and the entire `refreezing` phase were cosmetic. Every other permanent freeze in the codebase (Freeze-Over sweep, ice stream, frost traps) passes `Infinity`. | self-spotted 2026-09-02 (audit of uncommitted Ascent/Hoardmaw work) | ✅ fixed — 2026-09-02 — both sites pass `Infinity`, with the reason recorded at each. |
| 239 | **The gray Ascent's bone-slope belt moved without the picture changing — the ring the player walked on and the ring they saw disagreed** — `MistAscentSystem._rebuildSlopes()` splices and re-pushes static `BackgroundObject`s on room clear, but those live in the cached background layer, which only repaints when something dirties it. The new belt stayed invisible until an unrelated event forced a repaint. | self-spotted 2026-09-02 (audit of uncommitted Ascent/Hoardmaw work) | ✅ fixed — 2026-09-02 — `_rebuildSlopes` calls `renderer.markBackgroundDirty()` after the rebuild. |
| 240 | **A cyan Ascent room cleared during the 6-second open-water window skipped ALL of its room-clear bookkeeping** — `IceAscentSystem`'s `FILL_PHASES` listed `'broken'`, but `onPhaseStart('broken')` is the phase that explicitly sets `room.exitsLocked = false`. `_getFillPhases()` names the phases where exits stay locked, and `ExitSystem.updateRoomClearState` early-returns whenever `isHazardActive()` is true — so for the whole broken phase the captive spawn, `zoneSystem.recordRoomClear`, secret events, `mistAscentSystem.onRoomClear` and the pre-boss gate never ran, and the hazard's unlock stuck even with enemies still alive. `LavaAscentSystem` draws the line correctly: its fill phases end exactly where its unlock begins. | self-spotted 2026-09-02 (audit of uncommitted Ascent/Hoardmaw work) | ✅ fixed — 2026-09-02 — `FILL_PHASES = ['cracking']`. The file header's phase doc was also wrong (it described the cycle as repeating and claimed `refreezing` re-locks exits) and now matches what `PhasedHazardSystem.update` actually does. |
| 241 | **[rest-parity] The gray Ascent's bone-slope grab could lock the player's movement for the rest of the run** — `boneSlopeLock` was ticked down inside `MistAscentSystem.update()` behind the `isActive(room)` gate, and the system was dispatched only from `updateExploreState`. The slope belt pushes outward, straight at the room border, so a player grabbed near the edge is routinely carried through an exit mid-lock — into the next room (gate false, timer frozen) or south into REST (dispatch absent entirely). Either way the timer never reached zero and `Player.update`'s movement clamp held forever. | self-spotted 2026-09-02 (audit of uncommitted Ascent/Hoardmaw work) | ✅ fixed — 2026-09-02 — the lock ticks BEFORE the room gate, and the system moved to `updatePlayerMechanics` (the state-shared player path) as a net-zero dispatch move. `_playerOnBoneSlope` is also cleared on the way out so a stale edge can't swallow the next room's first grab. The fix deliberately lives in the system rather than `Player.update` — Player.js is already over its architecture budget from unrelated in-flight edits. |
| 242 | **The Mirror Imp / Ice Golem reflect shield was completely inert — it ate the arrow, popped "REFLECT", and the return shot passed through the player** — `ReflectShieldMechanic.tryReflect()` tagged its bounced projectile `reflected: true`. That flag does not mean "this projectile was reflected"; it means "already bounced, now traveling back toward the boss", and `CombatSystem`'s enemy-projectile loop skips the player hit test entirely for anything carrying it (which is what `BossSystem`'s reflect minigame and the deflector triangles want). The shield's bounce is the opposite case: a live enemy projectile aimed at the player. | self-spotted 2026-09-02 (audit of uncommitted Ascent/Hoardmaw work) | ✅ fixed — 2026-09-02 — the tag is gone, with a comment at the push site recording what `reflected` actually means so it isn't re-added. |
| 243 | **[damage-number-desync] Yellow Ascent charged-metal contact applied stun, knockback and damage numbers for damage that was never dealt** — both contact handlers in `StormAscentSystem` called `takeDamage()` and then acted unconditionally. `Player.takeDamage` no-ops under god mode and several other states beyond the iframe check the handler did make, and the enemy handler has no per-enemy cooldown at all — so a standing enemy sprayed one damage number per frame while its own post-hit iframes absorbed everything. | self-spotted 2026-09-02 (audit of uncommitted Ascent/Hoardmaw work) | ✅ fixed — 2026-09-02 — both sites capture the return value and gate the stun, knockback and numbers on it. |
| 223 | **Hoardmaw's hit geometry was one cell, offset down-right of a ~6×4-cell body — phase 2 effectively unhittable** — `Hoardmaw` treats `position` as the body-block CENTER (every draw in `HoardmawRenderer` and every helper — `bodyRect()`, `rootX()`, `mouthX()` — reads it that way), but it inherited `Enemy.getHitbox()`, which treats `position` as a TOP-LEFT corner and returns a single `CELL_SIZE` box. Hit tests therefore ran against ~4% of the visible mass, landing a cell down-right of center: most of the phase-1 scale field and two of the three phase-2 glints sat outside the box entirely. `LakeBoss` and `GooHead` already override `getHitbox()` for exactly this reason — composite bodies must publish their real extent. | self-spotted 2026-09-02 (audit of uncommitted Ascent/Hoardmaw work) | ✅ fixed — 2026-09-02 — added a `getHitbox()` override on `Hoardmaw` returning `bodyRect()`'s full span. No collision ripple: `PhysicsSystem.checkEntityCollision` is debris-only. |
| 224 | **Every located hit reported its box's top-left corner as the strike point, biasing hit-location lookups up-left by half a box** — Both `takeDamage` call sites in `CombatSystem` passed `proj.position.x/y` and `attack.position.x/y` as `opts.px/py`. Those are the hit box's top-left (see `checkProjectileCollision`), not the point of contact. Any enemy that routes damage by location — Hoardmaw refuses unlocated hits outright, so the whole encounter is positional — resolved the wrong sub-part, or none, for hits landing in the upper-left half of a box. | self-spotted 2026-09-02 (audit of uncommitted Ascent/Hoardmaw work) | ✅ fixed — 2026-09-02 — both sites now pass the box center (`position + width/2`, `position + height/2`). |
| 225 | **A `riseAgain` enemy collapsed mid-frenzy kept the buff flag, re-captured its already-buffed stats as the new baseline, and compounded permanently** — `RiseAgainMechanic` restored `_preFrenzySpeed`/`_preFrenzyDamage` only when the frenzy timer expired naturally, and the timer only ticks while upright. Collapsing during a frenzy left `frenzyActive` true with buffed `speed`/`damage` live; the next frenzy overwrote the pre-frenzy snapshot with those buffed values, so the multiplier stacked and the restore could never undo it. Reachable today on the Yeti (`maxRises: 2`) — it rises at ~2.25× speed and 4× damage. | self-spotted 2026-09-02 (audit of uncommitted Ascent/Hoardmaw work) | ✅ fixed — 2026-09-02 — added an idempotent `endFrenzy(enemy)` called from both `collapse()` and timer expiry. |
| 226 | **Yellow Ascent charged the metal at strike SCHEDULE time, so the 0.7s telegraph was a lie and every charge ran 0.7s short** — `StormAscentSystem.redirectStrike()` set `strikeFlash` and called `_chargeNearby()` inline while redirecting the bolt. `LightningStrikeSystem` schedules with a 0.7s delay that is the player's entire dodge window — the objects went live during the warning, and the 0.7s came out of `CHARGE_DURATION`. `SandstormSystem` also reached for the strike by indexing the strike array's tail rather than using the record `scheduleStrike()` returns. | self-spotted 2026-09-02 (audit of uncommitted Ascent/Hoardmaw work) | ✅ fixed — 2026-09-02 — `scheduleStrike` gained an `onResolve` impact hook fired once from `_resolveStrike`; the flash and charge now ride it. `SandstormSystem` uses the returned record. Dead `PhasedHazardSystem`/`PHYSICS` imports dropped from `StormAscentSystem`. |
| 227 | **Charged-metal contact never stunned the player and console-errored every hit** — `StormAscentSystem._checkPlayerContact` called `player.applyStatusEffect('zap', …)`. `zap` is an enemy-only status slot — the player's slots are goo/freeze/slimeBoost/dizzy — so the bug-#166 guard rejected it loudly and the stun was a guaranteed no-op. Charged contact dealt damage and knockback but no disorientation. | self-spotted 2026-09-02 (audit of uncommitted Ascent/Hoardmaw work) | ✅ fixed — 2026-09-02 — uses `dizzy` (scrambled inputs + gold blink, the same gold as the arc), the player's shipped disorientation effect. |
| 228 | **The gray Ascent's bone-slope grab was entirely inert — `applyBonePush` had zero callers** — `MistAscentSystem.applyBonePush()` and `Player.boneSlopeLock` were both written and wired into the movement branch, but nothing ever called the push. The intended trigger site is `PhysicsSystem`'s terrain pass, which holds no game reference and so could not reach the system. | self-spotted 2026-09-02 (audit of uncommitted Ascent/Hoardmaw work) | ✅ fixed — 2026-09-02 — `MistAscentSystem.update()` now detects the step-ONTO edge itself via a new `_boneSlopeUnder()` overlap test and fires the grab once per entry. Edge-triggered on purpose: a per-frame refresh would pin the player to the slope for as long as they touched it. |
| 229 | **Charged background objects froze mid-blink — the only cue that the metal is live never animated** — `chargedColor()` blinks off `performance.now()`, but it was applied inside `ExploreRenderer.renderBackground`, which early-returns unless `renderer.backgroundDirty`. The spire and metal slope grating were baked into the cached background layer on whichever half of the blink happened to be current and stayed there. Ground items were fine (foreground pass). Campfires, burning objects and Sinkholes are already `continue`d out of that loop for the same reason. | self-spotted 2026-09-02 (audit of uncommitted Ascent/Hoardmaw work) | ✅ fixed — 2026-09-02 — charged objects are skipped by the background loop and drawn each frame by a new `renderChargedObjects()` in `AscentRenderHelpers`; `StormAscentSystem._setCharged()` dirties the background on both edges so the static glyph is erased on charge and re-baked on discharge. |
| 230 | **Compacted deep snow kept rendering as fresh snow — the player's trail was invisible until an unrelated event forced a repaint** — `PhysicsSystem` sets `obj.compacted` and swaps `obj.color` to the variant's `compactColor` on contact, but the snow tile is drawn as a `fillRect` in the dirty-gated background pass and `PhysicsSystem` holds no renderer reference to request a repaint. | self-spotted 2026-09-02 (audit of uncommitted Ascent/Hoardmaw work) | ✅ fixed — 2026-09-02 — rooms now carry a `backgroundRepaint` flag (initialized in the `RoomGenerator` factory, not lazily); the compaction site raises it via the `room` argument it already receives, and `ExploreRenderer.renderBackground` consumes and clears it before the dirty gate. |
| 231 | **Yeti frenzy flicker erased iframe flash, windup white, DOT blink and the near-death warning on half of all frames** — The frenzy branch in `ExploreRenderer` resolved its off-phase to `enemy.color`, discarding the `displayColor` computed immediately above it — including the near-death blink that the adjacent comment calls the highest-priority signal. A frenzied Yeti taking a hit showed no damage feedback half the time. | self-spotted 2026-09-02 (audit of uncommitted Ascent/Hoardmaw work) | ✅ fixed — 2026-09-02 — the off-phase falls back to `displayColor`. |
| 232 | **The Ascent exit gate re-enumerated each hazard's fill-phase names by hand [drift]** — `ExitSystem.updateRoomClearState` hardcoded `'fillingFloor'/'fillingSlopes'` and `'cracking'/'broken'` — duplicates of `LavaAscentSystem`/`IceAscentSystem`'s `FILL_PHASES`. `PhasedHazardSystem.isHazardActive(room)` exists precisely to answer this and had zero callers, so renaming or adding a fill phase would have left the gate silently stale (#142 drift shape). Two hardcoded glyph tests carried the same fragility: `data.char === 'I'` gated the Ice Golem's windup shield drop in both `CombatSystem` and `ExploreRenderer`, and `data.char === 'u'` gated the Glacier Crab's deep-snow trail exemption — and `'I'` already collides with a background-object char (#102). | self-spotted 2026-09-02 (audit of uncommitted Ascent/Hoardmaw work) | ✅ fixed — 2026-09-02 — the exit gate calls `isHazardActive()` on both systems. The shield check moved to `ReflectShieldMechanic.isShieldDown()` behind a new `reflectShield.dropsDuringWindup` data flag, consumed by both the projectile check and the indicator; the trail exemption became `trailMechanic.suppressInDeepSnow`. The projectile bounce itself moved from `CombatSystem` into `ReflectShieldMechanic.tryReflect()` (the mechanic owns its own contract, and `CombatSystem` was over its architecture budget). Also restored three explanatory comments the `PhasedHazardSystem` extraction had dropped from `LavaAscentSystem`, including the load-bearing note that PhysicsSystem's lava detection keys off `char === '~'`. |
| 221 | **Bare `game` reference in `InventorySystem.updateConsumableWindups` threw ReferenceError on every EXPLORE frame — game froze on room entry** — the 2026-08-24 layer-guard refactor (commit `4d94307`) replaced `const enemies = currentRoom ? currentRoom.enemies : []` with `game._activeEnemies()`, but `InventorySystem` methods own `this.game`; there is no module-scope `game` anywhere (and nothing sets `window.game`). First EXPLORE dispatch after room entry threw, and because `GameLoop.loop` re-registers its rAF callback *after* the update callback runs, the uncaught exception killed the loop permanently — freeze on entering any room. Found headlessly while smoke-testing the true-name Command feature (`CommandSystem.update` was never dispatched because the throw aborted `updateExploreState` every frame); swept the rest of `4d94307`'s diff for the same shape — every other new bare-`game` site sits inside a function that aliases (`const game = this.game`) or takes `game` as a parameter. | self-spotted 2026-08-25 (true-name Command smoke test) | ✅ fixed — 2026-08-25 — `this.game._activeEnemies()` with a comment naming the regression. |
| 216 | **[clock-mismatch] LakeBoss timers run at 3× authored speed — the boss is stepped twice per frame on two different clocks** — LakeBoss is pushed into `room.enemies` (BossSystem.js:93), so `EnemyUpdateSystem._runEnemyLoop` calls `enemy.update(deltaTime * PHYSICS.ENEMY_TIMER_RATE)` on it like any Enemy (2× clock), and `BossSystem._updateLakeBoss` additionally calls `boss.update(deltaTime)` every frame (raw 1× clock). `LakeBoss.update` has no internal multi-drive guard — it unconditionally advances its state timers (hammer countdown, mouth cycle, jump arc, shot stagger, invulnerability) by whatever deltaTime it receives; the `_frameUpdateResult` canonical-tick cache only dedupes CombatSystem's *read* of update results (bug #92's pattern), not this double drive. Net effect: every timer runs at ~3× authored speed, so any phase-2 timing authored against real seconds lands divided by 3. Decision taken upstream (2026-08-23): fix the tick first — pick one canonical driver and make `_updateLakeBoss` a pure consumer of its results — then author/rebalance phase 2 against the corrected clock; do not tune phase-2 data to absorb the factor. | self-spotted 2026-08-23 (LakeBoss phase-2 planning) | ✅ fixed — 2026-08-24 — `EnemyUpdateSystem._runEnemyLoop` is now the sole driver: `BossSystem._updateLakeBoss` no longer calls `boss.update()` and is a pure consumer of the canonical tick. Two ordering seams the removal exposed were closed rather than papered over: (1) the slam-entry detection used to sample `prevState` around the local drive, which would have gone dead — `LakeBoss._transitionTo` now raises a one-shot `enteredSlamming` flag that BossSystem drains, which also catches slams triggered from `takeDamage()` inside CombatSystem, a transition the old sampling missed entirely; (2) `target`/`shockwaveActive` were assigned before the local drive and would now land a tick late — `target` is set once at `activate()` (it never changes) and `shockwaveActive` is staged at the end of the method for the next tick, with a comment saying why it is last. Per the upstream decision, timing constants were NOT rescaled to absorb the removed factor: the boss now advances 2×dt instead of 3×dt per frame, so phase 1 runs 1.5× slower and is re-balanced by playtest against the corrected clock. |
| 198 | **Leftover ARCADE_DEMO pickup-message text (e.g. "SPEAR") bleeds into the first REST screen of a real run** — `MenuSystem.showPickupMessage`/`showNextPickupMessage` set `game.pickupMessage`/`game.pickupMessageTimer` (`main.js:359-362`) on any pickup, including the demo's synthetic replay input — `enterDemoState()` runs ARCADE_DEMO through the real EXPLORE update loop, so a replayed weapon pickup drives the identical code path. Any keypress aborts playback straight to `enterTitleState()` (`main.js:568-571`), which resets a long list of run state (`zoneDepths`, `physicsSystem`, `inventorySystem`, etc. — `main.js:874-892`) but never touches `pickupMessage`/`pickupMessageTimer`/`pickupMessageQueue`; `enterRestState()` doesn't clear them either. If the abort lands mid-message (before the 2s `PICKUP_MESSAGE_DURATION` countdown finishes), the leftover item name and its still-running timer survive TITLE → REST, and `RestRenderer.js:645` draws it as though the player just picked that item up in the brand-new run. | ✅ fixed — 2026-08-24 — MenuSystem.clearPickupFeedback() (pickupMessage/timer/queue) called from both full-reset paths alongside restBundle=null and hasLeftRestOnce=false; enforced going forward by tools/check-reset-parity.mjs, which drives the real Game headlessly through REST→EXPLORE with pickup dirt and diffs contract-surface state against a virgin instance driven to the same terminal state. Also caught same-shape drift: _savedMagicMeter resurrected after its clear (enterRestState stamped the dying player's inactive meter back — capture now requires meter.active) and spectaclesObtainedThisRun was lazy-initialized at its set site (now declared in the constructor). | open |
| 132 | **[warp-divergence]-adjacent: NEUTRAL→EXPLORE return may discard the just-restored room** — `updateNeutralState` hand-restores `currentRoom`/`items`/`ingredients` from `this.savedExploreState` on exit (a field separate from `InventorySystem.savedExploreRoom`), then used to call `stateMachine.transition(GAME_STATES.EXPLORE)` → `enterExploreState(entryDirection=null)`. That function's `shouldRestoreExploreRoom` check reads the *other* saved-room slot (`inventorySystem.getSavedExploreRoom()`, populated only on EXPLORE→REST) — `null` in the ordinary EXPLORE→NEUTRAL→EXPLORE case — so `enterExploreState` fell into its "generate a brand-new room" branch, overwriting the room `updateNeutralState` had just restored two statements earlier. | 2026-07 architectural review (`claudedocs/layer-audit-2026-07.md`) | ✅ fixed — landed by commit `7003e84` (predates this doc entry being migrated) — `updateNeutralState`'s return-to-EXPLORE now sets `this.stateMachine.currentState = GAME_STATES.EXPLORE` directly instead of calling `stateMachine.transition()`, bypassing `enterExploreState()`'s generic restore/generate branch entirely (same defensive pattern `enterMazeTestRoom` already used, main.js:4742) since `updateNeutralState` had already restored the room itself. Confirmed via `git log -L` on the call site; the code-level fix predates this migration — this entry was only updated 2026-08-23 to move it out of the open list, not to fix new code, after being asked whether it shared #212's root cause (related family — both are gaps in `enterExploreState()`'s single-slot restore contract — but distinct code paths, fixed differently). |
| 212 | **Dying with a living second character (character-swap continuation) discarded the EXPLORE room the death happened in — the next hero's first EXPLORE entry generated a brand-new room instead of resuming the one the previous character died in** (e.g. died in a Q/Quagmire room, continued with the next hero, was dropped into a freshly-generated B room instead). Root cause: `enterExploreState()`'s anti-room-cycling-cheat restore (`shouldRestoreExploreRoom`, driven by `InventorySystem.savedExploreRoom`) only gets populated by the voluntary south-exit path (`saveExploreRoom(...)` call before `stateMachine.transition(GAME_STATES.REST)`); the character-death path transitioned straight from `GAME_OVER` to `REST` via `_switchToCharacterAtRest` without ever calling it, so the saved-room slot stayed `null` and the next EXPLORE entry fell into the "generate new room" branch. The character-swap continuation deliberately preserves the rest of the run (zone depths, unlocked/dead-character rosters, gray snapshots — untouched by `_switchToCharacterAtRest`, unlike the full `_resetRunToRest` reset), so leaving the death room un-persisted was inconsistent with every other EXPLORE→REST transition. | user-reported 2026-08-23 ("died in Q, had a 2nd character hero, entered EXPLORE and was brute forced to B room, expected to go back to Q with exact same room state, same enemies, etc.") | ✅ fixed — 2026-08-23 — extracted the south-exit's save-before-REST logic into a shared `captureExploreRoomForRest(game)` helper (`RoomStatePersistence.js`) and call it from both the voluntary south-exit site and the character-death handoff (before the `GAME_OVER` transition, only on the "other characters still living" branch — full game-over already discards the whole run). While routing the addition out of `main.js` to stay under its architecture budget, also moved `_respawnNextCharacter`/`_switchToCharacterAtRest` off `Game` onto `CharacterSystem` (`respawnNextCharacter`/`switchToCharacterAtRest`) — character-death handoff is that system's domain, not orchestration; updated both call sites (`main.js`, `GrayZoneSystem.js`'s mist-out path, which shares the same handoff). Build + `check:arch` green (main.js budget re-ratcheted down after the net extraction). Not yet live-verified in a real death — needs a playtest pass (die with a second character available, confirm the resumed room matches pre-death state) to close out. |
| 211 | **Errand NPC (E room) could spawn on lava tiles** — `ErrandSystem.spawnErrandCharacter()` always placed the traveler at the raw room-centre pixel (`(GRID.COLS/2, GRID.ROWS/2) * CELL_SIZE`) with no hazard check, unlike every other NPC/enemy spawn path in the codebase, which routes through `RoomGenerator.getRandomPosition()`'s liquid-tile rejection (water/lava/mud all render as `~`). In RED zone rooms where lava structures can land on/near room centre (caldera generation, molten-ascent cycling), the errand traveler had no lava immunity of its own and spawned directly on it. | user-reported 2026-08-23 | ✅ fixed — 2026-08-23 — added `ErrandSystem._findSafeSpawnPosition(room)`, which checks room centre against `room.collisionMap` and liquid background objects (`~`/`=`, matching `RoomGenerator`'s own `LIQUID_CHARS` convention) and spirals outward ring-by-ring to the nearest clear tile if blocked. `onRoomClear()` and `spawnErrandCharacter()` now take `room` as a parameter; both call sites (`ExitSystem.updateRoomClearState`, `main.js` E-room re-entry) pass `game.currentRoom`. Build + `check:arch` green. |
| 210 | **The guaranteed-miniboss `'B'` exit could silently point at a different zone's color** — `ExitSystem.generateExits()` forces a `'B'` letter into a random exit slot from L5 onward to guarantee the current zone's miniboss stays reachable (`!this.zoneSystem?.clearedZones?.has(zoneType)`), but that forcing happens *before* `assignExitColors()` runs, which independently rolls a random slot to recolor with an alternate zone's color. If the alt-color roll landed on the same slot the `'B'` was forced into, the exit kept its `'B'` letter but showed a different zone's color — and since `room.zone` (and therefore which zone's miniboss pool actually spawns, via `spawnMinibossOrFallback`) follows the exit's color, not its letter, the "guaranteed this zone's miniboss" promise silently broke: the player would be routed into a different zone's miniboss pool instead, and the gating checks (`clearedZones`, `isMinibossPoolExhausted`) that ran against the *current* zone no longer matched what actually spawned. The two other forced-`'B'` call sites (pre-boss and pre-miniboss gates, `ExitSystem.js`/`ZoneSystem.js`) already pinned `color: ZONES[zone].exitColor` explicitly — only the initial random-generation path missed it. | user-reported 2026-08-22 ("in colored zones, the force 'B' exit logic needs to ensure that the B is always the same color as the current zone") | ✅ fixed — 2026-08-22 — `generateExits()` now tracks which slot index the `'B'` was forced into (`forcedBossIndex`) and, after `assignExitColors()` runs, unconditionally pins that slot's color back to `ZONES[zoneType].exitColor`, overriding any alt-zone color the random pick assigned. Naturally-rolled `'B'` letters (not the forced guarantee) are untouched — they can still legitimately carry an alt-zone color, same as any other letter. Build + `check:arch` green. |
| 209 | **The companion-switch puzzle was invisible to any player who hadn't already recruited a companion — no switches, nowhere to interact — because it lived in a side room (Companion Gate) off Branch's East descent, and that descent itself rendered as a literal blank space without a companion**, contradicting the user's repeated report that "the pressure plates are STILL nowhere to be seen" even after #205's dispatch-stickiness fix landed, and their explicit correction that the switches "should be on the same room where the door to the trap room is." This was a two-layer regression, both introduced by the 6-floor dungeon rework (`6a92541`, "relocated switch puzzle"): (1) `DungeonFloorGenerator._generateBranch`'s East descent was gated `active: !!this.game.companion` (`paintDescentVisual()` renders an inactive descent's `char` as `' '`), and (2) even once that door was made visible, the switches themselves still lived one room away, behind it, in a separate `companionGate` side room — not "the same room" the user required. Git-archaeology into the pre-rework 5-floor `DungeonSystem.js` (`git show 6a92541^`) confirmed neither layer matched the original: the two switches (`SWITCH_A_COL`/`SWITCH_B_COL`, `○` glyph) were unconditionally created and always rendered directly on floor 2 (today's Branch) regardless of companion status, with no separate room and no wall template — only *solving* the puzzle ever required a companion. A follow-up user question ("The original implementation did not have floor templates. Is this the miss?") correctly identified the reason the switches had been relocated in the first place: Branch's default scaffold applies a random wall template, and the companion's switch-to-switch walk is a straight-line, unpathed movement with no obstacle avoidance — a template wall between the switches silently strands it (the Companion Gate room's own header comment documented this exact failure from an earlier templating experiment, commit `4429455`). | user-reported 2026-08-21, reopening #205 twice — first via a screenshot ("Do YOU see any pressure plates?????") showing only door glyphs and no switches after the first-round fix, then via "The original implementation did not have floor templates. Is this the miss?"; both rounds root-caused via git archaeology into the pre-rework companion-switch implementation per the user's standing directive to trace back to the original | ✅ fixed — 2026-08-21 — the Companion Gate side room is removed entirely; its switches (and the puzzle-solve logic that watches them, `DungeonPuzzleSystem._updateBranchSwitches`) now live directly on Branch's own floor, right next to the Trap Room's North door, and Branch's wall template is disabled (`_buildScaffold({useTemplate:false})`, bare layout matching the original) so no shared template can ever wall off the companion's straight-line crossing again. Branch's East descent is unconditionally visible (`active:true`) and now leads directly to the Pyramid (`{kind:'numbered', floorIndex:3}`) instead of through a side room, staying `locked:true` (rendering `⚿`) until the switch puzzle is solved, then unlocking to `v` — the same visible-but-locked contract as Corridor's skull-key door. Pyramid's up-stairs footprint moved to Branch's East landing point (`SPINE_ROW`/`EAST_COL`) to preserve the "matching doors" rule now that it's entered directly from Branch. Live-verified via `chromium` driving the real `DungeonFloorGenerator`/`DungeonPuzzleSystem`/`DungeonSystem` code paths end-to-end: Branch renders both switches (`○`/`○`) and both descents (North `v`, East `⚿`) simultaneously with **no companion present**; recruiting a companion and pressing both switches flips East to `v`; descending through it lands the player on the Pyramid at the correct matching footprint. Build + `check:arch` green. |
| 205 | **Companion Gate switch puzzle ("the pressure plate puzzle for the hidden true path to floor 4") permanently strandable — the dispatched companion aborts its walk to the far switch the instant the player steps off the near one, and never resumes** — `DungeonPuzzleSystem._updateCompanionGate`'s dispatch block re-derived `companion.commandTarget` from scratch every tick: `playerOnA && !compOnA → target B`, `playerOnB && !compOnB → target A`, else `commandTarget = null`. The two switches sit 10 columns (160px) apart and the companion's only movement (`CampNPCSystem._moveToTarget`) is unpathed straight-line steering at ~80px/s — a ~2s crossing. Any player movement off switch A during that window (turning to watch the companion, repositioning — completely ordinary play, not edge-case behavior) hit the `else` branch, nulled `commandTarget` mid-walk, and froze the companion wherever it happened to be standing; because the `else` branch fired on every subsequent tick the player wasn't back on a switch, the companion could never be re-dispatched to finish the crossing even if the player returned to A immediately after — from the player's perspective the companion "walks partway, then waits," and the onward descent to the Pyramid never unlocks. Every prior investigation into this report failed to reproduce it because each one held the player motionless on switch A for the entire test — the one thing a real player doesn't do. Confirmed live via `chromium` through the actual production code paths (`_completeCoinOffering` hire flow → `_enterDungeon` → `_descend` → the real per-frame `DungeonPuzzleSystem`/`CampNPCSystem`/`PhysicsSystem` update loop): with the player stepping off switch A ~0.5s into the companion's ~2s crossing, `puzzleSolved` stayed `false` for the remaining 4+ simulated seconds, companion frozen partway across the room. | user-reported across multiple prior sessions ("the pressure plate puzzle for the hidden true path to the 4th floor is STILL not rendered or has been lost... The NPC walks around walls to the pressure plate and waits"); root cause finally isolated 2026-08-21 after being redirected to re-examine the original companion implementation rather than trust an earlier (incorrect) "cannot reproduce" conclusion | ✅ fixed — 2026-08-21 — dispatch is now sticky: `commandTarget` is only re-derived while the companion isn't already holding either switch (`!compOnA && !compOnB`); once dispatched toward a switch it keeps walking there and then holds, regardless of whether the player later steps off their own switch. The player now only needs to be back on switch A (or B) *at some point* after the companion arrives, not continuously for the whole ~2s crossing. Re-ran the exact failing scenario (step off A mid-crossing, return to A ~1.8s later) post-fix: companion completes the crossing uninterrupted, holds at B, puzzle solves once the player returns to A. Build + `check:arch` green. |
| 207 | **Whip Trial's two switches were arranged for a strike direction the player never actually uses** — the switches sat one south, one further south of the gap-crossing's landing post (`SWITCH_A_ROW`/`SWITCH_B_ROW`, same column), on the stated design intent that "one southward crack from the post-landing spot hits both at once." But the player crosses the gap moving north-to-south and lands facing *east* toward the room's continuation, not back south the way they came — the whip's straight-line hitbox only covers the direction the player is actually facing, so the documented one-crack solve never matched how the room is actually approached. | identified during this session's dungeon puzzle-room infrastructure work (Whip Trial refactor onto the generic trigger state machine, adjacent to bug #205) | ✅ fixed — 2026-08-21 — switches rotated onto the post's own row, 2 and 4 cells east of it (`SWITCH_A_COL`/`SWITCH_B_COL`), matching the player's actual post-landing facing. Both switches also migrated off the bespoke `recentlyStruck`/`_struckTimer` fields onto the generic `kind`/`activation`/`neutralizeSeconds` trigger contract `DungeonPuzzleSystem._advanceTrigger` already drives for editor-authored puzzle rooms, so the Whip Trial and any future template-built puzzle room share one state machine. Build + `check:arch` green. |
| 206 | **Branch's and Companion Gate's up-stairs sat at the generic north entry point instead of the footprint the player actually descended through, breaking the "same door in, same door out" rule bug #179 established for descents** — `_makeStairsUp()` always placed the return staircase at the universal `STAIRS_UP_ROW`/`STAIRS_COL` point, with no way to override it. But Branch is reached via Corridor's *West* descent (the skull-key gate) and Companion Gate via Branch's *East* descent — per bug #179's `_landingAnchorFor`, the player lands at the matching West/East footprint on arrival, not the north point. The up-stairs used to leave, however, was still hardcoded to the north footprint on both floors: arriving and immediately trying to backtrack put the exit stairs somewhere across the room from where the player actually stood, rather than back through the same door. Branch's already-retired West *descent* object (a leftover from before Corridor's skull-key gate existed) also still occupied the West footprint, competing for the same cell the up-stairs needed to move into. | identified during this session's Companion Gate investigation (dungeon navigation trace, adjacent to bug #205) | ✅ fixed — 2026-08-21 — `_makeStairsUp(locked, row, col)` now takes an optional footprint override; Branch's up-stairs moved to `SPINE_ROW`/`WEST_COL` and Companion Gate's to `SPINE_ROW`/`EAST_COL`, matching each floor's actual entry descent. Branch's retired West descent object was removed outright now that the up-stairs occupies its footprint instead. Build + `check:arch` green. |
| 204 | **Locked dungeon descent tile flipped to a red "x" after a stray attack** — `DungeonFloorGenerator._makeDescent()` constructs the footprint as `new BackgroundObject('x', ...)`, a throwaway placeholder immediately overwritten by `paintDescentVisual()`'s real glyph (`v`/`⚿`/` `) — but `paintDescentVisual()` never updated `obj.originalChar`, which stayed pinned to that construction-time `'x'`. The interior overlay (`HutInteriorOverlay`, shared with dungeon) renders every background object via `getRenderPosition().char = animationChar` unconditionally, not gated on whether an animation is active, and `BackgroundObject.update()` reverts `animationChar` to `originalChar` once any transient animation (e.g. the `'shake'` an indestructible object plays when hit) finishes. SPACE near a locked descent isn't consumed by any interior/puzzle system (nothing to unlock there), so it falls through to the generic weapon-attack fallback in `main.js` — the resulting hit plays `'shake'`, and when that ends the tile's rendered char permanently snapped back to the stale `'x'`, still colored the correct locked-red, reading as "changes to a red x." | user-reported 2026-08-21 ("in the dungeon, pressing space on a locked stair/door changes the icon to a red x. It should stay with the original unicode, not change.") | ✅ fixed — 2026-08-21 — `paintDescentVisual()` now also sets `obj.originalChar = char` on every repaint, so an animation-end revert restores the currently correct glyph instead of the constructor's internal placeholder. Build + `check:arch` green. |
| 202 | **Charm was dead in its own primary use case — charming 2+ enemies at once (the normal outcome of both charm sources) made them all attack the player instead of each other** — `CombatSystem.applyTargetOverrides` (`CombatSystem.js:61-77`) had a charmed enemy search for the *nearest non-charmed* enemy to fight, explicitly skipping any `other.isCharmed()` candidate, and fell back to `enemy.setTarget(player)` when no such candidate existed. But both places that apply charm are unbounded-group AOE effects with no single-target cap: the Garnet Staff's `_castCharmAOE` (`MagicSystem.js:514-521`, comment: "charms all enemies in radius") hits every enemy within 80px, and the Charm Lure trap (`items.js:2369-2378`, `effectRadius: 128`) hits every enemy in its blast via `TrapSystem`'s generic one-shot-effect fallback. Any cluster of 2+ enemies (routine in this game) got charmed together, so every one of them failed the "find a non-charmed peer" search simultaneously and reverted to targeting the player — the opposite of "they fight one another." | self-spotted 2026-08-20 (charm investigation) | ✅ fixed — 2026-08-20 — removed the charmed-peer exclusion in `applyTargetOverrides`: a charmed enemy now targets the nearest *other* enemy regardless of that enemy's own charm state, so a simultaneously-charmed cluster turns on itself instead of collapsing back onto the player. Remaining gap (ranged/sap/tongue attacks still can't land a hit on a charmed target — only melee could) split out to open bug #203. |
| 199 | **Ascent slope ramps (and other environmental terrain — water/lava/mud) reacted like ordinary destructible props on hit, and non-frozen liquid water reused the frozen-water glyph as damage-number feedback** — `BackgroundObject.takeDamage()`'s generic indestructible/hp-null branch (`_playAnimation('shake')`) and `handleBulletCollision()`'s `'pass-through'` case fired unconditionally for any indestructible/hp-null object, with no way for terrain to opt out — so Ascent's push-slope ramps (`ʌ v < >`), water, lava, and mud all flinched like a rock or barrel being hit. Separately, `CombatSystem.js` spawned a floating `'='` damage number (the same glyph `BackgroundObject`'s own render logic uses exclusively to mark *frozen* water) whenever fire hit ordinary liquid water to produce steam — a non-frozen event borrowing the frozen-water glyph as its feedback, confusing on its own terms and a symptom of terrain effects leaning on the generic prop damage-number pipeline instead of their own state changes. | user-reported 2026-08-20 ("slopes in ascent are interactible background objects ... these are environmental, not typical bg objects, add to taxonomy as different class of bg object, like lava or water ... untangle any existing logic like frozen water showing a freeze unicode") | ✅ fixed — 2026-08-20 — added a new `environmental` taxonomy flag (`BackgroundObject.isEnvironmental()`, checking `_variantData.environmental` then `data.environmental`) covering water, lava, mud_dry, mud_wet, hot_water (`BACKGROUND_OBJECT_VARIANTS`), the legacy `'~'` Puddle fallback, and Ascent's `makeSlopeData()`-built slope tiles (`RoomGenerator.js`) — the same taxonomy tier as lava/water rather than a typical destructible/interactible prop. `takeDamage()` now early-returns before the generic shake reaction for any environmental object, and `handleBulletCollision()`'s pass-through case skips the shake animation for the same set. Removed all 8 `createDamageNumber()` calls from the water-state-transition blocks in `CombatSystem.js` (projectile and melee paths) — poison ☠, stun ⚡, obsidian-creation ◆, acid-flood ☠, and the mis-tangled frozen-glyph `'='` steam call — since every one of those events already has its own strong feedback (tile color/state change, a steam cloud, flood fill, or a new obsidian rock appearing) and doesn't need floating text. All underlying interaction logic (state transitions, steam clouds, obsidian creation, acid flood-fill, electricity seeding) is untouched. Build + `check:arch` green. |
| 197 | **Flail dealt damage almost immediately after the spin windup began** — `FlailSystem.update`'s angular-speed ramp used an ease-out curve (`1 - (1-t)²`) that front-loads speed gain: half of `spinMaxSpeed` (the documented no-damage threshold) was reached at only 29% of `spinRampTime` elapsed (~0.2s of the Flail's 0.7s ramp), which read as near-instant damage despite the threshold check itself being correct. On top of that, damage below top speed was a flat step (exactly half damage from the threshold to top speed, not a scale), not the continuous ramp the design called for. | user-reported 2026-08-20 ("FLAIL IS doing damage nearly as soon as the windup begins ... damage scales from 0 to max ... dependent on speed") | ✅ fixed — 2026-08-20 — ramp is now linear (`ratio = rampTime / rampDuration`), so half top speed genuinely means halfway through the hold, not a fast-approaching curve. Damage below half speed is still fully passthrough (no collision check runs at all, per existing `hasHit` gating); from half speed to top speed, `damageRatio` now scales continuously (`(ratio - 0.5) / 0.5`) instead of a flat 0.5 step. Build + `check:arch` green. |
| 196 | **Rescued Alchemist survived a full game over — reappeared "already rescued" in the Alchemy hut on the next run** — the Quagmire's rescue outcome (`RoundCombatSystem._spawnAlchemistRescue`) creates `game.alchemistNPC` as a singleton with `.rescued = true`, documented in its own comment as created "once, ever." Neither `_resetRunToRest()` nor `ZoneSystem.resetOnDeath()` cleared it, unlike every other per-run captive reward (`captives`, `characterNPCs`, `clearedZones`), so `HutSystem._syncAlchemistHutPresence` kept gating hut presence on the stale `alchemist.rescued` flag from the prior run — placing him in the hut before the new run's Quagmire had ever been cleared, and permanently zeroing the 'alchemist' weight out of `_rollQuagmireFinalEncounter`'s roll (it drops to 0 once `alchemistNPC?.rescued` is true, so the new run could never roll the rescue outcome at all). Violates the project's stated full-reset-on-death contract. | user-reported 2026-08-20 ("I got a game over, but the alchemist still appeared in A hut as if rescued") | ✅ fixed — 2026-08-20 — `ZoneSystem.resetOnDeath(game)` now takes the game reference and clears `game.alchemistNPC = null` alongside its existing `clearedZones` reset (same per-run captive-tracking responsibility, so the singleton's lifecycle now lives next to the state that gates it); both call sites (`_resetRunToRest()`, `enterTitleState()`) pass `this`. A new run's Quagmire final encounter can roll the rescue outcome fresh and the hut stays empty until this run actually earns it. Build + `check:arch` green. |
| 195 | **Room exits flapped open/closed repeatedly while a cowardly Rat/Plague Rat's coward flip resolved** — `7d55c18` made the Rat's `ThiefMechanic` coward flip reversible (`_unflip` after 2s continuous lost sight), and mirrored the flip's `enemy.uncounted = true` with a symmetric `enemy.uncounted = false` on recovery. Since `_countedEnemies()` gates `ExitSystem`'s room-clear/exit-lock state every frame, a Rat cycling flee (uncounted, door opens if it's the last threat) → recover (counted again, door re-locks per the existing "counted enemy appears after exits opened → re-lock" invariant, written for mimic/ambush-beast reveals) → re-engage → flee again produced a multi-second open/close flap with no player-facing cause. Every other `uncounted` user in the codebase (Aquifer eel, Quagmire Hag) is one-way/permanent by design — the Rat's reversal was the only place `uncounted` ever went back to `false`, an inconsistency introduced by the mirroring, not a considered choice about the door-lock side effect. | self-spotted 2026-08-20 (user: "doors are opening and closing as cowards logic resolves") | ✅ fixed — 2026-08-20 — user chose (of three presented options) to make room-clear status permanent once granted: `ThiefMechanic._unflip` no longer resets `enemy.uncounted`; recovery restores hunting/biting behavior only. A recovered rat can still corner-bite the player, it just never re-locks a door the player already earned open. Genuine reveals (mimic, shell camouflage) are untouched — they're driven by `_isHiddenEnemy`, a separate mechanism from `uncounted`. Build + `check:arch` green. |
| 194 | **`HutInteriorOverlay.js` had accumulated instructive-UI label/indicator text across several unrelated fixes (#192, #193) that was never actually requested** — bug #192 "restored" a `[ HUT ]`/`[ DUNGEON FLOOR N ]` floor label that had been deleted by a prior uncommitted edit, treating the deletion as accidental data loss rather than intended; on top of that, the file carried an enemy `hasKey` glyph indicator (dead code — `hasKey` was never set anywhere on any enemy), an alchemy trough "which key to press" digit hint, and three SPACE-prompt text labels (`SPACE  OFFER`, `SPACE  ASCEND`/`DESCEND`/`UNLOCK`, `SPACE  EXIT`). None of this was ever asked for, and it runs directly against the project's own non-instructive UI principle. | user-reported 2026-08-20 ("hutinterioroverlay was supposed to be sunset. Labels and indicators are NOT what I asked for, at any stage. Remove.") | ✅ fixed — 2026-08-20 — removed all six label/indicator blocks from `HutInteriorOverlay.js`: the bottom floor/hut text label, the dead enemy `hasKey` glyph loop, the alchemy trough fill-hint digit, and the OFFER/ASCEND-DESCEND-UNLOCK/EXIT SPACE-prompt text draws — along with the now-dead `spectaclesTransformString`/`isSpectaclesActive` import and unused `offsetX`/`offsetY` destructure this left behind, and `Player.js`'s now-unread `seenTroughFillHint`/`_troughHintShown` fields. The file's actual job — PiP rendering of interior entities, effects, and the player — is untouched; only on-screen text/glyph chrome was stripped. Build + `check:arch` green. |
| 193 | **[key-object] Both key items were boolean flags with no `Item` behind them, so bug #190's "fix" broke the Vault Key entirely** — #190 migrated the Vault Key off its weapon-slot `Item` to a room-scoped `vaultInfo.keyHeld` flag, following the Skull Key's pre-existing flag-only precedent (#178/#181/#192). That shape was wrong: with no `Item` ever spawned, the key rendered nowhere in the world after its carrying object died, and the Tab overlay's `KEY_ITEM_DEFS` registry (added in #192) could only ever show a static boolean-held row, not an actual pickup. User correction: "'key items' are still items, they for all intents and purposes are implemented exactly the same as equippable items, the only difference is they are not equipped and are maintained visually in inventory, above Treasures." | user-reported 2026-08-20 ("Now the Vault Key isn't rendering as the 'key' object anymore. It just doesn't appear, and it isn't displayed in inventory STILL. What the fuck is going on with the way 'key objects' are implemented?", clarified: real Items, not flags) | ✅ fixed — 2026-08-20 — unified both keys on one real mechanism: `ITEM_TYPES.KEY` (`items.js`), spawned as a genuine world `Item` via `LootSystem.spawnItemDrop` (`obj.keyChar` — `'߃'` Vault Key, new `'⚿'` Skull Key, both Unicode per the encoding rule), picked up through `InventorySystem.tryPickupItem`'s new KEY branch into a dedicated `keyItemInventory` array (never a quick/equip slot), with `hasKeyItem(char)`/`consumeKeyItem(char)` as the single source of truth every door/gate check reads (`InteractionSystem.canUnlockVault`/`unlockVault`, `DungeonPuzzleSystem._tryUnlockCorridorGate`, `DungeonFloorGenerator._placeSkullIfDue`, `InteriorManager.reset`, `HutInteriorOverlay`'s unlock gate). `InventoryOverlay.js` now builds its KEY ITEMS section (positioned above TREASURE, per spec) directly from `keyItemInventory` instead of the removed `KEY_ITEM_DEFS` registry. Pickup confirmation sfx (`vault_key_pickup`/`dungeon_key_pickup`) fires from `main.js`'s `tryPickupItem()` wrapper via a new additive `pickedUpChar` result field, matching the existing weapon-pickup precedent. Also removed `HutInteriorOverlay.js`'s corner-glyph "held-key" HUD indicator (added in #192) after a follow-up screenshot report — it rendered as a red '8' overlapping the floor-label row and was redundant with the now-functional Tab overlay and the existing SPACE-unlock door prompt; the in-world skull remains correctly indistinguishable from ordinary Bones decor. `CheatMenu.js` gained a KEY ITEMS spawn folder for dev-tool parity. Required two extractions to clear architecture budgets: `main.js`'s `updateExitCollisions` inline escape-route logic moved fully into `ExitSystem.updateExitCollisions`, and `InventorySystem.js`'s ~100-line `applyEquipmentEffectsToPlayer` body moved to a new `EquipmentEffectsSystem.js` (thin delegate left in place, zero call-site changes). Build + `check:arch` green. |
| 192 | **Dungeon (Skull) Key was never shown anywhere in the Tab inventory overlay, and holding it had no visual indication at all** — despite prior sessions claiming a "key items" display existed, `InventoryOverlay.js` had no such section; the dungeon key is tracked purely as the run-scoped flag `game.dungeonKeyObtainedThisRun` (never a slot-occupying `Item`), and nothing rendered that flag anywhere — not in the Tab overlay, not in the dungeon HUD. A player who cleared every skull had no way to confirm they were actually holding the key, and SPACE unlocking the door gave no feedback distinguishing "key present" from "no key required." | user-reported 2026-08-18 ("I have cleared every skull ... there is NO KEY in inventory ... Confidently incorrect", follow-up: "no indication to the player ... for all I know the lock opens without a key at all") | ✅ fixed — 2026-08-18 — added a new declarative KEY ITEMS section to `InventoryOverlay.js` (`KEY_ITEM_DEFS` registry, boolean-held display with no "xN" suffix) that surfaces the Skull Key whenever `dungeonKeyObtainedThisRun` is true. Also restored `HutInteriorOverlay.js`'s floor/hut label (`[ HUT ]` / `[ DUNGEON FLOOR N ]`), which a prior uncommitted edit had deleted while adding a held-key HUD glyph — the glyph now renders in a corner alongside the restored label instead of replacing it, so the dungeon HUD itself also confirms key possession. Build + `check:arch` green. |
| 191 | **Trap Room reward item could spawn in an unreachable location** — `DungeonFloorGenerator.generateTrapRoom` placed the reward at a fixed coordinate chosen only via `pickOpenCell`'s "not-a-wall" check, not actual graph reachability from the room's entry point. Per the documented "staircase footprint contract" (`dungeonFloorTemplates.js`), templates are only guaranteed to keep 4 single reserved cells walkable — they may legally wall off regions between them by design — so a template that happened to separate the fixed reward coordinate from the entry point stranded the item behind a wall with no way to reach it (screenshot showed it inside a walled-off water pool). | user-reported 2026-08-18 (screenshot: trap room reward stranded in a walled-off pool) | ✅ fixed — 2026-08-18 — added a BFS reachability helper (`_reachableCells`, 4-connected, seeded from the room's confirmed entry point `STAIRS_UP_ROW`/`STAIRS_COL`) and a reachable-cell picker (`_pickReachableCell`) to `DungeonFloorGenerator.js`; `generateTrapRoom` now filters candidate reward positions through the reachable set instead of a bare open-cell check, falling back to the BFS start cell if nothing else qualifies. Scoped narrowly to the reward item only — deliberately not extended to enemy/decor spawns, since walled-off sub-regions are an intentional, documented template pattern elsewhere. Build + `check:arch` green. |
| 190 | **Vault Key ('߃') occupied a weapon/equip slot instead of following the "held, not equipped" pattern used by every other narrative key** — unlike the dungeon Skull Key (a flag, never a slot item), the K-room Vault Key was a real `Item` with `type: ITEM_TYPES.WEAPON` and a `AFFINITY_POOLS` entry, so picking it up could bump an equipped weapon out of a quick slot and it could theoretically drop from generic humanoid enemies as loot, neither of which was ever intended for a single-use door key. | user-reported 2026-08-18 ("Move 'Vault Key' to key items once correctly implemented as well (not equipped)") | ✅ fixed — 2026-08-18 — migrated to the same held-not-equipped pattern as the Skull Key, but room-scoped: `RoomGenerator.js` adds `vaultInfo.keyHeld = false`; `InteractionSystem.js`'s `dropsKey` branch sets the flag instead of spawning an `Item`, `canUnlockVault`/`unlockVault` read/clear the flag instead of checking a quick slot; `data/items.js` drops `'߃'` from `AFFINITY_POOLS.humanoid.weapons[RARE]` (the `ITEMS['߃']` display-data entry itself is untouched). Room-scoping (vs. run-scoping) is correct here because the K room's vault and key-dropping object are always co-located in one visit, and EXPLORE rooms are only ever restored (not regenerated) on backtrack. Surfaced in the new KEY ITEMS inventory section alongside the Skull Key. Build + `check:arch` green. |
| 189 | **Moss Cloak stopped hiding the player from an enemy the instant it entered Search** — `hasVision`'s Moss Cloak gate (`enemyVision.js`) exempted an enemy from the cloak's vision block if either `enemy.enraged` (the real aggro flag) or `enemy.aggroMemoryActive` was set. `aggroMemoryActive` is Search's own bookkeeping flag, not aggro — `alert.js`'s proximity door ("door two") arms Search on mere in-range presence with no sight check at all, so a cloaked player standing next to a non-aggro'd, merely-searching enemy was recognized anyway, the opposite of the cloak's intent (a searching enemy should never make out a cloaked player, no matter the range; only a genuinely aggro'd — `enraged` — enemy should see through it). | user-reported 2026-08-18 ("new enemy AI statefulness is confused by the Moss Cloak ... only aggro state gives the enemy vision of the player in cloaked state") | ✅ fixed — 2026-08-18 — dropped the `!enemy.aggroMemoryActive` exemption from the gate; it now reads `!enemy.enraged` alone, matching the glossary's own designation of `enraged` as the codebase's aggro flag. Build + `check:arch` green. |
| 188 | **Dagger swings never rotate to match the player's facing direction — every other melee weapon category does** — `Item.createMeleeMultistab` (the dagger attack-pattern builder) fell back to `this.data.meleeChar || this.char` for its swing glyph, i.e. the weapon's own decorative pickup character, when no override was authored. Every sibling melee builder instead falls back to a fixed, rotation-safe glyph (sword `'/'`, axe/pickaxe `'═'`, spear `'→'`, whip `'~'`, hammer/slam `'○'`/`'█'`) — `getMeleeDrawAngle`'s `NATURAL_ANGLES` table only knows the natural orientation of a small whitelist of glyphs, and any char outside it returns `drawAngle = null`, which renders unrotated regardless of facing. Base "Dagger" happens to use `'↾'`, which is on the whitelist, so it looked correct by coincidence; Acid Blade (`'ᚢ'`) and Vampire Dagger (`'ᛘ'`) are not, so both always rendered their swing unrotated. Pre-existing bug, unrelated to the same-day #183 stacking work — found while investigating a user report of "weapon rotation is broken" during that feature's playtest. | user-reported 2026-08-15 | ✅ fixed — 2026-08-15 — changed `createMeleeMultistab`'s fallback to `this.data.meleeChar \|\| '↾'`, matching the established per-category fixed-default convention instead of the weapon's own pickup glyph. Verified via a headless Node repro (`Item.js`'s attack builders have no browser-API dependency) across Acid Blade, base Dagger, and Vampire Dagger at 5 facing directions — all three now produce distinct, non-null `drawAngle` values matching facing. Build + `check:arch` green. |
| 185 | **Interior deaths (hut/dungeon/maze) cut straight to the plain GAME OVER screen instead of showing the death in place** — `GameOverRenderer.render()` only special-cased `game.player.inHut`, delegating foreground rendering to `hutInteriorOverlay` for hut deaths only; `inDungeon` and `inMaze` deaths fell through to the surface branch, which draws the frozen (empty) surface room instead of the interior the player actually died in — exactly the kind of ad-hoc `if (player.inX)` block ADR-0001's `InteriorOverlay` dispatcher exists to replace, just missed when `GameOverRenderer` was written. Even for hut deaths, the "GAME OVER" text was drawn immediately on top of the interior PIP with no pause, superimposing the two instead of letting the death read before the screen changed. | user-reported 2026-08-15 ("when a death occurs in a maze/hut etc. the screen immediately is killed to show game over text ... there should be a 2 second delay, show the death in location, then kill the hut screen") | ✅ fixed — 2026-08-15 — `GameOverRenderer` now checks `game.interiorManager.isActive` and delegates to the shared `interiorOverlay` dispatcher (covers hut/dungeon/maze uniformly) instead of the old `inHut`-only branch. The interior PIP is held up for the full existing 2-second death timer (`gameOverDeathTimer`/`characterDeathTimer`, reused rather than adding a new one) — "GAME OVER"/"`<name>` lost" text is withheld until the timer expires, at which point the view switches to the plain surface-room GAME OVER screen and the text appears, matching the surface-death timing. Also tagged the player-death explosion particles `hutPlane = true` when `game.activeFloor` is set (hut/dungeon), since `HutInteriorOverlay`'s `drawParticles(game, true)` filters strictly on that tag and the explosion was otherwise invisible inside the held PIP; maze's overlay draws `game.particles` unfiltered so needed no tag. Build + `check:arch` green. |
| 184 | **Maze/hut music kept playing past death until its own loop finished, before the correct music cut in — only when a reserve character was available** — `_switchToCharacterAtRest()` (the "hero in reserve" character-swap path, shared with GrayZoneSystem's non-death mist-out) unconditionally called `this.audioSystem.play()` before transitioning to `GAME_STATES.REST`. The death path's `audioSystem.stop()` (main.js, player-death handling) halts playback but never clears `layer1Buffer`/`layer2Buffer` — those still held whatever was active at death (e.g. the maze's mono-track dual-layer override, loaded via `MazeSystem._enterMaze()`'s `switchMusic`). `play()` in dual mode calls `playDualLayer()`, which blindly resumes whatever buffers are currently loaded — resurrecting the stale maze track and setting `isPlaying = true`. `enterRestState()` then ran its normal `mode === 'dual'` branch, `switchRestMusic()` → `switchMusicAtLoopEnd()`, which — seeing `isPlaying === true` — treated the resumed maze track as a legitimately-playing track worth crossfading from at its *own* loop boundary, instead of falling back to the immediate `switchMusic()` path it takes when nothing is playing. Net effect: the maze track audibly continued after death until it looped, then the correct REST/zone track cut in. Full game-over (no reserve character) was unaffected — `_resetRunToRest()` uses `hardResetDualLayers()`, which reloads the green-zone buffers via immediate `switchMusic()` regardless of prior state. | user-reported 2026-08-15 ("maze music persisted after death (hero in reserve), correct music played after loop finished") | ✅ fixed — 2026-08-15 — removed the `audioSystem.play()` call from `_switchToCharacterAtRest()`. `enterRestState()` (run immediately after via the same function's `stateMachine.transition(GAME_STATES.REST)`) is mode-aware and already loads the correct track for every case: post-death, `isPlaying` is correctly `false` (untouched by the removed call), so `switchMusicAtLoopEnd()` now takes its immediate `switchMusic()` fallback instead of scheduling a crossfade against stale buffers; for the non-death gray-mist-out swap, audio was never stopped in the first place, so removing the no-op `play()` call (guarded by `if (this.isPlaying) return;`) changes nothing there. Build + `check:arch` green. |
| 182 | **Dungeon floor layout templates could repeat within a single dungeon visit** — `DungeonFloorGenerator._buildScaffold()` called `pickRandomTemplateName()` independently for every template-consuming floor/side-room (Entrance, Corridor, Branch, Pyramid, Trap Room, Key Vault, Companion Gate), and the picker had no memory of what earlier floors in the same visit had already rolled — each call was a fresh weighted draw over all 5 named templates (`open`, `pillar_rows`, `mild_maze`, `separated_zones`, `sewer`), so nothing stopped e.g. `sewer` from landing on both the Corridor and the Key Vault in one run. | user 2026-08-13 ("same layout used repeating in floors (never should appear more than once in a single dungeon") | ✅ fixed — 2026-08-13 — `pickRandomTemplateName(excludeNames)` now accepts a Set of already-used template names and draws only from the remainder, falling back to the full pool once every named template has been used at least once (there are only 5 templates but up to 7 template-consuming rooms per visit, so a 6th/7th room can still legitimately repeat one — full novelty isn't achievable with the current template count, only "no repeat until everything has appeared once"). `DungeonFloorGenerator._buildScaffold()` now reads/writes a run-scoped `game.dungeonTemplatesUsedThisRun` Set around the pick. Reset alongside the other dungeon run flags in `InteriorManager.reset()` (called on REST entry, room transitions, and cheat-warp swaps) and initialized in `main.js`'s constructor for the same reason `dungeonKeyObtainedThisRun` etc. are. Build + `check:arch` green. |
| 181 | **Dungeon key skulls could be destroyed with no key drop roughly 80% of the time** — the dungeon key isn't tied to a dedicated object type; it's a `dropsDungeonKey` flag set post-construction on an ordinary `'8'` (Bones) `BackgroundObject`, the same decor char used throughout every floor's ambient skull/bone-pile pool (see #178). `main.js`'s combat `objectEffects` loop gates all destructible-object drops behind `BACKGROUND_OBJECTS[char].dropChance` unless the object is flagged to bypass it — but that bypass checked only `obj.dropsKey` (the older K-room vault-key mechanic), never `obj.dropsDungeonKey`. `BACKGROUND_OBJECTS['8'].dropChance` is 0.2, so the flagged key skull was subject to the same 80%-destroyed-with-nothing roll as an ordinary bone pile, discarding the run's only key silently — indistinguishable in-world from just breaking decor, so a full floor 1–2 clear could easily never surface it. | user 2026-08-13 ("dungeon key never appeared, after fully clearing all skulls and enemies in floors 1 and 2") | ✅ fixed — 2026-08-13 — added `|| obj.dropsDungeonKey` to the dropChance-bypass condition in `main.js`, matching the existing `obj.dropsKey` precedent (key droppers always drop, full stop). Confirmed `InteractionSystem.js`'s downstream `dropsDungeonKey` handler (sets `game.dungeonKeyObtainedThisRun = true`) was already correctly ordered and untouched — the defect was purely the upstream gate never letting that handler get called most of the time. Build + `check:arch` green. |
| 180 | **Trap Room's lock didn't actually trap the player — the up-stairs could be used mid-fight** — `DungeonPuzzleSystem._updateTrapRoom()` correctly sets and clears `room.stairsUpLocked` (locked on entry with enemies present, cleared the instant `room.enemies.length === 0`), but nothing on the ascend path ever read the flag: `DungeonSystem._ascend()` and the ascend branch of `handleSpacePress()` both ran unconditionally once the player stood on the up-stairs footprint, so the lock only ever affected the *visual* (via `paintStairsUpVisual`) and never actually blocked leaving. | user 2026-08-13 ("Trap room doesn't actually trap the player, I can just leave. Should trap until enemies cleared from room.") | ✅ fixed — 2026-08-13 — `handleSpacePress()`'s ascend branch now short-circuits on `game.activeFloor.stairsUpLocked`, and `_ascend()` itself early-returns on the same flag as a second guard (defense in depth against any other future call site). `HutInteriorOverlay.js`'s SPACE-prompt label now also suppresses "SPACE ASCEND" while locked — the red-tinted footprint (existing `paintStairsUpVisual`/`FOOTPRINT_LOCKED_COLOR` treatment) is the only signal, matching the non-instructive-UI precedent already set by the Key Vault's locked-without-key state. Build + `check:arch` green. |
| 179 | **Both of a floor's descents to the next floor landed the player at the same fixed spot, with no correspondence to which staircase was used — and ascending didn't reliably reverse the right one** — `DungeonSystem._descend()` always spawned the player at the destination floor's single fixed `stairsUpRow`/`stairsUpCol`, regardless of which of that floor's active descents (North/West/East) triggered the transition, so e.g. Corridor's North and West descents (both routed to Branch by design) put the player in the identical location on arrival. `_ascend()` had the mirror problem in reverse: it picked the first descent on the previous floor whose `destination` matched the current floor by *value*, which is ambiguous whenever more than one descent shares a destination — it could reverse through the wrong staircase entirely. | user 2026-08-13 ("both floor two stairs down lead to the same floor 3 (and not corresponding locations either, both west and north downstairs led to the same north stair on floor 3)") | ✅ fixed — 2026-08-13 — added `_landingAnchorFor(id)`, which maps a descent id (`'north'`/`'west'`/`'east'`) to that same physical footprint on the *arriving* floor (`NORTH_ROW`/`STAIRS_COL`, `SPINE_ROW`/`WEST_COL`, `SPINE_ROW`/`EAST_COL` — the shared footprint-geometry contract in `dungeonFloorTemplates.js` guarantees these coordinates are identical across every floor), so `_descend()` now spawns the player adjacent to the corresponding footprint on the new floor instead of always the up-stairs. `_descend()` also stamps a new non-serialized runtime field, `nextFloor._enteredViaId`, recording which descent was actually used; `_ascend()` reads it back to find the exact originating descent on the previous floor (falling back to the old by-value match only if that field is somehow absent), so ascending now reverses precisely, not just approximately. Build + `check:arch` green. |
| 178 | **Dungeon key skull silently reused the hut Cauldron's background-object char, making every Cauldron destructible as a side effect** — investigating a report of "cleared all enemies, never found the dungeon key" led first to the key mechanic itself: the key is not enemy-tied at all, it's `dropsDungeonKey` on a destructible `BackgroundObject`, findable only by attacking decor. The user then flagged the deeper defect: `DungeonFloorGenerator._placeSkullIfDue()` (added by the 6-floor dungeon rework) instantiated that object as `new BackgroundObject('Ω', ...)`, and `GameConfig.js` gained a matching `BACKGROUND_OBJECTS['Ω']` entry (`hp: 2, dropEffect: 'destroyObject:dungeonKey', bulletInteraction: 'block'`) to back it. But `'Ω'` was already load-bearing — `HutSystem.js` builds the hut alchemy Cauldron as `new BackgroundObject('Ω', ...)` and `AlchemySystem.nearCauldron()` proximity-checks the same char. `BackgroundObject`'s constructor looks up `BACKGROUND_OBJECTS[char]` once at construction (`this.data = BACKGROUND_OBJECTS[char]`), so every Cauldron in the game silently inherited the new entry: previously the "unknown char" fallback gave it `indestructible: true, hp: null, bulletInteraction: 'pass-through'`, but with the entry present it became a 2-hp destructible object that blocks bullets and shakes on hit — an unintended regression letting a Cauldron be permanently destroyed mid-hut-visit. Separately, `'8'` (name: `Bones`) already fills the "skull/bone pile" decor role used throughout dungeon floors (`_addDecor`'s `decorChars` pool), so a second, differently-named "Skull" concept on a different glyph was also a straight duplication. | user 2026-08-13 ("skulls already exist as background object '8'. This is a mistake with the implementation.") | ✅ fixed — 2026-08-13 — removed the bespoke `BACKGROUND_OBJECTS['Ω']` entry entirely (restoring the Cauldron to its original indestructible/pass-through fallback) and switched the key-carrying object to `new BackgroundObject('8', ...)` — the existing Bones decor, already in every floor's ambient decor pool. It's indistinguishable from ordinary bone piles in-world; only the `dropsDungeonKey` instance flag (set post-construction, same as `dropsKey` for the K-room vault key) marks which one actually grants the key on destruction, so finding it is the same "break them to find out" pattern the K room already teaches. Also updated `HutInteriorOverlay.js`'s held-key HUD indicator glyph from `'Ω'` to `'8'` to match. Build + `check:arch` green. Cauldron-destructibility regression was found via code trace (GameConfig lookup + both call sites), not confirmed by playtest before the fix. |
| 177 | **Inventory (Tab-held overlay) didn't render on top of a hut/dungeon/maze interior — the PiP painted over it instead** — `ExploreRenderer.renderForeground()` drew the Tab-held `InventoryOverlay` *before* `interiorOverlay.render(game)` in the same `fgCtx` pass. The interior PiP's shared frame (`drawInteriorFrame()` in `interiorFrame.js`) paints a full-canvas `rgba(0,0,0,0.55)` dim veil and then an opaque panel rect — since it drew later in the same immediate-mode canvas pass, it painted straight over whatever the inventory overlay had just drawn, hiding it completely whenever Tab was held while inside a hut, dungeon, or maze. | user 2026-08-13 ("inventory doesn't render on top of the dungeon") | ✅ fixed — 2026-08-13 — swapped the two draw calls so `interiorOverlay.render(game)` runs first and the Tab-held `inventoryOverlay.render(game)` runs after, landing on top of the PiP veil/panel as intended. Incidental architecture fix required to land cleanly: reordering pushed `ExploreRenderer.js` past its architecture budget tolerance (+420 chars); extracted the three near-duplicate spinning-arc-toss render methods (`_renderDonationArc`, `_renderWellRitual`, `drawCoinArc` — donation arc, well ritual coin, and NPC coin-trade arc all share the same parabolic-arc-plus-spin-frame logic) into a new `src/rendering/effects/ArcTossEffects.js` module (`drawDonationArc`/`drawCoinArc`/`drawWellRitual`), following the same pattern as the file's existing `SniperEffects.js`/`WireEffects.js`/`BombEffects.js` imports. Updated all call sites in both `ExploreRenderer.js` and `HutInteriorOverlay.js` (the latter called `drawCoinArc` through `renderController.exploreRenderer`, now imports the shared function directly). Budget ratcheted `113252` → `111058` via `--update` (only `ExploreRenderer.js`'s entry changed — every other file's budget was already at its ceiling and unaffected). Build + `check:arch` green. |
| 176 | **Grass moved and permanently imprinted while the player was underground in a Grass (G) room's Sinkhole cave** — `SinkholeSystem.dive()` puts the player on plane 1 within the *same* room grid the surface grass already occupies (it carves a cave over the existing coordinates rather than swapping rooms), so the plane-1 cave and the plane-0 grass share space. Two independent gaps let plane 0 leak into plane 1 there: (1) `ExploreRenderer.shouldRenderBackgroundObject()` defaults an unflagged object to "render on both planes" — the opposite of `PlaneSystem`'s documented plane-0-default contract — so grass drew straight through the cave with nothing opting it out, the same gap the obsidian-rock-near-dungeon-caves case had already needed `surfaceOnly` to close. (2) The tall-grass bend/imprint logic (then still inline in `main.js`, pre-dating this fix) computed player/enemy proximity as raw pixel distance with no plane check at all, so a plane-1 player standing in the cave could bend and dodge-roll-imprint plane-0 grass at the same coordinates — the literal "layers should stay separated the way tunnel/underground already are" from the report. | user 2026-08-10 ("grass moves/interacts while you are underground in G room. In layer 1 (not 0) it should be the same separation of layers as with tunnel and underground") | ✅ fixed — 2026-08-10 — (1) `generateGrassSwaths` (now in `roomFeatures.js`, see below) sets `surfaceOnly = true` on both grass `BackgroundObject`s it places, same opt-in flag and rationale as the existing dungeon-cave obsidian rocks; `shouldRenderBackgroundObject` already honors it, no renderer change needed. (2) The bend/imprint proximity checks (player and per-enemy) are now gated on `inSamePlane(entity, obj)` from `PlaneSystem`, so a plane-mismatched entity can no longer bend or permanently imprint grass it isn't actually standing in. Incidental architecture fix raised mid-session by the user ("why is this logic in main.js?"): the grass-bending/imprint block was inline in `main.js`'s EXPLORE update loop, violating main.js's dispatch-only rule (`WorldEffectsSystem.js` is the documented home for transient world effects) — extracted verbatim (plus the plane gates) into a new `WorldEffectsSystem.updateBackgroundObjects(deltaTime)`, called from main.js as a single dispatch line; routes background-object access through `game._activeBackgroundObjects()`/`game._activeEnemies()` per the existing layer-isolation guard. That extraction shrank `main.js` enough on its own; a second extraction was still required to land the `surfaceOnly` fix inside `RoomGenerator.js`'s architecture budget — `generateGrassSwaths()` moved out to `src/systems/roomFeatures.js` following that file's existing `(gen, room)` sibling-helper pattern (same convention as `seedSinkholes`), call site and import updated in `RoomGenerator.js`. Budgets ratcheted down via `--update` (`main.js` 217462→214436, `RoomGenerator.js` 173303→170889); `ExploreRenderer.js`/`Player.js` budgets untouched. Build + `check:arch` green. Scoped to the reported mechanism only — `GameOverRenderer.js`'s grass-draw loop has no plane check either but was not confirmed broken (game-over is a frozen final frame; may not be player-reachable while underground) and was left untouched pending an actual repro. |
| 173 | **Border walls didn't reflect bullets correctly, and never pushed detritus out of a wall overlap at all** — three independent defects under one report. (1) The Ricochet Rifle (`items.js` `ricochet: true`) skipped the room's `collisionMap` entirely and instead bounced off hardcoded canvas edges (`GRID.WIDTH`/`HEIGHT`, `CombatSystem.checkRicochet`) — the *outer* edge of the border-wall tile rather than its inner face, so bullets visibly flew into the wall glyph before turning around, and the hardcoded 480×480 was wrong for every interior room (hut 10×10, dungeon 24×24), where a ricochet bullet wouldn't bounce until well past that room's actual wall. (2) Fester's Gun's orbital-loop bullets (`loopOmega`) were unintentionally caught by the generic "bullets ricochet off interior structure walls" path (`_tryStructureWallRicochet`) shared by all `type: 'bullet'` projectiles — but the loop formula recomputes velocity analytically from `loopTime` every frame, overwriting the ricochet's velocity flip the very next tick, so the bullet visibly glitched at the wall instead of cleanly dying. (3) `Debris` (death detritus, `entities/Debris.js`) had `hasCollision = false` and no `collisionMap` reference at all, and `PhysicsSystem.updateEntity`'s wall-ejection pass (`resolveCollisionMapOverlap`) was gated on `entity.hasCollision` — so debris pieces that ended up inside a wall cell (e.g. spawned near a border wall) were never pushed back out; they just sat there or drifted through. | user 2026-08-04 ("border walls do not currently reflect bullets as expected, and it does not push all objects like detritus"; follow-up: "fester's gun should not reflect. Walls should kill the bullet.") | ✅ fixed — 2026-08-04 — (1) `checkRicochet(proj, room)` now derives the border-wall inner face from `room.collisionMap` dimensions (`leftFace/rightFace/topFace/bottomFace = cs` / `(cols-1)*cs` etc.) instead of the hardcoded canvas constants, using the same cell-top-left/visual-center convention as `hitsWall`/`tryStructureWallRicochet`, so it now scales correctly to any room size and bounces at the actual wall face. (2) `_tryStructureWallRicochet` is now skipped for `proj.loopOmega` bullets (Fester's Gun is the only weapon that sets it), so any wall — border or interior — destroys the bullet outright instead of attempting a ricochet the loop formula would immediately undo. (3) Gave `Debris` a `collisionMap` field + `setCollisionMap()` (mirroring the `Player`/`Enemy` pattern), wired at creation in `WorldEffectsSystem.spawnDeathDetritus` by reusing the dying enemy's already-resolved `collisionMap` (correct for both surface and interior deaths); `PhysicsSystem`'s wall-ejection gate was changed from `entity.hasCollision` to `entity.collisionMap` presence (the function already no-ops without one), so debris now gets pushed out of any wall it overlaps without becoming a solid obstacle to the player/enemies — `hasCollision` still gates movement-blocking and background-object collision separately, both left untouched for Debris. Extraction: moved `hitsWall`/`isOutOfBounds`/`tryStructureWallRicochet`/`checkRicochet` out of `CombatSystem.js` into a new `WallRicochetMechanic.js` composition module (same pattern as the existing `BoomerangMechanic.js`) to bring the file back under its architecture budget after the border-wall rewrite; all 6 call sites (player + enemy projectile loops) updated in the same pass, budget ratcheted 106481 → 106481 region down to reflect the smaller file. Build + `check:arch` green. Scoped to the reported paths only — Ricochet Rifle interior-wall ricochet (it still only bounces off the border, not interior structure walls, a pre-existing separate gap) and `InteractionSystem.js` cage-destruction debris (never registered with `physicsSystem.addEntity()` at all, so it doesn't move via the main loop regardless) were left untouched — not part of this report. Manual in-game verification (fire Ricochet Rifle at a hut wall, fire Fester's Gun at any wall, kill an enemy next to a border wall) pending. |
| 172 | **Room generation could crash outright — "next room does not load" — when a Goblin's random spawn loadout rolled a melee weapon** — `Enemy` constructor's `spawnEquipment` block (rolls a random starting weapon for goblins, `Enemy.js` ~line 276) ran and called `this.pickupItem(weapon)` → `equipWeapon(item)` *before* `this.stateMachine = new EnemyStateMachine(...)` was assigned later in the same constructor. `equipWeapon`'s melee branch reads `this.stateMachine.has('recover')` to backfill a Recover state for archetypes that don't natively declare one — on a melee roll this hit `undefined.has`, threw a `TypeError` out of the `Enemy` constructor, and aborted `RoomGenerator.generateCombatRoom()` mid-spawn, so the room never finished generating. Goblin's `spawnEquipment.weapons` pool is 7 bow entries to 3 melee entries, so roughly 30% of Goblin spawns crashed the room they were in. | user 2026-08-02 (console error: `TypeError: undefined is not an object (evaluating 'this.stateMachine.has')` in `equipWeapon` ← `pickupItem` ← `Enemy` constructor ← `generateCombatRoom`) | ✅ fixed — 2026-08-02 — moved the `spawnEquipment` roll block to after `this.stateMachine` initialization in the `Enemy` constructor; no other change. Build + `check:arch` green. |
| 171 | **Trap Goblin was a nightmare to fight: erratic wandering after one sighting, dies to its own trap ~100% of the time, and can freeze in place after laying one** — four distinct root causes, found and fixed across two passes. (1) **Self-inflicted fuse damage**: `TrapSystem.js`'s timed fuse on enemy-placed traps detonates via `_fireOneShotTrap()`, which — unlike the proximity-trigger path in `updatePlacedTraps()` (`if (enemy === entry.owner) continue`) — had no owner-immunity check anywhere in its ~250 lines, so a goblin standing anywhere near its own trap when the fuse force-fired took the hit like any other target. (2) **No real "safe distance" exit**: the first-pass fix (`moveBack` for a fixed `clearAfter` window) was a timer, not a distance check, so it could hand off before the goblin actually cleared the blast radius — and separately, the timeout safety-net that replaced it was compared against `machine.timer`, which runs in double-seconds; the effective real-world timeout (1.75s) was too tight for the largest trap radius (Fire, 112px / 136px clear distance) and fired on nearly every run instead of the real distance check ever getting the chance to. (3) **Reacquisition deadlock**: `useTrap`'s hand-off to `alert` left two stale flags set from the flee episode — `lastKnownPosition` (the frozen flee mark) and `hadVisualContact` (permanently true once Flee first saw its target) — closing all three of `alert.next()`'s doors back to Flee, so a goblin that didn't independently resume fleeing stood still, facing the wrong way, until the player just walked up and killed it. (4) **Erratic movement**: `flee.js` snapshotted `enemy.fleeElapsedTime = machine.timer` every frame — but `machine.timer` resets to 0 on every state transition, including the routine `flee -> lookback -> flee` bounce that happens roughly every second of a sustained chase (whenever the target is still visible). `moveFlee`'s scatter-jitter narrows from 60° down to a 12° floor over 2.5s of `fleeElapsedTime`, so the per-frame snapshot re-widened it back to near-maximum on every single bounce, for the entire duration of a chase — this is the literal "keeps erratically moving around... after seeing the player once" from the report. | self-spotted 2026-08-02 (user playtest report: "trap goblin is currently laying traps and not moving, inevitably killing themselves"; user correction after an incomplete first fix: "the traps are hurting it, and the expected behavior was for it to move away from its trap"); user 2026-08-02 (full report: "trap goblin is a nightmare to solve... erratically moving around and laying traps after seeing the player once... gets hit by its own trap and dies 100% of the time... expected experience has always been a cowardly combat avoiding enemy... PATH away from the player... use lookback to course check... only then places a trap... a new PATH clearly successfully will get it a safe distance from the trap radius, then idle... do not trigger flee from any state other than idle") | ✅ fixed — 2026-08-02 — four changes, one per cause above. `TrapSystem._fireOneShotTrap()` now filters `targets = entry.owner ? enemies.filter(e => e !== entry.owner) : enemies` before every damage/status loop, matching the proximity-trigger path's existing immunity. `useTrap.js`'s post-placement retreat is a real `moveFlee` run against the trap's own position, gated on actual center-to-center distance clearing `trapPos.radius + clearMargin` (not a timer) before handing off to `alert`; its safety-net timeout (for the boxed-in case) was recalibrated from an effective 1.75 real seconds to 3.5, sized against the measured ~70px/s retreat speed and the largest trap's clear distance, so the real distance check is now the dominant exit path — confirmed via a headless repro (`tools/debug/trapgoblin-repro.mjs`) exiting via the real clearance condition across every run, and `useTrap` never transitions back to `flee` under any path, structurally satisfying "don't re-trigger flee from any state other than idle." `useTrap.js`'s `exit()` hook clears `lastKnownPosition`/`hadVisualContact` on the way out, re-arming Alert's re-engage doors the same way a freshly-spawned enemy starts. `flee.js`'s `enter()` now zeroes `fleeElapsedTime` only on a genuinely new flight (the existing `if (enemy.fleeing) return` re-entry guard), and `update()` accumulates it (`(enemy.fleeElapsedTime ?? 0) + ctx.deltaTime`) instead of snapshotting `machine.timer`, so it survives lookback bounces and the scatter jitter actually converges — verified across 20 headless repro runs asserting the accumulator never regresses mid-flight, plus manual position/heading tracing showing steady progress toward a corner instead of re-randomized headings on every bounce. Scoped to Trap Goblin only — Bomb (the only other `flee`/`lookback` user) never declares `useTrap`, so its behavior is untouched. Build + `check:arch` green. **Reopened same day** — user pushback ("the code is broken, not that the concept needs direction... only in the case of active pursuit from the player should more than one trap ever actually happen") after the above still let a merely-visible, non-chasing target retrigger the whole episode, since fix (3)'s `exit()` reset was doing its job too well: it reopened *both* of `alert.next()`'s doors (sight and the facing-independent proximity door) on every single post-trap cycle, and `useTrap`'s hand-off went straight back to `alert` with no elapsed gap, so a same-tick re-check was possible. Fifth and sixth root causes, found via a headless harness rebuilt to actually call `PhysicsSystem.updateEntity()` each frame (the first version only called `enemy.update()`, which — per `enemyMovement.js`'s own header comment — only ever writes `velocity`/`targetVelocity`; real position integration happens exclusively in `PhysicsSystem`, called separately in the main loop, so the goblin's own position was silently frozen for the entire run and the harness's results could not be trusted). (5) **No real gap before re-checking**: `useTrap.to` now hands off to a new `withdraw` State (real non-frozen retreat, `movement:'back'`, 0.7s duration) instead of straight to `alert`, so a re-engage check can no longer land on the same tick the retreat ends. (6) **"Still visible" read as "still chasing"**: added an opt-in `requirePursuit` config flag (`src/entities/enemyPursuit.js`) to `alert`/`withdraw` that gates re-engagement behind "has the target closed real distance since this state visit began," not merely "is it visible/in range." First version compared raw `ctx.effectiveDistance` against an entry snapshot, which a physics-accurate harness then caught as contaminated by the *enemy's own* motion — `requirePursuit` also had to stop Alert's freeze-while-in-range hold (a frozen enemy has no distance left to close, which deadlocked the gate shut forever the moment a pursuer reached it), and once Alert wandered instead, a purely random wander step toward a stationary target read as "pursuing" by the raw-distance metric. Fixed by anchoring the comparison to the enemy's own position *at arm time* and measuring only the target's displacement from that fixed point, which the enemy's later self-motion cannot affect. Re-verified with the corrected harness (real `PhysicsSystem` integration each frame): a target that stops the instant the goblin starts laying its trap produces exactly 1 trap and the goblin holds `alert` for the rest of a 30s run with zero further transitions; continuous genuine pursuit for a full 3 simulated minutes produces well-spaced repeat episodes (~30 real seconds apart) rather than the original bug's 18-26 traps, and goblin HP stays at max in both. Build + `check:arch` green. |
| 170 | **Respawning a Bomb (`'6'`) after one had already detonated came in already permanently aggro'd instead of fleeing** — `src/data/stateDefaults.js`'s `statesFor(data)` returned `data.states` *by direct reference* for any re-authored enemy (`data.states ?? defaultStates(data)`). `data` is the shared, module-level `ENEMIES['6']` catalogue entry every instance of that enemy type reads from, so `enemy.stateMachine.declared === ENEMIES['6'].states` for every Bomb. `RipenMechanic.updateGrowth()`'s growth-stage-3 archetype flip retroactively adds `declared.approach`/`declared.search` (the same idiom `Enemy.js`'s `equipWeapon()` already used once for `declared.recover`) so the transition into chase-mode resolves instead of silently no-op'ing via the Flee-wildcard fallback (`FALLBACK.approach = ['flee','alert']` in `EnemyStateMachine.js`) — but mutating the live `declared` object meant that flip permanently corrupted the shared catalogue object for the rest of the page session: every Bomb spawned afterward inherited `approach`/`search` already declared and skipped Flee entirely, reproduced via cheat-menu respawn after one Bomb had detonated. | user 2026-08-02 (post cheat-menu bomb load) | ✅ fixed — 2026-08-02 — `statesFor` now shallow-clones an authored `states` block (`{ ...data.states }`) instead of handing out the shared reference; the clone is shallow (nested state configs like `states.flee` are still shared, which is fine since nothing mutates them in place, only adds sibling top-level keys). Verified with a headless test against the real `statesFor`/`EnemyStateMachine` modules: mutating one simulated Bomb's `declared` no longer affects a second freshly-constructed instance or the shared `data.states` object. Also closes the same latent vulnerability in `equipWeapon()`'s `declared.recover` mutation, which shared the identical root cause but had not been reported as a separate bug. Build + `check:arch` green. Architecturally significant — noted on `docs/adr/BACKLOG.md` row 39 (closed State spine) as concrete evidence for a proposed `declare(id, cfg)` API on `EnemyStateMachine`. |
| 165 | **Two ingredient inventories — the cauldron, the press and the fairy fountain each saw only half of what the player was carrying** — ingredients lived in two arrays that no single feature ever reconciled: `InventorySystem.restInventory` (banked, written on the way into REST) and `Player.inventory` (carried, filled during EXPLORE). `game.getActiveIngredients()` returned whichever one matched the current game state, so anything built on it silently ignored the other half. `AlchemySystem` and `PressSystem` went further and read `game.player.inventory` directly, which meant the cauldron and the press could not see a single banked ingredient — in REST, where both are used, that is the entire pile the player walked in with. Three other features had each hand-rolled their own carried-then-banked fallback to work around it (`ErrandSystem`, `RidgeSystem`, `InteractionSystem`), and those three implementations had already drifted apart. The split protected nothing: `handleGameOver()` clears both, so "banked" was never safer than "carried". | user 2026-07-26 (design directive, after #163) | ✅ fixed — 2026-07-26 — one pile. `InventorySystem.ingredients` is the only ingredient store; the array and both mutators are gone from `Player`, and `bankLoot()` no longer transfers anything (it saves quick slots only). Everything reads it through `game.getIngredients()` / `hasIngredient()` / `countIngredient()` / `addIngredient()` / `removeIngredient()`, converted in one pass across AlchemySystem, PressSystem, MagicSystem, RidgeSystem, ErrandSystem, InteractionSystem, TreasureOfferingSystem, MenuSystem, InventoryOverlay, DeathLedgerSystem and RoomGenerator — the three dual-pool hand-rolls collapsed to single calls. Coins are untouched: they stay in the passive wallet and `'c'` is redirected there by the accessors. Verified by grep (no `player.inventory`, `restInventory` or `getActiveIngredients` left in `src/`) and headlessly (add/count/remove, pile survives `bankLoot`, cleared by `handleGameOver`); build + `check:arch` green. The change forced an extraction to stay inside the InventorySystem budget — the robe aura, Moss Cloak and blue-armor water effects moved to a new `ArmorEffectsSystem.js` (code unchanged, bug #131 retargeted). Architecturally significant, so it is on `docs/adr/BACKLOG.md` as a P1 candidate. |
| 163 | **The fairy fountain's treasure offering menu listed only the coin — every gem was invisible to it** — `TreasureOfferingSystem._collectTreasures` read `game.getActiveIngredients()`, which is one pool, not both: `restInventory` in REST and `player.inventory` everywhere else (`main.js:4996`). Fountains are EXPLORE rooms, so the menu only ever saw the *carried* pool, while coins showed up regardless because they live in a passive wallet (`inventorySystem.getCoinCount()`) that no pool switch touches. Any gem the player had banked — which is every gem they were holding the last time they walked back to REST, since `main.js:4120` pushes the carried pool into `restInventory` on the way in — was simply missing from the cards, so a full pocket of gems presented as a one-card menu offering the coin. The Tab inventory overlay disagreed on screen at the same moment: it concatenates both pools ("banked REST + unbanked carried, always together", `InventoryOverlay.js:174`) and listed the gems the fountain claimed not to have. | user 2026-07-26 (playtest) | ✅ fixed — 2026-07-26 — `_collectTreasures` now counts across both pools and merges the totals, matching what the Tab overlay shows, and a new `_removeTreasure(char)` spends from carried first and banked second — the same order `ErrandSystem` already uses when an NPC asks for an ingredient (`ErrandSystem.js:113`), so loose findings go before stores. `game.removeIngredient` was deliberately left alone rather than given a cross-pool fallback: it is called from everywhere and its active-pool-only semantics are load-bearing elsewhere; coins still route through it, since it already redirects `'c'` to the wallet. Verified headlessly against a split inventory (Garnet in both pools, Ruby carried, Diamond banked, 3 coins): cards came back as Ruby/coin×3/Garnet×2/Diamond, and consecutive Garnet offerings drained the carried one before the banked one. Build + `check:arch` green. **Superseded the same day by #165** — the two pools no longer exist, so `_collectTreasures` reads the one pile and `_removeTreasure` is gone. |
| 161 | **`revolve`'s Attack Shape count subdivided the one travelling arc instead of repeating it around the ring, so a higher count made the teeth smaller rather than more numerous** — the `attackShapeCount` field landed as a draw-time concern in `strikeMarks` (`TelegraphAnimation.js`), which split the arc's `[from, to]` span into `count` equal steps and laid a glyph on each. Since `ARC_SWEEP` is 0.35 rad, that packed every copy into the same ~20° of circumference: a count of 5 drew five 2.5px slivers touching each other instead of five teeth spaced round the band, and the ring still read as one mark. Only the sliver widths responded to the number. The subdivision would also have been drawing-only if it had been correct — `strikeHitsBox` resolves damage against `strikeGeometry`'s arcs, which knew nothing about the count, so repeating the glyph at draw time would have shown teeth that could not hit. | user 2026-07-25 | ✅ fixed — 2026-07-25 — the count moved into the geometry: `strikeGeometry` takes it as a fifth argument and its `angle` branch emits `count` arcs at `spoke + i·2π/n`, each keeping the full `ARC_SWEEP` span, so every tooth is the same size as the lone one and all of them are hitboxes. `strikeMarks` went back to one glyph per arc and no longer reads the count for arcs at all — the multiplication has already happened upstream, which is what keeps the picture and the hit test the same object. All four `strikeGeometry` call sites in `Telegraph.js` (hit sweep, retire test, hairline trace, glyph pass) thread `attack.attackShapeCount` so none of them can disagree. Circles are untouched: a `radiate` circle damages continuously the whole way round, so its count is genuinely a sampling density and stays in `strikeMarks`. Verified: `revolve` unset → 1 arc / 1 mark / 12.5px chord (unchanged); count 5 → 5 arcs at 0/72/144/216/288°, all 12.5px; count 8 → 8 at 45° spacing; `radiate` unchanged at 14 marks unset, 4 when asked. Build + `check:arch` green. |
| 160 | **`doubleSweep`'s return stroke drew its Attack Shape unflipped, so the second pass read as a second identical swing rather than as the same one coming home** — the same defect as #158 on the other axis. Beat 1 declares `reverse: true`, which `strikeGeometry` consumed only as a direction flip (`if (!!animation.reverse !== !!beat?.reverse) t = 1 - t`) — the line travelled back across the shape, but the glyph riding it was stamped in the identical orientation, because handedness lived solely in `mirrorFrom` and that field describes a converging *pair* within one beat, not a beat that is another beat's return trip. An enemy with `attackShape: '/'` therefore swung `/` out and `/` back, when a blade coming home is `\`. | user 2026-07-25 | ✅ fixed — 2026-07-25 — `strikeGeometry` now also computes `const back = !!beat?.reverse` and returns it as `mirrorAll` on all seven geometry branches; `strikeMarks` composes it with the existing per-pair flag by XOR (`mirror: (i >= mirrorFrom) !== back`), so a returning half of a converging pair is the unmirrored glyph again — reflecting a reflection is the original. Deliberately keyed on `beat.reverse` alone rather than the animation/beat XOR that sets travel direction: `recoil` and `closeIn` are *base*-direction-reversed, so they are first strokes that happen to travel inward and are not coming back from anywhere; mirroring them would draw a return trip that never happened. Verified: `doubleSweep` beat 0 → `mirror: false`, beat 1 → `mirror: true`; `recoil`, `closeIn`, `slap` all stay unmirrored; `clap` still mirrors its far half. Build + `check:arch` green. |
| 159 | **Enemy editor: switching a telegraph's Area left the old animation selected, pairing motions with Areas they were never choreographed for** — `animationOptionsFor` (`tools/enemy-editor/src/telegraph.js`) filters the Animation dropdown by `ANIMATIONS[name].areas.includes(area)`, so the pairing rule existed — but nothing applied it to the value already held, and the filter deliberately kept the current selection listed (`|| name === t.animation`) so it stayed visible and looked chosen. Observed: box + `slap` switched to `ring` kept `slap`, and `slap` is `axis: 'across', motion: 'pass'`, so `strikeGeometry` drew a straight line sliding across the ring's full outer span — reading as a sweep over the whole area rather than as anything ring-shaped, and hit-testing along that line. The invalid pair survived every re-render and would have been emitted into `enemies.js` by codegen; `telegraphNotes` warned about it in text, which is the only reason it was noticeable at all. | user 2026-07-25 | ✅ fixed — 2026-07-25 — added `reconcileAnimation(def)` beside the filter that already owns the pairing rule: on an Area change it checks the held animation against the new Area's `areas` list and, when it does not fit, replaces it with the first motion that Area supports (ring → `blink`). The replacement is deliberately not a mapping from the old animation — any such mapping would be a guess about intent, and the right motion is one dropdown away and now visible in it, since the stale value no longer occupies the list. Wired through a generic `field.reconcile(def)` hook in `FormView.afterEdit` rather than an Area-specific branch, so the form stays ignorant of telegraph rules; it runs before `emit()`, so the sandbox never previews the inconsistent pair. Blank animations and already-valid pairs are left alone. Verified against the reported case: box+`slap` → ring now reports changed and lands on `blink`, and the dropdown reads `blink radiate closeIn revolve` with no stale `slap`. Build + `check:arch` green. |
| 158 | **`clap`'s two converging Attack Shapes were drawn as identical copies instead of mirror halves, so jaws read as two glyphs sliding the same way** — the `converge` branch of `strikeGeometry` (`TelegraphAnimation.js`) returns two lines drifting toward the shape's midline from opposite sides, and the old `strikeAnchors` reduced each to a bare point. `stampGlyph` then drew the same glyph, in the same orientation, at both — so the Bat's `attackShape: 'v'` (`enemies.js:136`) rendered as `v` above and `v` below, both pointing down, closing on each other like two falling marks rather than as a mouth. The visual is the only place the mirror could live: nothing in the geometry distinguished the far half from the near half, because a point carries no handedness. | user 2026-07-25 | ✅ fixed — 2026-07-25 — the `converge` branch now returns `mirrorFrom: 1`, declaring that from that line index on, the mark is the earlier one seen in a mirror; `strikeMarks` carries the flag through as `mark.mirror`, and `stampGlyph` applies `ctx.scale(1, -1)` after rotating into the mark's frame. The Bat's lower `'v'` is therefore drawn as a `'^'` and the pair reads as jaws closing. Handedness is expressed once, in the choreography that knows about it, rather than re-derived per glyph at draw time. Fixed in the same pass as the strike-is-the-hitbox change, so the mirrored glyph is also the mirrored hitbox. Build + `check:arch` green. |
| 157 | **Tamed rat didn't run away when hit, and could end up permanently invulnerable** — two independent defects in `NPCRat`, both visible in one encounter. (1) **The retreat had no direction of its own.** `takeDamage()` set `state = 'flee'`, but the flee branch in `update()` only ran when `this.target` was non-null and `_fleeFromTarget` ran away from that target — the nearest hostile within `AGGRO_RANGE` (9 cells), re-acquired each frame. A rat shot from outside that range, or whose target had just died, had `target === null`, so the flee branch fell straight through to the re-acquire/idle path: it never moved, and `state` was immediately overwritten back to `'idle'`/`'chase'`, discarding the remaining flee window. Even the melee path was weak — `CompanionSystem.applyEnemyDamageToTamedRats` passed no attacker, so the direction was whatever happened to be nearest, not who hit it. (2) **`permaFlee` could strand the rat forever.** On the 3rd hit in a room the rat runs for the nearest exit and despawns on arrival (`fleeReached`, checked in `updateTamedRats`). `_startPermaFlee` left `fleeTargetPos = null` when `currentRoom.exits` was missing or had no open direction — reachable, since `RoomGenerator` closes east/west (and conditionally north) on the blue-zone linear spine and `south` is `false` in `noRest` zones — and `_updatePermaFlee` then just zeroed velocity and returned, never setting `fleeReached`. The same dead end came from the ordinary blocked-path case: the exit run is a straight steer with no pathfinding, and `PhysicsSystem` zeroes the blocked axis, so scenery can park the rat short of its target. Because `permaFlee` gates both `takeDamage` (early `return false`) and `EnemyUpdateSystem._selectTarget`, the stranded rat sat there undamageable and untargetable until the player left the room. Reproduced headlessly against the real state machine: a long-range hit moved the rat 0.0px and left it `idle`; with no open exit, the rat held `fleeTargetPos: null`, `fleeReached: false` and refused damage after 10s of updates. | user 2026-07-25 | ✅ fixed — 2026-07-25 — (1) `takeDamage(amount, attacker)` now records the hit origin in `fleeFromPos`, and `_fleeFromTarget` became `_fleeFromThreat`, which retreats from that point (falling back to the current target, then to the existing heading if the source is exactly on top of the rat). The flee branch gates on `fleeFromPos || target`. Both call sites in `CompanionSystem` pass the real attacker — `p.owner` / `m.owner`, which every enemy projectile and `enemy_melee` attackData already carries; the melee site passes the swinging enemy rather than the hitbox, since the hitbox lands on the rat and gives no usable direction. (2) `_startPermaFlee` falls back to `_nearestEdgePoint()` when no exit is open (the rat bolts for the nearest wall — it can't leave through it, but the run reads as fleeing instead of freezing), and `PERMA_FLEE_TIMEOUT` (4.0s, tracked by `permaFleeTimer`) sets `fleeReached` unconditionally so the despawn always happens. Re-measured on the same repro: the long-range hit now retreats 231px and holds `flee` for the full 1.5s; all three exit configurations despawn. Normal arrival is unchanged at 1.07s, well inside the timeout, and a rat fully blocked from its target despawns at 4.00s. Build + `check:arch` green. |
| 154 | **K room vault keys could spawn inside an unbreakable object, stranding the key permanently** — both K-room key-drop paths in `RoomGenerator.js` tested eligibility by *character* alone (`keyDropConfig.eligibleObjects.includes(obj.char)`, chars `p # 0 B 8`), but an eligible char can be an unbreakable *instance*. Two kinds exist for `'0'`: obsidian rock variants (`generateMineralFormations` rolls `Math.random() < 0.3` per formation rock → `new BackgroundObject('0', …, { obsidian: true })`, which `CombatSystem.js:818` bounces every weapon off) and tunnel entrance cap rocks (`indestructible = true`, `RoomGenerator.js:970/1024`). The post-generation guarantee `ensureKeyDroppers()` was the exposed path: it sweeps *all* `room.backgroundObjects` for a char match, and mineral-formation rocks are in that array even though they never went through `applyKeyDropLogic()` themselves. When it fired, the key — and the gold `key_glitter` secret-event marker painted on top of it — landed on a rock nothing in the game can break. Measured against the real object mix (8 formation rocks at the actual 30% obsidian rate + a barrel + a crate): **26.6% of guaranteed key placements landed on an unbreakable object** (2000 runs). | user 2026-07-24 | ✅ fixed — 2026-07-24 — added `isKeyDropEligible(obj, keyDropConfig)` as the single eligibility predicate for both paths: char match **and** actually destructible (`!indestructible && !obsidian && hp !== null && !destroyed`). `obsidian` is tested on its own because the melee gate in `CombatSystem` reads `obj.obsidian`, not the `indestructible` flag — see open bug #155 for why those disagree. Extraction (RoomGenerator was 905 chars over budget): moved the whole key-drop trio out of `RoomGenerator.js` into `roomFeatures.js` as `isKeyDropEligible` / `applyKeyDropLogic(gen, obj)` / `ensureKeyDroppers(gen, room)`, matching the `(gen, room)` convention already used there by `seedSinkholes`/`generateCalderaRoom`; all 11 call sites updated in the same pass. Budget ratcheted down 181066 → 180357. Re-measured on the same repro: 0/2000 keys on unbreakable objects, and 0/2000 rooms left keyless in a realistic mix. The all-obsidian worst case now logs the pre-existing "no breakable objects" warning instead of silently placing an unreachable key. Build + `check:arch` green. |
| 153 | **Goo blobs from a melee slime kill all flew at exactly 300 px/s regardless of the momentum they inherited** — `WorldEffectsSystem.checkGooBlobHits()` shoved any blob overlapping a live melee attack, and its speed limiter read `if (speed > 150) { scale velocity to 300 }` — a *set*, not a clamp, so anything above 150 px/s was accelerated to 300 rather than held below it. Two things fed blobs straight into that threshold. (1) `spawnDeathDetritus` inherits half the enemy's knockback velocity (default weapon knockback is 300 → 150 px/s) on top of the 40-70 px/s radial launch, so a melee-killed slime's blobs start at 110-205 px/s — mostly already over the line. (2) `GooBlob.spawnInvulnDuration` (0.5s) gated only *destruction*, not the shove, and a swing lives 0.08-0.15s with no per-attack "already pushed" guard (enemies get one via `attack.hasHit`, background objects via `hasHitObject`, blobs got nothing) — so the same killing swing that spawned the blobs re-shoved every one of them on every frame it stayed alive, re-snapping each to 300 px/s. Net effect: the inherited momentum was overwritten by a fixed-magnitude radial push away from the player's own weapon. Replayed the exact `GooBlob.update` + `checkGooBlobHits` math at the real fixed 1/60 timestep: pre-fix, all 5 blobs of a melee kill peaked at exactly 300 px/s and travelled 169-171px (>⅓ of the 480px room) no matter which direction they launched; a projectile kill (no melee attack anywhere near the corpse) travelled 33-78px at 73-165 px/s, which is why bullet deaths never showed the bug. | user 2026-07-24 | ✅ fixed — 2026-07-24 — three changes in `checkGooBlobHits()`, all one root cause: the spawn-invulnerability window now `continue`s past the shove as well as the pop (a death blob is left alone while it's still flying on inherited momentum); a blob only takes one shove per swing (`GooBlob.lastPushAttack`, initialized in the constructor — identity check against the attack object, mirroring `attack.hasHit`); and the limiter is a real clamp (`if (speed > MAX) scale to MAX`), with the two magic numbers named `GOO_BLOB_PUSH_FORCE`/`GOO_BLOB_MAX_SPEED`. The `300` ceiling is kept as-authored — it is now a ceiling rather than a target, so no balance value was invented. Re-measured on the same replay: a melee kill now spreads 51-97px at 110-205 px/s, i.e. the five blobs differ by launch angle instead of all reading identically; a heavy 480-knockback weapon still throws them further (94-141px), so the momentum feature is visible for the first time; projectile kills are unchanged; a settled blob batted by a swing takes a single 22px nudge instead of being pumped to 300 px/s for the swing's duration. Also moved the `spawnDeathDetritus` doc comment back onto that method — it had been sitting above `checkGooBlobHits`, describing the wrong function. Build + `check:arch` green; feel of the new spread wants a playtest pass. |
| 152 | **Dodge roll granted every character a 0.5s post-roll invulnerability tail that was meant to be Cyan-only** — `Player.startDodgeRoll()`'s `case 'dodge'` set `invulnerabilityTimer = duration + 0.5 + extraIframes` for all three `rollType: 'dodge'` characters (Gold Hero, Green Ranger, Gray Assassin), and the Cyan Rogue's invis-recovery branch used the same buffer. Extended post-roll i-frames are the Cyan Rogue's distinguishing capability (`rollType: 'hide'`, `duration + 1.25`); handing a flat 0.5s to everyone erased that distinction and gave Gold a 0.650s invulnerable window against a 0.575s roll cooldown (`rollCooldown 0.5 × rollCooldownMult 1.15`) — i.e. the roll came back *before* its own i-frames expired, so mashing dodge was uninterrupted immunity with no punishable gap at any enemy tuning. Green Ranger's roll is a sprint (repositioning, not evasion) and was never meant to grant invulnerability at all. | user 2026-07-24 | ✅ fixed — 2026-07-24 — made the window per character rather than a shared constant, since every character's roll is meant to feel different: added `dodgeRoll.iframes` (does the roll grant invulnerability at all?) and `dodgeRoll.postRollIframes` (the tail that persists after it ends) to `Player.js`, fed from two new `characters.js` fields (`rollInvulnerable`, `rollIframes`) via `CharacterSystem.js`. Values: Gold `0.1`, Gray Assassin `0.08` (tightest — rolls often and precisely), Green Ranger `rollInvulnerable: false` / `0` (sprint, none at any point). Red Warrior (`'damage'`), Yellow Mage (`'blink'`), and the Cyan Rogue's non-recovery hide branch were already correct and are untouched. Verified headlessly against the real `startDodgeRoll()` with each character's config applied the way `CharacterSystem` applies it: i-frames/cooldown now gold 0.250/0.575s, green 0/0.517s, red 0.200/0.690s, cyan 1.500/2.300s, yellow 0/0.920s, gray 0.200/0.402s — every character leaves a vulnerable gap between rolls. Build + `check:arch` green; first-pass values need a playtest pass (logged on the pinboard). |
| 151 | **[clock-mismatch] Enemy windup blink was truncated at ~49%, so the documented 4-phase telegraph never played** — `Enemy.createWindupAttackVisual()` copied the enemy's `attackWindup` (double-seconds, per `PHYSICS.ENEMY_TIMER_RATE`) into the melee visual's `windupDuration`, but `CombatSystem`'s melee loop advanced that visual's `windupElapsed` with **raw** `deltaTime`, so progress was measured against a clock running at half the rate the value was authored for. The enemy's own `windupTimer` (ticking correctly at `deltaTime * ENEMY_TIMER_RATE`) reached zero and ended the windup when the visual's own ratio was only ~half done. Measured on the Troll (`attackWindup: 1.2`): the blink reached 48.6% of `windupDuration` and played phases `[1, 0.25, 1]` instead of the intended `[1, 0.25, 1, 0.25]` at 0/25/50/75%. The pre-existing module test could not catch this — it stepped the visual by fractions of `windupDuration`, baking in the same wrong assumption the bug was made of. | self-spotted 2026-07-23 (Telegraph validation) | ✅ fixed — 2026-07-23 — same one-line call-site fix as #150; the two are the same seam seen from either side. Re-measured on the Troll through the real frame path: blink now reaches 100% of `windupDuration` and plays all four phases. |
| 150 | **[clock-mismatch] Telegraph timers were documented in double-seconds but driven with raw deltaTime, so every authored pulse delay landed at 2× its intended time** — `src/game/Telegraph.js` states that its timers run on the enemy double-second clock (matching `Enemy.update(deltaTime * PHYSICS.ENEMY_TIMER_RATE)`), and pulse `delay` values are authored against that contract in the enemy data. But `CombatSystem`'s enemy-melee loop stepped `updateEnemyMeleeAttack(attack, deltaTime)` with raw real seconds. Measured with an authored `delay: 1.5` (intended 0.750s real): the second pulse landed at 1.500s real. Every telegraph pulse, and every windup blink, was therefore twice as slow as its data said — silently, since there were no `telegraph:` adopters in `enemies.js` yet to make it visible. | self-spotted 2026-07-23 (Telegraph validation) | ✅ fixed — 2026-07-23 — `CombatSystem` now steps the melee list with `deltaTime * PHYSICS.ENEMY_TIMER_RATE`, matching `Enemy.update()`, so an attack's `windupDuration` (copied straight from the double-second `attackWindup`) is measured against a clock that agrees with it. The melee list carries *all* enemy melee attacks, so the three real-second durations entering it were converted at their boundaries in the same pass: `createAttack()` and `createWindupAttackVisual()` now author `0.30` dbl-sec (unchanged 0.15s real), and `convertToEnemyAttack()` multiplies the incoming `duration` by `ENEMY_TIMER_RATE` (player weapons in enemy hands are authored against the raw-ticked *player* melee list). `Telegraph.js`'s `ACTIVE_DURATION`/`FLASH_DURATION` were compensated 0.15→0.30 and 0.1→0.2 so the live-hit flash is unchanged in real time. The enemy-editor sandbox (`tools/enemy-editor/src/sandbox.js`) steps the same shared module and was updated identically — otherwise the editor's preview would have diverged from the game, which is the whole reason the module is shared. Verified external readers (`CampNPCSystem.js:628`, `CompanionSystem.js:229`) touch only `windupPhase`/`hasHit`/position and are unaffected. Re-measured: authored `delay: 1.5` now fires at 0.733s real (one 60Hz frame of quantization off the 0.750s target). Build + `check:arch` green. | 
| 149 | **Duplicated/overlapping music after a full game-over reset** — `_resetRunToRest()` (`main.js:4223-4227`) calls `this.audioSystem.hardResetDualLayers(...)` without awaiting it, then immediately calls `this.stateMachine.transition(GAME_STATES.REST)` synchronously — which invokes `enterRestState()` in the same tick. Since green has a dedicated REST track, `enterRestState()`'s dual-mode branch calls `switchRestMusic()` → `switchMusicAtLoopEnd()`; because `audioSystem.isPlaying` is still `false` at that instant (the first call's fetch/decode hadn't resolved), it fell through to a **second, concurrent** `switchMusic()` call. Neither `AudioSystem.switchMusic()` nor `switchMusicAtLoopEnd()` guarded against a second call landing while the first was still in flight — `startDualSources()` only overwrites the `layer1Source`/`layer2Source` references, never stops sources a different in-flight call already started — so whichever call resolved first left its looping sources orphaned and still connected to the gain graph while the second call's sources also started on top, producing doubled/overlapping playback. Root-caused and reproduced directly (bypassing the full game, per `claudedocs/playwright-debugging.md`'s escalation ladder) by importing `AudioSystem` in an isolated Playwright page and firing the exact `hardResetDualLayers()` (unawaited) + `switchMusicAtLoopEnd()` sequence while instrumenting `AudioBufferSourceNode.prototype.start/stop` to count live sources: pre-fix, 4 sources ended up live (expected 2). | user 2026-07-22 | ✅ fixed — 2026-07-22 — added a generation counter (`AudioSystem._musicLoadId`) claimed by `switchMusic()` and `switchMusicAtLoopEnd()` before their async fetch+decode; each checks the counter again after its await and bails out (without touching `layer1Buffer`/`layer2Buffer` or calling `startDualSources()`) if a newer call has since claimed a higher value, so a superseded load can never stack sources onto the graph. `dispose()` also bumps the counter so a full teardown (e.g. re-entering TITLE) invalidates anything still in flight. Re-ran the same isolated repro against the fix: exactly 2 sources live at the end, `maxLiveCount` also 2 (the stale call never created sources at all). Confirmed the repro fails without the fix (`git stash` the change, rerun → 4 live) and passes with it. Build + `check:arch` green. |
| 147 | **AquiferSystem.generateAquifer calls a nonexistent method** — `AquiferSystem.js:93` calls `this.game.roomGenerator._cellularCaveGrid(...)`, but `RoomGenerator` has no such method (only the standalone `cellularCaveGrid` export from `roomFeatures.js`, used directly by `generateUndergroundRoom`/`generateBatBelfryRoom`). Any Frog SPACE-diving into a Quagmire Pond entrance (`AquiferSystem._dive` → `generateAquifer`) throws a TypeError instead of generating the cave. | self-spotted 2026-07-19 (Sinkhole Shortcut implementation, while using AquiferSystem as a reference pattern) | ✅ fixed — 2026-08-25 — replaced with a direct call to the imported `cellularCaveGrid(...)` from `roomFeatures.js` (same signature `(cols, rows, isOpen, seedChance, generations)`), matching how `generateUndergroundRoom`/`generateBatBelfryRoom` already consume it. Build + `check:arch` green. |
| 148 | **Yellow-zone wind (SandstormSystem) renders and pushes the player while underground in the Aquifer tunnel** — `SandstormSystem.update()` (`SandstormSystem.js:124`) and the render guard in `ExploreRenderer.js` (line 661/1043) both gated only on `isInteriorActive(game)`, which per `PlaneSystem.js` covers hut/dungeon/maze (`player._activeInteriorKind`) but not the separate Aquifer/underground tunnel mechanism (`player.plane === 1`, set by `AquiferSystem.js`). Diving into the underground tunnel left `playerInside`/`playerInInterior` false, so wind push force, lightning-strike scheduling, and sand-mote rendering all still ran underground. Lightning *damage* itself was already plane-gated (`LightningStrikeSystem.js:104`) so was not exploitable, but strikes still visually fired underground. | user 2026-07-19 | ✅ fixed — 2026-07-19 — `SandstormSystem.update()`'s `playerInside` check now also treats `game.player?.plane === 1` as inside (covers push force + lightning scheduling in one place); `ExploreRenderer.js`'s sandstorm render call site adds the same `&& game.player?.plane !== 1` guard locally (not folded into the shared `playerInInterior` flag, since that also gates unrelated bow-charge/ranger-cooldown indicators). While in the area, extracted the underground cave-fog overlay block out of `ExploreRenderer.js` into `drawUndergroundFogOverlay()` in `rendering/ui/torchLight.js` (already home to the related `hasTorchLight`/`drawPlayerTorchLight` helpers) to bring the file back under its architecture budget. Build + `check:arch` green. |
| 146 | **[damage-number-desync] Red Warrior's dodge-roll contact damage shows a hit number and knocks enemies back even when no damage actually landed** — `CombatSystem.updateRollDamage()` (`CombatSystem.js:2085-2089`) called `enemy.takeDamage(1)` and discarded the return value, then unconditionally called `createDamageNumber`, set knockback velocity, and applied the `'knockback'` status effect. `Enemy.takeDamage` returns `false` (no HP change) while the enemy is within its own 0.3s post-hit invulnerability window (`ENEMY_INVULNERABILITY_DURATION`, `Enemy.js:63/3044-3047`) or in shell form (`Enemy.js:3038`) — both reachable mid-roll (e.g. rolling through an enemy just struck by a prior attack, or a shelled enemy) — so the floating "1" and knockback fired with zero real damage applied. Every other combat call site in the file already gates display on the `takeDamage` result; this was the one exception. Third instance of the same bug class, following #51 and #145 — promoted to a `[damage-number-desync]` category. | user 2026-07-15 | ✅ fixed — 2026-07-15 — `updateRollDamage` now captures `const result = enemy.takeDamage(1)` and only spawns the damage number, applies knockback velocity, and triggers the knockback status effect inside `if (result)`. Build + `check:arch` green. |
| 144 | **[layer-leak] Eating bread (or any consumable) inside a hut gives no visual feedback** — `InventorySystem._executeWindupEffect`'s `default` case (heal/buff/shield consumables, `InventorySystem.js:1053`) and its `_createExplosion`/`_createSparkBurst` helpers (`:1089`, `:1111`) push landing-burst particles into `game.particles` without ever setting `particle.hutPlane`. `ExploreRenderer.drawParticles(game, hutPlane)` filters on `!!particle.hutPlane !== hutPlane` (`ExploreRenderer.js:2349`); untagged particles default to `hutPlane: false` and only render on the surface pass, which is never drawn while the player is shown via `HutInteriorOverlay`'s PiP (`hutPlane=true`) path. The item consumes and `player.heal()` fires correctly — only the particle feedback silently vanishes. Same root cause as resolved #107 (`[layer-leak]` category) but for consumable particles specifically, which weren't covered by that fix. | user 2026-07-10 | ✅ fixed — 2026-07-10 — tagged all three particle-push sites in `InventorySystem.js` with `hutPlane: !!this.game.activeFloor`, matching the pattern already used in `WorldEffectsSystem.js:58`. Build + `check:arch` green. Manual in-hut verification pending. |
| 143 | **Caldera (Red Zone C-room) never generates its hot-spring healing water tiles** — three stacked bugs in `generateCalderaRoom` (`roomFeatures.js`). (1) The pool-cell loop only checked `room.collisionMap[row]?.[col]` and skipped any blocked cell, with no clearing step, unlike the sibling non-red camp room which explicitly force-clears collision at its fixed campfire center. (2) `generateCalderaRoom` calls `protectRegion(room, { kind: 'cells', cells: poolCells })` right after placing the `hot_water` tiles, but `RoomGenerator.js`'s post-pass `cleanupStrayBackgroundObjects()` (`RoomGenerator.js:290`) strips any background object in a protected cell that isn't flagged `structural` — the pool tiles deleted themselves on every single generation. (3) The one actually responsible for "still just lava, no water": `gen.generateBackgroundObjects(room)` runs first and, for red zone, unconditionally scatters lava tiles (`placeLiquidStructures`/`generateLiquidFormation`/`generateDepthBasedObjects`, also char `'~'`) across the whole room. The pool loop's dedup check (`hotWaterAt`) matched by `char === '~'` + position only, not `typeId` — if a lava tile already landed on a pool cell (near-certain given lava placement is essentially room-wide), the code treated it as "water already here" and skipped creating the real `hot_water` tile, silently leaving the pre-existing lava in place. Separately, `hot_water`'s color (`#ff6633`) was nearly identical to lava's (`#ff6600`), so even a correctly-placed pool would have read as lava on sight. | user 2026-07-10 | ✅ fixed — 2026-07-10 — added missing collision-clear per pool cell; set `tile.structural = true` so cleanup no longer strips the pool; changed the dedup check to `typeId === 'hot_water'` and made the pool loop evict *any* pre-existing object (lava included) at each pool cell before placing the real tile, so the hot spring always wins at the fixed center. Changed `hot_water` color to a distinct cyan (`#33e6ff`, `GameConfig.js`). Added shore-cell derivation (walkable ring around the pool) and moved the rare outdoor Weapons Master spawn to a random shore cell instead of anywhere in the room. Added a minimal ambient steam-puff tick for hot-spring tiles (`WorldEffectsSystem.js`, ~every 3-7s, reuses `createSteamPuff`). Build + `check:arch` green; manual in-game verification of a red-zone Camp room pending. |
| 141 | **`DungeonSystem._exitDungeon` doesn't restore the companion's surface collision map** — `_activateFloor` syncs `game.companion.collisionMap = floor.collisionMap` on entry (bug #116) but `_exitDungeon` never restores it to `game.currentRoom.collisionMap` on the way out, so the companion keeps the dungeon floor's (likely differently-sized) collision map after returning to the surface — same shape as bug #140's hut gap, just on the opposite system/side. Not yet confirmed as observably broken in play (companion movement on the surface may not rely on `entity.collisionMap`-driven physics collision in every code path); needs the same live-verification treatment #140 got before fixing. Fix, if confirmed: mirror `HutSystem._exitHut()`'s `if (game.companion && game.currentRoom?.collisionMap) game.companion.collisionMap = game.currentRoom.collisionMap;`. | self-spotted 2026-07-06 (during hut companion-support investigation, resolved bug #140) | ✅ fixed — 2026-08-24 — `_exitDungeon()` now restores `game.companion.collisionMap = game.currentRoom.collisionMap` exactly as the hut path does (landed alongside the pets-enter-dungeons work, whose exit hook shares the same restore block). Build green; live verification rides the dungeon-boss playtest pass. |
| 142 | **Dodge-roll diagonal keys (Q/E/Z/C) leak movement input while the cheat-menu room-warp letter prompt is capturing keystrokes** — `DiagonalInputSystem.js` registers its own independent `window.keydown`/`keyup` listeners (at `main.js:148`, before `setupInput()`), mapping `q/e/z/c` onto `game.arrowKeys` diagonal pairs. It only guarded on `player?.polymorphed`, unlike the main input handler in `setupInput()`, which explicitly swallows all keys (`cheatMenu.isOpen` block, `main.js:450-530`) while the cheat menu — including the "press R then a letter" room-warp prompt — is open. Since `update()` never checks `cheatMenu.isOpen` either, the player's dodge roll actually fired during warp-letter capture. Confirmed live via Playwright: holding `Q` while the warp prompt was open visibly moved the player (240,432 → 211.6,403.6) on the old code. | user 2026-07-10 | ✅ fixed — 2026-07-10 — `DiagonalInputSystem` now checks `cheatMenu?.isOpen`, `menuOpen`, and `pauseSystem?.isPaused()` alongside `polymorphed` before setting `arrowKeys`, mirroring the suppression gates the main handler already applies to its own arrow-key path. Verified live: holding Q while the warp prompt is open no longer moves the player or sets `arrowKeys`, and the warp itself still correctly resolves to the target room letter. |
| 140 | **Camp NPC companion carries the surface room's stale collision map while inside a hut** — `HutSystem._enterHut()`/`_exitHut()` had no equivalent of `DungeonSystem._activateFloor`'s `game.companion.collisionMap = floor.collisionMap` sync (added for bug #116). Confirmed live via Playwright: after warping into a hut with a hired companion, `companion.collisionMap` remained the 30×30 surface-room map while `game.activeFloor.collisionMap` was the hut's actual 10×10 interior map — a dimension mismatch that would feed wrong wall data into `PhysicsSystem.updateEntity`'s collision checks for any companion physics movement indoors. Found while confirming hut-system companion support per user request, following up on bug #139's fix. | user 2026-07-06 | ✅ fixed — 2026-07-06 — `_enterHut()` now sets `game.companion.collisionMap = game.activeFloor.collisionMap` (mirroring the dungeon fix); `_exitHut()` restores `game.companion.collisionMap = game.currentRoom.collisionMap` on the way out. Verified live: `companion.collisionMap === game.activeFloor.collisionMap` and dimensions match (10×10) after entry. Rendering itself was already correctly wired (`HutInteriorOverlay.js` unconditionally renders `game.companion`); this fix addresses movement/collision correctness only, not a render skip. Note: the equivalent exit-side restoration is also missing in `DungeonSystem._exitDungeon` — not fixed here (out of scope for the hut investigation), logged for awareness. |
| 139 | **Hired camp NPC companion goes invisible the instant it's hired — real cause was NaN'd position, not a render skip** — `CampNPC.js:81` set `this.acceleration = 800` (a bare scalar). `PhysicsSystem.updateEntity` applies acceleration integration as `entity.velocity.vx += entity.acceleration.ax * deltaTime` whenever `entity.acceleration` is truthy, per the `{ax, ay}` convention used by `Player`/`Enemy`; against the scalar, `(800).ax` is `undefined`, so `undefined * deltaTime` = NaN, corrupting velocity (then position) every physics tick. This field was otherwise dead — `CampNPCSystem` drives the companion's velocity directly every frame and never reads `npc.acceleration`. The NaN was invisible before hire because only `game.companion` (not the idle/interested `room.campNPC`) is ever registered with `physicsSystem` — the corruption starts the moment `_completeCoinOffering` calls `registerWithPhysics`, so the companion's position goes NaN before the very first render after promotion, matching the reported "not rendering at all while following." Confirmed live via Playwright + NaN-trap instrumentation on `position`/`velocity` (see `claudedocs/playwright-debugging.md`): stack traced the corruption to `PhysicsSystem.js:215-216`. | user 2026-07-06 | ✅ fixed — 2026-07-06 — removed the stray `this.acceleration = 800` field from `CampNPC.js`; verified with the same Playwright repro that companion position/velocity stay finite and the companion pixel-samples visible on the foreground canvas after hire. (Bug #138 in the same session addressed a real but separate coin-toss/room-transition race — both were present but #139 is the actual cause of the reported symptom.) |
| 137 | **Huntable Moose renders as a giant slime, charges the player before fleeing, never despawns at the exit, and (after the first fix round) moved through walls with erratic slow/speed behavior** — three root causes sharing the letter `'M'` / the generic `Enemy` movement pipeline. (1) `ExploreRenderer.js` had a hardcoded `enemy.char === 'M'` special case that renders a huge 3×-scaled 'o' — written for Giant Slime (`ENEMIES['M']`), but `HuntingSystem` spawns the Moose via `Enemy`'s `dataOverride` param with `char: 'M'` too (bug #133's prior "root cause not found" session had ruled out `enemy.data` cross-contamination, but never checked the renderer for char-only checks). (2) `Enemy.update()`'s generic aggro/chase/attack state machine had no concept of `GameAnimalMechanic`'s flee/burrow state — it ran unconditionally every frame and kept re-setting `targetVelocity` toward the player, with `GameAnimalMechanic.update()` (called later in the same method) only able to correct it after the fact, surfacing as "runs straight at the player before fleeing." (3) Even after gating that off, `GameAnimalMechanic._fleeTowardTarget` still routed movement through `Enemy.updateVectorNavigation()`/`getSpeedMultiplier()` — a stuck-timer/node-path system tuned for chasing a moving player — which produced a straight line through obstacles, a slow-stack that reset instead of decaying per-stack, and a post-hit speed-up instead of continued deceleration. | user 2026-07-06 | ✅ fixed — 2026-07-06 — renderer's giant-slime branch now also gates on `enemy.data?.splitOnDamage?.enabled` (Giant Slime's defining mechanic), not just char. Added a general `data.pacifist` designation checked near the top of `Enemy.update()`: pacifist enemies skip the entire aggro/chase/attack pipeline and delegate movement fully to their own mechanic; set on `MOOSE`/`RABBIT`. Removed the now-dead duplicate `GameAnimalMechanic.update()` call further down in `Enemy.update()`. Per user's explicit choice ("simplify — bypass generic movement entirely"), `GameAnimalMechanic.js` was rewritten with self-contained movement: `_steerDirection()` uses `enemy.hasLineOfSight()` to fan out to a clear heading instead of `updateVectorNavigation()`'s stuck/node-path state; `_slowFactor()` replaces `getSpeedMultiplier()`, and slow stacks now decay one at a time on a rolling `SLOW_DECAY_INTERVAL` timer instead of a single shared reset. Architecture budget for `Enemy.js` raised (146782 → 147857) for the pacifist guard — genuine dispatch-only glue with no system to extract it into (must short-circuit inline before the aggro pipeline, which itself lives in `Enemy.js`). Build + arch check green. Follow-up architectural question (flag vs. class split) logged in `docs/adr/BACKLOG.md`. **Manual verification (spawn a hunt, confirm normal-size Moose renders, flees immediately without approaching, side-steps obstacles, slow stacks compound/decay gradually, despawns at exit) pending.** |
| 136 | **[layer-leak] Polymorphed (frog) player cannot exit hut/maze via SPACE — regression of #31** — the ADR-0001 `InteriorManager` refactor rewrote the polymorph branch in `main.js handleSpacePress()` to only check `aquiferSystem.handleSpacePress()` before unconditionally firing `createTongueAttack`, dropping the `interiorManager.handleSpacePress()` (hut/maze/dungeon exit dispatch) call that #31's original fix added. | user 2026-07-05 | ✅ fixed — 2026-07-05 — restored `if (this.interiorManager.handleSpacePress()) return;` before the tongue-attack fallback in the polymorph branch (`main.js`). |
| 135 | **[layer-leak] Frog tongue + maze weapon/projectile attacks render on the surface layer while inside an interior** — two separate causes: (1) `game.playerTongueAttacks` (frog polymorph tongue) is a bespoke collection outside `CombatSystem._tagCombatHutPlane()`'s tagged-array list, so entries were never `hutPlane`-stamped and always drew via the surface pass; (2) `_tagCombatHutPlane()` derived `hutPlane` from `game.activeFloor` only, which is null while in the maze (maze state lives on `game.mazeInterior`, not `activeFloor` — see [[active_floor_naming]]), so ALL maze combat entities (melee/projectiles/arrows/damage numbers) were tagged surface-plane and drawn both by the (unfiltered) `MazeInteriorOverlay` and by the surface pass. | user 2026-07-05 | ✅ fixed — 2026-07-05 — `_tagCombatHutPlane()` now treats `player.inMaze` as an interior plane alongside `activeFloor`; `PolymorphSystem.createTongueAttack` stamps `hutPlane` at creation and now sources enemies via `game._activeEnemies()` (plane-generic) instead of a hut-only branch; `MazeInteriorOverlay`'s bespoke unfiltered combat draw block replaced with the shared `ExploreRenderer.drawX(game, true)` helpers (the same render_helper_pattern `HutInteriorOverlay` already used), including the previously-missing player-tongue/enemy-tongue/mimic-tongue draws in the maze overlay. Build + arch check green. **Manual verification (frog tongue + weapon swing inside hut and maze) pending.** |
| 134 | **Consumable pickup silently wrote into the mana slot, then offered a phantom swap-menu slot** — Yellow Mage's free mana slot (`MagicSystem.grantYellowFreeManaSlot`) reserves an `equippedConsumables` index via `magicMeter.slots` but never sets a non-null value there. `InventorySystem.tryPickupItem`'s CONSUMABLE branch (and the equivalent crafting-menu equip path in `MenuSystem.js`) checked `equippedConsumables[i] === null` for a free slot without excluding mana-reserved indices, so the first potion picked up got silently equipped into the mana slot (hidden forever behind the mana-meter render). Every subsequent pickup then saw that index as occupied and opened `SlotReplacementSystem`'s swap menu — whose `storeIndex` was additionally hardcoded to `2` for all consumable slot counts, independent of `maxConsumableSlots` (1 by default) — presenting a "free slot" to swap into that wasn't real. Root cause of the reported "swap menu shows a slot but there's no consumable slot available" symptom; the displaced potions also explained the TAB inventory list showing stray uncollectable items. | user 2026-07-05 | ✅ fixed — 2026-07-05 — added `InventorySystem.firstFreeConsumableSlot(player)` (excludes `magicMeter.slots` when active), used by both pickup call sites; `SlotReplacementSystem.storeIndex`/`_moveSlot`/`open()`/number-key handling now derive from `equippedConsumables.length` and skip mana-reserved indices for consumables; `SlotReplacementOverlay` renders reserved indices the same way as destroyed weapon slots and matches the real store-option index; `MagicSystem.activateMagicMeter` no longer scans the unrelated weapon `destroyedSlots` array to pick its target index. |
| 133 | **Elite Mimic (`'m'`) never drops loot** — `enemies.js:781` set `affinities: ['aberration']`, but `'aberration'` is not a key in `AFFINITY_POOLS` (`items.js:2499`). `LootSystem.js:110` → `generateEnemyDrops(['aberration'], 'elite', …)` → `mergeAffinityPools` (`items.js:2872`) resolved to an undefined pool → empty drops, silently, every time. Same shadow-key-drift mechanism as open bug #90. | 2026-07 architectural review (`claudedocs/layer-audit-2026-07.md`) | ✅ fixed — 2026-07-05 — changed Mimic's `affinities` to `['generic']`, the broad grab-bag pool (weapons/armor/consumables across tiers), fitting for an enemy disguised as a treasure chest. |
| 129 | **Chain-stun gem wand cast crashed with `Can't find variable: inSamePlane`** — after bug #127 fixed the call site to reach `LightningStrikeSystem.createChainLightning`, the method itself referenced `inSamePlane` (from `PlaneSystem.js`) to gate chain targets to the same plane as the hit enemy, but the file only imported `planeOf`, not `inSamePlane` — a second, deeper crash in the same code path. | user 2026-07-05 | ✅ fixed — 2026-07-05 — added `inSamePlane` to the `PlaneSystem.js` import in `LightningStrikeSystem.js`. |
| 128 | **[warp-divergence] Switching away from Yellow Mage kept consumable slots locked as mana** — `enterExploreState` (`main.js`) captured the outgoing player's `magicMeter` before the room-transition Player reconstruction, then unconditionally pasted that stale snapshot back over `applyCharacterType`'s freshly-computed per-character meter after the fact — so a character swap immediately followed by a room transition (or one bundled into the same action) had Yellow's forced all-slots-mana state clobber the new character's correct (usually empty, or their own well-earned) slot state. `CharacterSystem.applyCharacterType` itself already restores/snapshots correctly per character via `characterInventories[type].manaState` — the bug was the later blind overwrite, not missing per-character tracking. | user 2026-07-04 | ✅ fixed — 2026-07-04 — moved the capture/restore into `CharacterSystem.captureMagicMeterForRoomTransition`/`restoreMagicMeterForRoomTransition`; restore now only applies the captured meter if the active character type is unchanged since capture, otherwise `applyCharacterType`'s restore stands. |
| 127 | **Chain-stun gem wand cast crashed with `createChainLightning is not a function`** — `MagicSystem.js`'s `_castChainStun` called `this.game.combatSystem.createChainLightning(...)`, but that method lives on `LightningStrikeSystem`, not `CombatSystem` (confirmed by two other correct call sites in `CombatSystem.js` itself, both calling `this.game.lightningStrikeSystem.createChainLightning(...)`). Surfaced via demo/attract-mode auto-play triggering the chain_stun gem wand effect. | user 2026-07-04 | ✅ fixed — 2026-07-04 — `_castChainStun` now calls `this.game.lightningStrikeSystem.createChainLightning(source, nearest, enemies)`. |
| 8 | **Flail attack speed too high** — continuous attack while holding down let the flail wall off any number of melee enemies with an instant 8-hit 360° ring burst on cooldown, making the player nigh-untouchable against crowds. | devlog v0.3→v0.4; rework 2026-07-04 | ✅ fixed — 2026-07-04 — replaced the instant ring burst with a hold-to-spin mechanic (`FlailSystem.js`): a single orbiting hit point ramps from 0 to max angular speed over `spinRampTime` and only damages at full speed; landing a hit drops the ramp back to 0 (plus a brief `spinStagger`), so every connect buys the rest of the room a re-approach window instead of the old wall-off. |
| 126 | **Errand traveler (E room) rejected an item the player was actually carrying, falling through to a weapon attack** — `ErrandSystem.checkGive`'s stage 1-2 (non-ingredient) branch only checked `player.heldItem` — the single currently-active quick slot — never scanning the player's other two quick slots, equipped armor, or carried armor spares. Any stage-2 request for an armor char (Leather Armor, Chain Mail, Robe, Warplate all appear in that stage's pool) could never be satisfied even if equipped, and a weapon sitting in a non-active slot was likewise invisible. Because a failed give doesn't consume the SPACE press, control fell through to the unconditional weapon-attack fallback later in `main.js`'s EXPLORE handling. | user 2026-07-04 | ✅ fixed — 2026-07-04 — `checkGive` now takes `inventorySystem` and checks all `player.quickSlots`, then `inventorySystem.equippedArmor`, then `inventorySystem.armorInventory` before giving up. |
| 125 | **Glittering Rock gem drop was 100% on the first hit, not 25%-per-rock-with-last-guaranteed** — `InteractionSystem`'s `spawnGemstone` handler computed `rocksRemaining` by filtering `game._activeBackgroundObjects()` for `o.glitteringRock`, but `BackgroundObject` never flattens that flag onto the instance — it only lives at `o.data.glitteringRock` (confirmed by `CombatSystem.js` correctly reading `obj.data?.glitteringRock` for the pickaxe gate). The filter always returned an empty array, so `rocksRemaining` was always `0`, and the `rocksRemaining === 0` "last rock, guarantee a gem" branch fired on every single rock — including the first — instead of only the true last one. | user 2026-07-03 | ✅ fixed — 2026-07-03 — filter now reads `o.data?.glitteringRock`; the 25%-per-rock roll with a guaranteed drop on the actual last remaining rock now behaves as intended. |
| 124 | **Sapphire ingredient shadowed by Remote Bomb — same char `(`** — the seven gemstone raw ingredients (Topaz `1`, Garnet `9`, Emerald `` ` ``, Diamond `_`, Onyx `6`, Ruby `?`, Sapphire `(`) used non-letter/non-digit punctuation chars, violating the two-tier Character Encoding Rule (raw ingredients must be letter/digit) and colliding outright: `ITEMS['(']` (Remote Bomb, TRAP) and `INGREDIENTS['(']` (Sapphire) shared the same key, and `getItemData()`'s two-tier check only special-cases `/^[a-zA-Z0-9]$/` chars, so non-alnum `(` fell through to `ITEMS[char] \|\| INGREDIENTS[char]` — Remote Bomb always won, silently shadowing Sapphire wherever code resolved the char via `getItemData` instead of the raw `INGREDIENTS` table. | user 2026-07-03 | ✅ fixed — 2026-07-03 — re-keyed all 7 gems to unused Unicode diamond/lozenge glyphs (Topaz `◇`, Garnet `⬥`, Emerald `⬦`, Ruby `◈`, Sapphire `⬨`, Onyx `⬧`, Diamond `⧫`), matching the existing raw-ingredient Unicode precedent (Pearl `●`, Arrowhead `△`, Artifact `⚜`, Moss `❦`). Updated every reference: `items.js` (INGREDIENTS, gemstone/rare_gemstone/electric dropTables), `recipes.js` (all gem-wand/whip/robe/gun/melee recipes), `MagicSystem.js` (INGREDIENT_MANA_VALUES), `MazeSystem.js` (rare-tier maze object pool), `RoomGenerator.js` (crow hoard pool), `InteractionSystem.js` (GEM_CHARS rock-drop pool), `reference.md`. Also renamed the U-room "Glittering Rock" background object from `2` to `5` (background objects stay ASCII per the encoding rule; `2` was a bland reused digit) across `GameConfig.js`, `RoomGenerator.js` (×2 spawn sites), and `TrapSystem.js`'s freeze-skip set. |
| 123 | **Enemies spawned from grass/barrels/spawn-on-death died instantly to the same swing that spawned them** — `RoomGenerator.spawnEnemiesFrom()` (used by the grass-beast drop in `InteractionSystem.handleObjectEffect`, `EnemySpawnSystem.flush()`'s mid-combat spawn queue, and `EnemySpawnSystem.handleEnemyDeath()`'s spawn-on-death) created the new `Enemy` with no hit-immunity. A melee swing's collision loop in `CombatSystem.update()` runs across the attack's whole multi-frame duration until it registers a hit (`attack.hasHit` only flips true on an actual hit), so a grass-cutting/barrel-breaking/enemy-killing swing still active on the very next frame would immediately hit and kill the enemy it just spawned. | user 2026-07-03 | ✅ fixed — 2026-07-03 — `spawnEnemiesFrom` now sets `newEnemy.invulnerabilityTimer = 1.0` (0.5 real seconds) on every newly spawned enemy, mirroring the existing spawn-iframe pattern used for Giant Slime split children (`Enemy.registerSplitChild`). |
| 122 | **Crafted Mana wrongly tried to equip into a weapon/consumable slot** — `CraftingSystem.claimCraftedItem()` unconditionally wrapped the claimed center-slot char in `new Item(char, x, y)`. Mana (`𝑚`) is a raw ingredient (defined in `INGREDIENTS`, not `ITEMS`), so `Item`'s constructor fell back to its `{ type: 'WEAPON' }` default, and `MenuSystem`'s claim handler pushed it into a quick slot instead of the ingredient pile — unlike ingredients cleared from the left/right crafting slots, which already routed correctly via `isIngredient()`. | user 2026-07-03 | ✅ fixed — 2026-07-03 — added `CraftingSystem.claimCraftedIngredient()` (returns the char only when `isIngredient()`); `MenuSystem`'s crafting-center handler now checks it first and calls `game.addIngredient()` (or `magicSystem.addMana()` directly if the meter is already active, mirroring `LootSystem.collectIngredient`) before ever falling through to the equipment-claim path. Also added a real Mana Potion (`🜛`, Base Potion + Mana recipe) as a proper `CONSUMABLE` with a `manaSlot` auto-trigger (`MagicSystem.grantTempManaSlot`/`activateTemporaryManaSlot`) — the only way to gain a *temporary* mana slot without a true well/cauldron/Yellow-Mage slot. While either kind of slot is active, `meter.active` is true, so the existing mana spawn trigger (kill drops in `main.js`) and mana refill mechanic (`LootSystem`/`CraftingSystem` pickup-to-`addMana` bypass) apply identically, since both only ever gate on `meter.active`. The temp slot expires after its duration (`MagicSystem._updateTempManaSlot`, ticked per-frame) and reverts to the true-slot state, or fully inactive if none exists. Mana itself remains a raw ingredient, never equippable. |
| 121 | **Giant Slime boss applied goo status mid-leap** — `EnemyUpdateSystem._applySlimeContact` slimed the player based purely on distance to any goo-affinity enemy, with no check for the Giant Slime's `leapWindupActive`/`leapAirborneActive` state. During its leap the boss's position is lerped across the room to the landing target (windup holds it, then it arcs through the air per `LeapAttackMechanic`) — the player would get slimed by sweeping through that path even though the body is airborne, not touching the ground. Regular melee-attack damage was already unaffected (Enemy.update() suspends the whole AI/attack state machine while leaping), so only the status leaked. | user 2026-07-03 | ✅ fixed — 2026-07-03 — `_applySlimeContact`'s slime-enemy filter now excludes enemies with `leapWindupActive` or `leapAirborneActive` set. |
| 119 | **[layer-leak] Camp NPC companion's attacks target wrong enemy list inside dungeons** — `CampNPCSystem._tryAttack` passed `game.currentRoom?.enemies` to `combatSystem.createAttack` for BOW, GUN, and MELEE; dungeon enemies live on `game.activeFloor.enemies`, so companion swings and shots never registered hits inside a dungeon floor. `_updateCompanion` already used the correct routing predicate — `_tryAttack` did not. | user 2026-06-26 | ✅ fixed — 2026-06-26 — extracted `activeEnemies` with the same `inDungeon\|\|inHut` predicate used by `_updateCompanion`; all three weapon branches now pass `activeEnemies`. |
| 118 | **Camp NPC companion renders on surface canvas while inside dungeon (double-render / wrong coords)** — `ExploreRenderer._renderCampNPCs` skipped companion rendering only when `player.inHut`, but not when `player.inDungeon`. When inside a dungeon, the companion's interior coordinates were drawn on the surface canvas (appearing at the wrong position), while `HutInteriorOverlay` correctly drew it in the PiP — resulting in a ghost render on the surface. | user 2026-06-26 | ✅ fixed — 2026-06-26 — added `!game.player?.inDungeon` to the surface-render guard, matching the `inHut` check already there. |
| 117 | **Camp NPC companion SPACE coin-offer triggers inside dungeon/hut** — `CampNPCSystem.handleSpacePress` did not guard against being called while the player was inside a dungeon or hut. `game.companion` persists across interiors, so if the player was within `COIN_INTERACT_RADIUS` of the companion (snapped beside them), pressing SPACE would trigger a coin offering with intent='hint' (companion is already COMPANION state) → zone wise-saying dialogue — even though no NPC was visible in the context. | user 2026-06-26 | ✅ fixed — 2026-06-26 — added early `if (player.inDungeon \|\| player.inHut) return false` at the top of `handleSpacePress`. |
| 116 | **Camp NPC companion not following immediately after hire; doesn't appear in next room** — `CampNPCSystem._completeCoinOffering` set `game.companion = npc` but never called `registerWithPhysics`. The campNPC existed as `room.campNPC` and was never added to `physicsSystem` (only `game.companion` is registered). So velocity was computed each frame but position never changed — the companion stayed frozen at the campfire until the next room transition. Fix also corrects the companion's dungeon-floor collision map: `DungeonSystem._activateFloor` now copies `floor.collisionMap` onto the companion after snapping position, replacing the stale surface-room map. | user 2026-06-26 | ✅ fixed — 2026-06-26 — `_completeCoinOffering` now calls `this.registerWithPhysics(game.physicsSystem)` immediately after hire; `DungeonSystem._activateFloor` sets `game.companion.collisionMap = floor.collisionMap` on floor entry. |
| 115 | **Fur Vest (and other armor/consumables) shows in weapons/items after selecting STORE IN CHEST from the slot-replacement overlay** — `SlotReplacementSystem._confirmStore()` unconditionally called `_routeToChest()` → `itemChest` for all item types, but `itemChest` is weapon-only storage that `openCraftingMenu` reads straight into the weapons column. `_confirmSlot()` already knew to return displaced armor to `armorInventory` and consumables to `consumableInventory` (same logic added in #112), but `_confirmStore` was missed. Result: any armor/consumable routed via STORE IN CHEST landed under weapons in the crafting menu. | user 2026-06-25 | ✅ fixed — 2026-06-25 — `_confirmStore` now checks `slotType`: armor → `armorInventory`, consumable → `consumableInventory`, weapon/trap → `_routeToChest` unchanged. Mirrors the type-aware routing already in `_confirmSlot`. |
| 114 | **New crafted intermediate (Axe head `⊿`) shows as "Unknown" in the crafting menu** — a craftable intermediate that is both a recipe result and a pool ingredient must be registered in **both** `ITEMS` (full def, `type: INGREDIENT`) and `INGREDIENTS` (pool/display def) in `items.js`. Axe head was added only to `ITEMS` when its recipes shipped, but ingredient-pool display resolves via `INGREDIENTS[char]` only (`main.js` getIngredientData → `INGREDIENTS[char] \|\| {name:'Unknown'}`; `Ingredient.js` constructor), so the pooled axe head rendered as "Unknown". Arrowhead `△` (the prior Rock+Rock output) worked because it was already dual-registered. | user 2026-06-19 | ✅ fixed — 2026-06-19 — added `'⊿': { char: '⊿', name: 'Axe head', color: '#aaaaaa' }` to `INGREDIENTS`, mirroring the Arrowhead entry. Now `isIngredient('⊿')` is true, the center-claim `INGREDIENT` branch pools it as a char, and display resolves correctly. Build + arch check green. Logged the dual-registration parity smell (no validation, silent "Unknown") as a P2 ADR backlog candidate. |
| 113 | **Enemy frog tongues + mimic tongues invisible inside huts/dungeons** — both tongue render blocks in `ExploreRenderer` were gated `if (!inHut && !inMaze && !inDungeon)` and had no interior-overlay pass (only the *player's* polymorph tongue, `game.playerTongueAttacks`, was routed through the overlay). So a frog enemy's tongue lash and a mimic's hook (mimics commonly spawn in dungeons disguised as chests) drew nothing in the PiP panel even though the attack landed. Same interior-render-coverage family as #110 (sticky triplines) and #111 (thrown weapons). | user 2026-06-18 | ✅ fixed — 2026-06-18 — extracted `drawEnemyTongues(game)` + `drawMimicTongues(game)` (sharing `_drawTongueSegment`), both reading `game._activeEnemies()` so they resolve to `activeFloor` enemies in interiors and `currentRoom` on the surface; enemy frog tongues now filter to owners on the active layer (the tongue list is global). Surface pass keeps its guard; `HutInteriorOverlay` calls both after the player-tongue pass (translate already applied). Build + arch check green. **Manual verification (frog enemy + revealed mimic inside a dungeon) pending.** |
| 112 | **Base Potion (and other consumables/armor) appears in the weapons crafting column** — picking up a consumable (e.g. Base Potion `G`) or armor while its slots were full opened `SlotReplacementSystem`, and `_confirmSlot` routed the **displaced** item via `_routeToChest` → `itemChest`. But `itemChest` is weapon-only storage that `MenuSystem.openCraftingMenu` reads straight into `weaponsList`, so the bumped consumable/armor then showed under WEAPONS in the crafting picker. (Same path is reused by the crafting-claim flow, which delegates occupied armor/consumable slots to `SlotReplacementSystem.open`.) | user 2026-06-18 | ✅ fixed — 2026-06-18 — `_confirmSlot` now returns the displaced armor to `armorInventory` and the displaced consumable to `consumableInventory` (their type-correct banks + menu columns), instead of `_routeToChest`. Weapon/trap branch unchanged (chest is correct for those); `_routeToChest` still used by `_confirmStore`. Build + arch check green. **Manual verification (pick up a 3rd base potion with both slots full → displaced one lands in the consumables column) pending.** |
| 111 | **[layer-leak] Thrown weapon that lands in a dungeon vanishes** — `TrapSystem._landThrownWeapon` tagged the landed pickup `item.hutPlane = t.inHut === true`, but `hutPlane` is the shared interior-floor tag for *both* huts and dungeons (LootSystem stamps `hutPlane=true` for any item on `game.activeFloor`; DungeonSystem filters dungeon loot by it; the shared `HutInteriorOverlay` draws interior items via `drawItems(game, true)` = the `hutPlane` filter). A weapon thrown inside a **dungeon** has `inHut===false`, so it landed tagged as surface loot and the dungeon overlay never drew it (and it leaked onto the surface room's item list). In-flight rendering was fine (filters on `t.interior`, set for all interiors); huts were fine (`inHut===true`). | user 2026-06-17 | ✅ fixed — 2026-06-17 — captured `inDungeon` on the in-flight weapon entry and set `item.hutPlane = t.inHut === true || t.inDungeon === true` on landing, matching the LootSystem/DungeonSystem convention. Maze unaffected (`mazePlane`). Build green. **Manual in-game verification of throw→land→pickup inside a dungeon still pending.** |
| 110 | **Sticky Tripline invisible inside huts/dungeons** — `WireSystem` fully supports placing wires in interiors (it commits triplines to `game.activeFloor.triplines` and accepts wall/bg anchors there, and SPACE falls through to `wireSystem.handleSpacePress` when not near stairs/exit), but the renderer never drew them: `ExploreRenderer._drawWires` read only `game.currentRoom?.triplines` and was guarded out in interiors (`if (!inHut && !inMaze && !inDungeon)`), while `HutInteriorOverlay` (PiP for both hut and dungeon) had no wire pass. Result: the committed segment, live preview, and red-X feedback were all silent inside huts/dungeons even though the wire was placed and ticking status. | user 2026-06-17 | ✅ fixed — 2026-06-17 — `_drawWires(game, interior=false)` now sources triplines from `activeFloor` when `interior` (player/preview coords are already interior-space, and the overlay applies the offset translate to fgCtx, so no remapping); added a `_drawWires(game, true)` pass to `HutInteriorOverlay` after particles, before the player. Surface path unchanged. Build + arch check green. **Manual in-game verification of placement+render inside a hut and a dungeon still pending.** |
| 109 | **Fairy fountain refuses most weapons when thrown** — the fountain's accept/upgrade decision (`FountainSystem._findNextTierChar`) and the duplicate-upgrade crafting cycle (`CraftingSystem.getNextTierPool`) both walked the hand-maintained `WEAPON_TIERS` table in `items.js`, which had drifted: it covered only 42 of 81 weapons (GUN/BOW/sword/axe/hammer), so the other 39 — every spear, staff, bat, dagger, whip, wand, scythe, flail, plus later GUN/BOW/sword additions like Sling, Sky Bow, Longsword, Crystal Maul — found no next tier and always hit the `refuse` branch (carried by a fairy, handed back unchanged). | user 2026-06-15 | ✅ fixed — 2026-06-15 — moved the ladder onto the items themselves: added a `tier` field to each weapon def (codemod, since removed) and replaced the `WEAPON_TIERS` literal with `buildWeaponTiers()`, derived from `ITEMS` at module load and grouped by `weaponSubtype \|\| weaponType` (same `{ family: [[tier1],[tier2],…] }` shape, so both consumers are unchanged). New weapons now participate the moment they declare a `tier`; the table can't drift again. Every weapon family's base tier now upgrades at the fountain and via duplicate-craft; the 47 remaining refusers are all genuine top-of-ladder or intentional no-ladder items (gem wands/whips per design call — element sidegrades, not a power ladder; pickaxe, scythe, flail, stun baton, fishing pole, vault key, bread). Two deliberate grouping deltas: Acid Blade `ᚢ` moved from sword-T3 to dagger-T2 (its true subtype), and Pickaxe `⛏` left the axe ladder for its own `pickaxe` family (now refuses, was an axe-T1 upgrade source). Build + arch check green. **Manual in-game verification of the carry/upgrade animation across families still pending.** |
| 108 | **Enemy wielding a picked-up melee weapon (e.g. goblin with a sword) rarely lands a hit** — the standoff/back-away branch in `Enemy.update()` (`Enemy.js:1022`) that keeps a melee enemy at its natural attack distance gated on `attackType === 'melee'` only. Enemies that pick up and equip a melee weapon run as `attackType === 'item_melee'`, so they skipped the back-away and the chaser drove them into point-blank overlap with the player. The swung-weapon arc (from `Item.createMeleeArc`) is offset by the weapon's `range` in the facing direction, so at full overlap every segment lands *past* the player and whiffs — same failure class as the boar point-blank note at `main.js:3352`. Headless sim: 6/21 swings connected. | user 2026-06-13 | ✅ fixed — 2026-06-13 — extended the back-away condition to `attackType === 'melee' || attackType === 'item_melee'` so weapon-wielding enemies retreat to their sword's-length standoff after each swing instead of overlapping. Bow-wielders (`item_ranged`) unaffected. Headless physics-integrated chase sim after fix: 20/20 swings connect, goblin holds ~22px standoff (its 24px `attackRange`). Build + arch check green. |
| 107 | **[layer-leak] Combat effects spawn on the surface while inside a hut/dungeon** — long-standing (months): using the lava sword inside a hut/dungeon spawned lava `BackgroundObject`s into the **surface** `currentRoom.backgroundObjects` at the player's relative position instead of the interior; same leak for the freeze trap (ice tiles), burn trap (ignition), enemy shockwave shake, and the Transmutation Wand polymorph (which additionally was a silent no-op on interior enemies, since it removed from `currentRoom.enemies`). Root cause: the correct accessors (`game.activeRoom` / `_activeBackgroundObjects` / `_activeEnemies`) existed but combat code bypassed them — three systems (MagicSystem, TrapSystem, FireSystem) kept **divergent private copies** of the routing (MagicSystem's checked only `inHut`, not `inDungeon`, and read the `game.backgroundObjects` surface mirror, so magic in a dungeon hit the surface), and several spawn sites hardcoded `currentRoom` + `GRID.COLS/ROWS`. | user 2026-06-12 | ✅ fixed — 2026-06-12 — centralized layer routing: `game._activeBackgroundObjects`/`_activeEnemies` made null-safe + added `game.activeGridBounds()` (interior dims/collisionMap vs surface GRID); deleted the divergent copies in MagicSystem/TrapSystem/FireSystem (now thin delegations); routed lava sweep (CharacterSystem, now reads active layer + interior bounds), freeze-trap ice (`game.activeRoom`), burn-trap ignite, shockwave shake, boulder cheat, and the full polymorph block (removal + bg/enemy/item spawns) through the canonical accessors. Added a combat-file denylist guard to `tools/check-architecture.js` forbidding direct `currentRoom.backgroundObjects`/`game.backgroundObjects` access in CharacterSystem/MagicSystem/TrapSystem/CombatSystem/WorldEffectsSystem/FireSystem/enemyMechanics; guard verified to fire on a reintroduced violation. Maze stays suppressed by design (no bg spawns, never leaks). New `[layer-leak]` category. Build + arch check green. **Manual in-game verification still pending** (lava/traps/magic inside hut+dungeon). |
| 106 | **Cheat-menu armor/consumable spawns silently stored instead of equipping when a slot was occupied** — `CheatMenu.spawnItem` only auto-equipped armor/consumables into a *free* slot; with one already equipped, the spawn fell through to `armorInventory`/`consumableInventory` storage (a dedup guard even discarded a repeat). For a gear-testing tool this read as "nothing happened / auto-equip is broken" — spawning a 2nd piece never swapped what was worn. | user 2026-06-12 | ✅ fixed — 2026-06-12 — cheat spawns now always equip: armor calls `equipArmor` (which returns the prior piece to `armorInventory`, never lost); consumables equip into the first empty slot or replace slot 0 when full (displaced piece → `consumableInventory`), then re-sync `player.equippedConsumables`. Still skips the SlotReplacementSystem prompt by design (cheat menu is itself a modal). Verified live: 2nd armor swaps cleanly with no ghost (the markBackgroundDirty at end of `spawnItem` was already present); 3 consumable spawns into 2 slots equip + displace correctly. |
| 105 | **Popup-driven armor swap in REST ghosts both glyphs in the armor slot** — REST equipment-slot glyphs draw to the background canvas layer (`EquipmentSlots.drawCell` → `bgCtx`), which is only erased on a `markBackgroundDirty()`, and the slot's per-frame "clear" is a space char (`fillText(' ')` paints nothing). Every `MenuSystem` equip path issues `markBackgroundDirty()`, but `SlotReplacementSystem._confirmSlot` (the prompt shown when armor is already equipped, reached via world-pickup or crafting-claim) did not — so after the swap the displaced armor glyph lingered in the background buffer with the newly-equipped glyph drawn on top, rendering both until the next unrelated dirty mark. | user 2026-06-12 | ✅ fixed — 2026-06-12 — `_confirmSlot` now calls `game.renderer.markBackgroundDirty()` after `updateUI()`, before `closeModal()`, matching the convention every MenuSystem equip path already follows. Covers armor, consumable, and weapon/trap swaps through the popup. |
| 104 | **Crafted armor/consumables not auto-equipped on claim from crafting table** — `MenuSystem.handleCraftingSlotClaim` ('crafting-center') pushed claimed ARMOR into `armorInventory` and CONSUMABLE into `consumableInventory` without equipping, while the world-pickup path (`InventorySystem.tryPickupItem`) auto-equips armor (`equipArmor` + `applyEquipmentEffectsToPlayer`) and consumables (first empty `equippedConsumables` slot), opening the SlotReplacementSystem prompt when occupied. Crafted gear therefore sat unequipped until a manual equip-menu trip. | user 2026-06-12 | ✅ fixed — 2026-06-12 — center-slot claim now mirrors world pickup: empty armor slot → equip + apply effects; empty consumable slot → equip there; occupied → `slotReplacementSystem.open(item, 'armor'/'consumable')` (safe for an already-claimed item: PauseSystem modal has no dismiss-without-confirm path, and `_takeFromWorld` no-ops on items absent from `game.items`/physics). |
| 97 | **Forced-zone flag eaten by render loop + gray zone not sticky — ridge north exit never actually entered gray** — `ZoneSystem.checkZoneTransition()` consumed `_forcedZone` on every call, but it also ran every render frame via `getBlendedEnvironmentColors()` → `getProgressionBlend()` (ExploreRenderer:115) and from `getProgressionColor()`/`incrementRoomCount()`, so any force set before the multi-frame `animateExitWarp()` was consumed by the next frame (blue zone only survived because its force is set synchronously 8 lines before the read in `enterExploreState`). Separately, gray was not sticky: `checkZoneTransition()` only returned 'gray' on `consecutiveGreenRooms >= 10`, so a force-entered (or cheat-warped) gray reverted to green on the very next transition. The Ridge room also never set `forceZone` at all — only the exit color. | gray-zone build-out 2026-06-11 (user: ridge north should enter gray immediately) | ✅ fixed — 2026-06-11 — split `peekZoneTransition()` (non-consuming) out of `checkZoneTransition()` (consume = peek + clear); all read-only callers peek, leaving main.js:1858 the sole consumer; sticky-gray rule added to peek (`currentZone === 'gray'` → 'gray'; escape paths `resetOnRest`/`resetOnDeath` already restore green); `generateRidgeRoom` sets `room.exits.north.forceZone = 'gray'`. |
| 98 | **Gray hardMode/noRest hardcoded in main.js and missed all runtime spawns** — the +50% buff loop in `enterExploreState` only hit room-generation enemies: Necromancer summons (`spawnEnemiesFrom`), hut interior enemies (HutSystem), and dungeon enemies (DungeonSystem) were never buffed, and the zones.js `hardMode`/`noRest` flags were dead data (main.js hardcoded `currentZone === 'gray'` for both). | self-spotted 2026-06-11 (gray build-out audit) | ✅ fixed — 2026-06-11 — flags are live: `applyZoneCombatModifiers(enemy, zone)` exported from zones.js (with `_zoneBuffed` guard against double-buffing on saved-room restores), called from `RoomGenerator.addEnemyToRoom` + `spawnEnemiesFrom` + HutSystem + DungeonSystem; `noRest` applied in `ExitSystem.generateExits` (`south: !ZONES[zoneType]?.noRest`); hardcoded main.js block deleted. |
| 99 | **B-rooms spawned off-theme bosses in every zone without a bossPool** — `createBossEnemy(depth)` rolled the legacy non-zone `SPAWN_TABLES`, so gray B-rooms (Boss exit boosted ×3 there) spawned fire elementals/knights, and red/cyan/yellow B-rooms drew off-theme too (only green had a bossPool). | self-spotted 2026-06-11 (gray build-out audit) | ✅ fixed — 2026-06-11 — `createBossEnemy(depth, zoneType)` uses `getZoneRandomEnemy` when a zone is passed; RoomGenerator passes `room.zone`. Gray additionally got its own `bossPool` (`bone_legion`, `grave_tyrant`) so the fallback rarely fires there. |
| 100 | **`zoneDepths` resets dropped the `blue` key** — the Game constructor field init, page-load reset, and `_resetRunToRest` all rebuilt `zoneDepths` without `blue` (only the death-reset at main.js:831 had it), so blue depth tracking read `undefined` after a run reset until something re-seeded it. | self-spotted 2026-06-11 (gray build-out audit) | ✅ fixed — 2026-06-11 — all three reset sites now include `blue: 0`. |
| 96 | **Flail doesn't knock back boulders (Ancient Shell fight)** — `BoulderSystem` melee-deflect gate was `if (!attack.canSmash) continue` (hammers only, from the Red puzzle "right tool" design in `7709cd7`), so the flail and every other blunt weapon passed straight through rolling boulders — most visible during the Ancient Shell head-reveal boulder rain. Not a regression; blunt deflect was never coded. | user 2026-06-11 | ✅ fixed — 2026-06-11 — deflect gate widened to `canSmash \|\| isBlunt` (flail, staff, stun baton, bat); whip's `isBlunt` removed from `SUBTYPE_DEFAULTS` per user — a lash has no mass to shove a boulder, and as a side effect whip lashes now damage background objects (cut grass, break pots) since `isBlunt` is also the can't-damage-objects gate; `r.empowered = true` now only applies on `canSmash` hits, so the ×2 speed/damage + cave-break empowerment stays hammer-exclusive and the Red puzzle is untouched. Blunt hits just knock the boulder away from the player (same cardinal-snap heading + striker grace). Phase-2 ricochet rocks ('0' enemy projectiles, bug #59 fix) are a separate system and still aren't melee-deflectable. |
| 95 | **Fisherman coin pay has no visible animation in the hut** — `FishermanDemoSystem.trySpacePress` removed the coin and started the demo instantly (SFX + 'WATCH CLOSE.' with no coin visual). The coin-arc ritual existed for the well (WellSystem) and camp NPCs (CampNPCSystem) but was never extended to the hut trade, and the HutInteriorOverlay PiP had no coin-arc draw call. | user 2026-06-11 | ✅ fixed — 2026-06-11 — extended the existing ritual instead of mirroring it: `FishermanDemoSystem` now runs a `coinAnim` (player → fisherman, interior coords, same 0.55s arc; plink/dialogue/demo start on landing, cancelled if the player exits mid-flight); coin-arc drawing extracted from `_renderCampNPCs` into shared `ExploreRenderer.drawCoinArc(anim)` (render-helper pattern); `HutInteriorOverlay` draws it inside the translated PiP context via `getCoinAnim()`. | **[warp-divergence] Giant Slime never split when reached via cheat warps — room-swap paths skipped `enemy.setGame()`** — `_trySplitOnDamage` early-returns when `this.game?.enemySpawnSystem` is undefined. Only `enterExploreState` wired `setGame`/`setRoom`; every other `currentRoom`-swap path (`handleZoneTeleport`, `handleDepthJump`, `handleBossTest`, `handleRoomWarp`, `_setupDemoWorld`, `enterMazeTestRoom`, `_applyDemoEnemies`) hand-copied fragments of the room-entry block and drifted: no `setGame` (split children, per-enemy hit SFX, and sound detection all silently dead on warped rooms), `handleDepthJump`'s grace wrote a never-read `enemy.graceTimer` (aggro not suppressed), zone teleport carried the previous room's items, and the empty-room exit unlock never ran on warps. | user 2026-06-10 ("boss slime still does not spawn slime enemies on damage" — tested via cheat warp) | ✅ fixed — 2026-06-10 — extracted `Game.applyRoomSwap(room, {resetEntities, grace})` as the single room-swap core (entity-array resets + ridge/fairy/fisherman/errand attachments, sound-event clear, exitsLocked policy, `wireRoomEnemies` incl. setGame/setRoom/setTarget/setCollisionMap, entry grace, physics+combat reset, redraw) and routed `enterExploreState` (new-room AND restore branches) plus all warp/demo/maze paths through it; `switchZoneMusic(zone)` de-duplicated the same way; `handleRoomWarp` now also stamps `room.exitLetter`. Verified live (Playwright): all six entry paths produce identically wired rooms (game/room refs, grace, physics registration), Giant Slime splits one child per hit via `handleRoomWarp('B')`, zero errors over 60-frame boss fight. main.js −170 lines; arch budget ratcheted 6367→6215. Remaining cheat-only diffs tracked as #94; category tag created. |
| 92 | **Enemies tick twice per frame — stacked update loops swallow events and double all enemy timers** — `main.js` `updateExploreState` enemy loop and `CombatSystem.update` enemy loop each called `enemy.update(deltaTime)` every frame. Effects: (1) all enemy timing data was de facto double-seconds; (2) each caller consumed only some `updateResult` fields and discarded the rest, so one-shot events were silently swallowed ~50% of the time (main.js dropped `dotDamage`/`sapDamage`; CombatSystem dropped `itemAttack`, `shouldSpawn`, `shouldSpewGoo`, `shouldLeapLand`, `justAggrod`); (3) the loops fought over `setTarget` — main picked nearest of player/companion/rats, CombatSystem reset to player unless charmed, so enemies flipped targets between ticks. Same disease as resolved #88. Interior enemies had the same double tick via HutSystem/DungeonSystem + CombatSystem. | self-spotted 2026-06-10 (root-caused while fixing #91) | ✅ fixed — 2026-06-10 — single canonical tick per frame: main.js surface loop (and HutSystem/DungeonSystem interior loops) call `enemy.update(deltaTime * PHYSICS.ENEMY_TIMER_RATE)` (new constant = 2, preserving tuned pacing; enemy timing data stays double-seconds, documented in GameConfig) and cache the result on `enemy._frameUpdateResult`; CombatSystem consumes the cached result instead of re-ticking, so dot/sap AND item-attack/spawn/spew/leap/aggro events all fire exactly once. Charm/noise targeting extracted to `CombatSystem.applyTargetOverrides`, applied at the tick site before `update()` — nearest-target (player/companion/rats) now wins when no override applies. Interior exits null the cache so stale events can't replay on re-entry. Verified live (Playwright tick counter): exactly 1 update/enemy/frame at exactly 2× dt, no errors. NOTE: un-swallowing events is a net enemy buff (item attacks/spawns/spews now fire reliably) — balance playtest still recommended. |
| 91 | **Giant Slime (B room) split children re-merge after 2.5s — no-reabsorb cooldown half its data value** — `splitOnDamage.mergeCooldown: 5.0` was tuned as seconds, but enemies tick twice per frame (bug #92: main.js enemy loop + CombatSystem enemy loop both call `enemy.update(dt)`), so `ReformMechanic` drained `mergeCooldownTimer` at 2×/s. Children spawned correctly (verified headless + live Playwright: spawn, `parentRef`, hp, link all correct) but in melee combat boss/children/player converge on one pile, so each child hit contact range right as the shortened cooldown expired and was absorbed ~2.5s after spawning — reading as "boss never releases separate slimes." | user 2026-06-10 | ✅ fixed — 2026-06-10 — `mergeCooldown` set to `10.0` double-seconds (= 5 real seconds), matching the weapon double-seconds convention from #88; comment on the field documents the tick doubling. Live verification: child born t=0.33s reabsorbed t=5.33s (exactly 5.0s window), 8 children roaming as separate entities at end of a 12s fight. |
| 89 | **Yellow zone: player permanently vanishes (NaN position) — Spark jumper on river water** — Two compounding bugs. (a) `JumpMechanic.update` resolved jump params via `onWater ? cfg.waterJumpSpeed ?? jb?.waterJumpSpeed : …` with no land fallback; the Spark ('e', `movementStyle: 'jumper'`, `float: true`) defines no water params, so any Spark jumping while over a yellow river tile got `velocity = dir * undefined = NaN`, NaN-ing its own position. (b) `PhysicsSystem.resolveEntityContacts` then propagated the corruption to the player: `dx/dy` vs the NaN enemy are NaN, but `Math.sqrt(NaN) \|\| 1` masks the distance as 1, which passes `dist < MIN_DIST`, and `applyImpulse` writes NaN into the player's velocity → position. `enforceGridBounds` is NaN-blind (all `<`/`>` comparisons false) so the position never recovers and `fillText` at NaN draws nothing — player invisible for the rest of the run. Repro'd in-browser (Playwright NaN-trap on position/velocity setters): red warrior dodge-rolling in yellow rooms NaN'd within ~18s; stack trace pinned `applyImpulse ← resolveEntityContacts`. | user 2026-06-10 | ✅ fixed — 2026-06-10 — (a) `JumpMechanic` water params now fall back to land params (`waterX ?? landX`), so jumpers without water tuning just use their land jump over water. (b) `resolveEntityContacts` skips enemies whose relative offset is non-finite, so a corrupted enemy can never poison the player again. Verified: unit check on the mechanic + 2.5-min Playwright runs (all 5 characters, 14 yellow rooms) with zero NaN traps and zero invisible frames. |
| 88 | **Held-item update ran twice per EXPLORE frame — all weapon timing was 2× data values; hammer shockwaves intermittently dropped** — `updatePlayerMechanics` (shared by REST/NEUTRAL/EXPLORE) contained the staff-block-clear + `heldItem.update(dt)` → windup-attack pipeline, and `updateExploreState` repeated the block verbatim. In EXPLORE both ran each fixed step: every cooldown/reload/windup/charge ticked at double rate (REST/NEUTRAL at 1×), and because the duplicate lacked the `triggerShockwave` branch, whichever copy crossed the windup threshold consumed the attack — hammer shockwaves fired only when the shared copy won. Git history: `179bdbb` extracted the pipeline into `updatePlayerMechanics` and pointed EXPLORE at it without deleting the inline copy, so all weapon tuning since was done against 2× ticking. | refactor audit 2026-06-09 | ✅ fixed — 2026-06-10 — duplicate EXPLORE block deleted; single source in `updatePlayerMechanics` now ticks at `PHYSICS.WEAPON_TIMER_RATE` (= 2), preserving tuned EXPLORE feel exactly while giving REST/NEUTRAL parity and making shockwaves always fire. Weapon timing data is therefore in "double-seconds" (effective = value/2) — documented on the constant; normalizing data values is an optional deliberate follow-up pass. |
| 87 | **Yellow Mage boss reward unlocks a normal consumable slot, not a mana slot** — `CharacterSystem.applyCharacterType` calls `MagicSystem.activateAllMagicMeterSlots` for the Yellow Mage on character activation, which sweeps `i < maxConsumableSlots` and pushes each index into `meter.slots`. But `BossSystem._grantBossReward` only invoked `inventorySystem.unlockConsumableSlot()` (which bumps `maxConsumableSlots` and grows the equippedConsumables array) and never re-ran the sweep. So the freshly unlocked slot stayed a regular consumable slot — Yellow Mage's "always-on mana" invariant was violated until the next character swap re-ran activation. | user 2026-06-06 | ✅ fixed — 2026-06-06 — `_grantBossReward` now re-runs `magicSystem.activateAllMagicMeterSlots(player)` after `unlockConsumableSlot()` when `activeCharacterType === 'yellow'`. The activate sweep is idempotent for existing mana slots and adds the newly unlocked index, restoring the always-on-mana invariant. Gated on character type so non-yellow boss rewards still grant a normal consumable slot. |
| 86 | **Displaced weapons survive death via REST chest** — When the player picked up a weapon in EXPLORE with all quick slots full, `main.js:7302` routed the swapped-out weapon directly into `inventorySystem.itemChest` via `addToChest`. Character death (`main.js:6405-6408`) only cleared `restQuickSlots`, `equippedArmor`, and `equippedConsumables` — `itemChest` was untouched (only full game over cleared it via `handleGameOver`). So a player who picked up two weapons during EXPLORE and then died on the same character pass would find the displaced weapon still in the chest on the next character's REST entry, violating the "everything equipped is lost on death" rule. | user 2026-06-06 | ✅ fixed — 2026-06-06 — added `pendingChestDeposits` buffer to `InventorySystem` with `deferToChest(item)` method. EXPLORE pickup site now defers via `deferToChest`; `bankLoot` flushes the buffer into `itemChest` on safe REST return; character death and `handleGameOver` both clear the buffer. REST-state pickups (cheat menu, crafting reclaim) still call `addToChest` directly since they aren't at risk of being lost. |
| 85 | **Reclaiming a weapon from a crafting slot overwrites the equipped weapon** — `MenuSystem._returnSlotItemToInventory` (called when the player presses on a filled left/right crafting slot) called `game.player.pickupItem(item)` and discarded the returned dropped item. When all quick slots were full, `pickupItem` swapped the incoming weapon into the active slot and returned the previously equipped weapon — which was then dropped on the floor. The center-claim path (`handleCraftingSlotClaim` for `crafting-center`, line 827) already routed the dropped weapon into the chest; the left/right reclaim path didn't. | user 2026-06-06 | ✅ fixed — 2026-06-06 — `_returnSlotItemToInventory` now captures the return of `pickupItem` and forwards it to `inventorySystem.addToChest` for WEAPON/TRAP, matching the center-slot path and explore-mode parity. |
| 84 | **Goblin Brute boss-room followers fly across the screen on melee swing** — `LeaderFollowerMechanic.update` multiplied `enemy.velocity *= 1.3` per frame for the 2s rally-boost window (`followerBoostDuration`). Each frame: `_blendVelocity` could pull velocity toward target by at most `accelRate*dt ≈ 10 px/s`, but the rally mult adds `0.3v` and friction (`*0.9`) only undoes part of it — net factor `1.17` per frame above the threshold where blend can keep up. When `_executeLeapAttack` stamped velocity to 130 px/s on a melee swing (sword/spear/longsword followers), the boost compounded into exponential runaway — by mid-window the goblin teleported screen-wide in a tick. | user 2026-06-05 | ✅ fixed — 2026-06-05 — folded rally boost into `Enemy.getSpeedMultiplier()` (scales targetVelocity so `_blendVelocity` converges cleanly) and removed the post-blend `velocity *= mult` in `LeaderFollowerMechanic`. `_steerToSlot` (formation encircle/line) now applies the same rally mult to its direct velocity stamp so the boost stays visible during regroup. |
| 83 | **Magma Slug burns itself on its own fire trail; fire/ice enemy trails persist forever** — Two issues in the enemy-trail pipeline. (a) `TrapSystem._applyFirePuddle`/`_applyIcePuddle` checked immunity via `enemy.elementalAffinity?.immunity?.includes('burn'/'freeze')`, which bypasses the affinity-based auto-immunity introduced in the rework (`Enemy._isImmuneToEffect` recognizes `data.affinities.includes('fire')` as auto-immune to burn via `EFFECT_AFFINITY`). Magma Slug, Fire Bat, Dragon, and the ice trail-leavers all stack burn/freeze on themselves from their own trail tiles. (b) `_spawnEnemyTrailPuddle` hard-coded `Infinity` lifetime, ignoring the per-enemy `trailMechanic.trailDuration` (Magma Slug 5.0s, Fire Bat 3.0s, etc.) that was already plumbed through `trailData.duration`. Trails persisted until room exit instead of decaying. | user 2026-06-05 | ✅ fixed — 2026-06-05 — (a) both puddle handlers now gate on `enemy.shouldApplyStatusEffect('burn'/'freeze')`, routing through `_isImmuneToEffect` so affinity auto-immunity applies. (b) `_spawnEnemyTrailPuddle` takes a `duration` arg and passes it as the per-tile lifetime; main.js / DungeonSystem / HutSystem call sites now forward `td.duration`. Comment on `_dropTrailTile` updated. |
| 69 | **Demo #3 (Topaz Staff) never fires — no projectile, no enemy damage** — `_captureDemoStartState`/`_applyDemoStartState` snapshotted hp/quickSlots/activeSlot/position but not `magicMeter`. Wands require `magicMeter.active && current >= manaCost` (`MagicSystem.hasMana`), so any wand-loadout demo replayed with the default inactive meter silently failed `tryStartCharge` (`"NOT ENOUGH MANA"`). Demo #3's recorded payload had no `magicMeter` field. | user 2026-06-03 | ✅ fixed — 2026-06-03 — added `magicMeter: { active, slots, current, max }` to capture and apply paths in `main.js`, and patched the existing demo #3 entry in `src/data/demoRecording.js` with `{ active: true, slots: [0], current: 10, max: 10 }` so the wand can cast without re-recording. |
| 68 | **`[Audio] Cannot play SFX: destroy` spam during arcade attract demo** — SFX loaders only ran inside the `enterRestState` title→gameplay music transition; the ARCADE_DEMO state runs the full EXPLORE loop directly from TITLE, so any in-demo enemy death (`main.js:5402 playSFX('destroy')`) and every other gameplay SFX call hit `AudioSystem.playSFX` before any buffers existed and emitted the not-loaded warning. Preloading the full SFX bundle on the title context broke the audio flow (queued sources fired in a burst on first user gesture; title music interrupted the demo), so the warn is suppressed at the source instead. | user 2026-06-03 | ✅ fixed — 2026-06-03 — `AudioSystem.playSFX` now silently no-ops when `Object.keys(sfxBuffers).length === 0` (the pre-REST state where nothing has been loaded yet). Once any SFX loads via `loadSFX`, the original not-loaded warning is restored for genuine typo/missing-asset cases. SFX are intentionally silent during the attract demo — matches the original audio architecture. |
| 62 | **Object-effect queue gates dropped non-destructive results across multiple combat paths** — Five sites in `CombatSystem` (`:314` bullet, `:665` glittering-rock melee, `:685` main melee, `:1896` dodge-roll smash, `:2107`/`:2124` explosion) queued object effects with `result.destroyed && result.effect`, so non-destructive results from `BackgroundObject.takeDamage` (`'cutGrass'`, `'glitterHit'`) never reached `InteractionSystem.handleObjectEffect`. Visible symptom: blade attacks cutting tall grass never rolled pollen; dungeon `puzzleSignal` glitter objects never pulsed when struck by explosions, rolls, or pickaxe-only paths. Same gate class would silently swallow any future cut-only effects. | user 2026-05-26 | ✅ fixed — 2026-06-03 — relaxed all five gates to `if (result.effect)`. Downstream `dropChance` logic in `main.js:4986` already gates destroy effects correctly (cutGrass/glitterHit objects have no `dropChance`, so the chance gate falls through). Main-melee site was already corrected in an earlier pass; this audit aligned the remaining four. |
| 82 | **P-flagged hut bread can spawn on top of the oil press, and SPACE near both opens the press menu instead of picking the bread up** — Two compounding issues in `HutSystem.generateHutInterior`. (a) Order of operations: bread loaves were placed before the press, with a 12-attempt loop that only consulted `collisionMap` (border walls). The press is fixed at `(col=2, row=2)`; any bread roll landing there overlapped the `⊓` glyph. Decor and barrels had the same gap. (b) `Game.handleSpacePress` delegated to interior systems (`dungeonSystem`/`pressSystem`/`hutSystem`/etc.) before checking for a nearby item pickup. With bread and the press sharing one tile, SPACE always opened the press menu — there was no input route to grab the loaf. | user 2026-06-01 | ✅ fixed — 2026-06-01 — (a) reordered `generateHutInterior` so the press is placed FIRST (right after collision-map init), then added a `cellOccupied(col, row, items=[])` helper that rejects walls, existing background objects, and existing items; decor, barrels, bread, and enemy-encounter spawns all gate on it now, and the duplicate later press block was removed. (b) Lifted the nearby-item pickup probe (`physicsSystem.getDistance < 20` → `tryPickupItem`) to the top of the EXPLORE/ARCADE_DEMO branch in `handleSpacePress`, above all interior-system delegations, so any overlapping item wins SPACE. The existing lower-block pickup check is preserved as a safety net. |
| 80 | **Blue-zone armor — Shark Mask falls through to regular dodge in water; Stingray Mantle shocks its wearer and the wake is too short to matter** — Three issues with one shared root: (a) `updatePlayerMechanics` only `return`s from the Shark Mask branch when `player.diving === true`. When in water with the mask equipped but not diving (dive ended while arrows held, or `dodgeRoll.cooldownTimer > 0`), execution fell through to the standard/green-ranger dodge block at the same arrow input, producing a normal land-style roll in the water. (b) Electrified-water effect at `main.js:4222` applied stun + 1 damage to *any* entity in `liquidState === 'electrified'`, including the player who emitted the wake themselves via Stingray Mantle — wearer kept getting zapped by their own trail. (c) `_updateStingrayMantle` set each vacated water cell to `'electrified'` for only `1.0s`, so the wake decayed before the player had moved more than ~1 cell, leaving no useful trail length for enemies to walk into. | user 2026-06-01 | ✅ fixed — 2026-06-01 — (a) hoisted `dodgeDirection` to `let` and, in the Shark Mask branch's non-diving fall-through, zero it out (`{x:0,y:0}`) before reaching the standard / green-ranger dodge handlers so they cannot start a roll while the mask is active in water; movement (WASD) still flows. (b) added `isShockImmune = entity === player && player.stingrayMantle` next to the existing `isImmune` check and gated the `electrified` branch on `!isShockImmune` so the wearer ignores their own (and any) shock-water damage + stun. (c) bumped wake `setWaterState('electrified', …)` from `1.0` → `4.0s` in `_updateStingrayMantle` and updated the item-data comment to match. |
| 79 | **REST-mode ingredients tracked per-character instead of shared** — `InventorySystem.constructor` (and `setActiveCharacter`) stored a separate `inventory` array on each entry of `characterInventories`, then aliased `this.restInventory` to the active character's array. Swapping characters via `applyCharacterType` → `setActiveCharacter` redirected `restInventory` to that character's pile, hiding ingredients banked by other characters from the crafting table and `getActiveIngredients()` helpers. Ingredients have no character affinity (recipes don't care who picked them up), so partitioning them was always wrong — only quick slots and active slot index need per-character isolation. | user 2026-05-30 | ✅ fixed — 2026-05-30 — promoted `restInventory` to a single shared array on `InventorySystem`, removed the per-character `inventory` field from `characterInventories`, and stopped reassigning `restInventory` in `setActiveCharacter`. `clearAllCharacterInventories` now also clears the shared pile in place (preserving external references). Quick slots, active slot index, and mana state remain per-character. |
| 78 | **Pre-boss B exit color desyncs zone tracking — depth 14+ silently reverts to green zone** — At depth 14 in any colored zone, the pre-boss gate forces `exits.north = { letter: 'B', color: '#ff6600' }` (`main.js:5245`). The orange `#ff6600` does not match any `ZONE_COLORS` entry. `ZoneSystem.recordExit` stores that color in `pathHistory`, then `checkZoneTransition()` requires the last 3 exits to share one zone's exact color; with an orange in the mix it falls back to `'green'` (default). On the resulting room transition, `currentZone = 'green'`, `zoneDepths.green` starts incrementing, and the next pre-boss/B-exit eventually spawns the **green** zone boss even though the player believes they're still progressing the red/cyan/yellow run. Cheat depth-jump (`handleDepthJump`) and room-warp also read `currentZone`, so warping after the desync sends the player to green L<n>. Renderer was unaffected (B-exit pulse uses `preBossGateActive`, not the stored color) so the visual gave no warning. | user 2026-05-29 | ✅ fixed — 2026-05-29 — store `color: ZONES[preBossZone].exitColor` instead of the hard-coded orange so the streak survives the B-exit. ExploreRenderer still paints the sin-pulse from `preBossGateActive && letter === 'B'`. |
| 68 | **Bow-equipped enemies never fire** — `Enemy.createAttack()` calls `equippedWeapon.use(fakePlayer)`. For BOW weapons, `Item.use()` sets `isCharging = true` and returns `null`, expecting a later `releaseBow()` call. No enemy code path ever invokes `releaseBow()`, so the bow stays in charging state forever and no arrow ever fires. Affects every `attackType: 'item_ranged'` enemy with `weaponType: BOW` — currently ~70% of goblins after the spawn-pool change. Visible symptom: goblin stays in detection state (yellow `!`) at its keeper preferred distance and never resolves to an attack. | user 2026-05-29 | ✅ fixed — 2026-05-29 — `Enemy.createAttack` now special-cases BOW: starts the draw via `use()`, snaps `chargeTime` to `maxChargeTime`, and immediately calls `releaseBow()` so the arrow fires in the same tick. Player charge mechanic is untouched. |
| 77 | **Music doesn't switch to red on a green→red zone transition through exits** — `enterExploreState` set `currentMusicZone = 'red'` *before* calling `audioSystem.switchToRedSequence()`, but `switchToRedSequence` bails silently when `redBuffers` haven't loaded yet (`loadRedTracks` is fire-and-forget at first REST entry). If the player reaches red zone before that fetch resolves, `currentMusicZone` gets stuck on `'red'`, so every subsequent room transition fails the `currentMusicZone !== 'red'` guard and the switch is never retried. Cheat-menu zone teleport worked because by the time the user opened it, the tracks had loaded. | user 2026-05-29 | ✅ fixed — 2026-05-29 — `switchToRedSequence` now returns `true`/`false`; both music-switch sites (`enterExploreState` and the cheat-menu zone-teleport block) only commit `currentMusicZone = 'red'` on success, so a not-yet-loaded retry attempt re-fires on the next room transition. |
| 76 | **Warping to a zone boss room spawns the mini-boss encounter on top of the GooDragon** — `RoomGenerator.generateBossRoom` unconditionally spawned the `zone.bossPool` encounter (giant_slime / goblin_army) whenever a `ROOM_TYPES.BOSS` room was generated, including the zone-boss path. After generation, `BossSystem.activate()` then pushed the GooDragon + GooHeads into `room.enemies`, so the player faced both encounters at once. Same code path that caused bug 75: the bossPool was originally the only "boss" concept and was never gated when `BossSystem` took over zone bosses. | user 2026-05-29 | ✅ fixed — 2026-05-29 — `generateBossRoom` now skips both the bossPool branch and the `createBossEnemy` fallback when `this.isZoneBossRoom` is true, leaving `BossSystem.activate` as the sole source of zone-boss entities. 'B'-exit mini-boss path (non-zone-boss) is unchanged. |
| 75 | **Mini-boss (Giant Slime / Goblin Army) could spawn on the L1 first-room-from-REST** — two paths produced this. (a) `ExitSystem.getLetterWeightsForZone` gave 'B' a base weight of 0.10 with only a depth>=5 doubling boost, never zeroed below the threshold, so 'B' could roll at any depth and `generateBossRoom`'s bossPool branch would pick from `zone.bossPool`. (b) The REST→EXPLORE first room is generated with no exit-letter object (`currentRoom.exits.north` is the boolean `true`), so `enterExploreState` left `roomType = null` and `RoomGenerator.generateRoom` fell back to `determineRoomType()`, which had a 10% BOSS slice with no depth gate — independently of any 'B' exit. | user 2026-05-29 | ✅ fixed — 2026-05-29 — (a) `ExitSystem.getLetterWeightsForZone` now sets 'B' weight to 0 when `depth < 5`; the depth>=5 doubling is preserved. (b) `RoomGenerator.determineRoomType()` now gates its BOSS slice behind `currentDepth >= 5`; below L5 that slice rolls into COMBAT. Mini-bosses can no longer appear before L5 via either path. |
| 74 | **Top-bar "I" count showed banked ingredients in REST; ingredients duplicated across bank cycles** — `enterRestState` (main.js:874) overwrote `player.inventory` with `[...inventorySystem.restInventory]`, so the unbanked-count display showed the entire crafting-table stash. `bankLoot` never cleared the source array, so the second EXPLORE→REST cycle re-banked the already-banked items (`restInventory` grew geometrically). Design intent: `player.inventory` = unbanked-carried (at-risk on death), `inventorySystem.restInventory` = banked (crafting-table store, persists across character death). | user 2026-05-29 | ✅ fixed — 2026-05-29 — `bankLoot` now clears `playerInventory` after the push; `enterRestState` no longer copies `restInventory` back into `player.inventory`. Added `Game.getActiveIngredients/addIngredient/removeIngredient` helpers that route by current state (REST → banked, else → carried); REST-mode pickup, crafting-menu reads, crafting-consume, slot-clear returns, the Tab inventory overlay, and the cheat ingredient spawn now go through the helpers so banked ingredients remain visible and usable at the crafting table without polluting the "I" count. "I" count now blinks yellow (0.35 s cycle, `#665500`↔`#ffff00`) above 20 to nudge the player to bank. |
| 73 | **O-room (and any room with enemies) south escape-route exit shows open door but bumps invisible wall** — `ExploreRenderer.js:203/1587` renders the south gap as open whenever `exits.south && (exitsUnlocked \|\| playerHasNoItems())`, but `ExitSystem.updateExitCollisions` only opened the physical south wall cell when `exitsLocked === false`. With enemies alive + empty quick slots, the player saw the open door + REST label but the collisionMap kept `[ROWS-1][centerX] = true`, so the 4px hitbox stopped at row 28 (~y=454), never reaching `southThreshold = 458` → south-exit trigger never fired. Not Ocean-specific — same gap existed in any locked room when the escape-route condition engaged. | user 2026-05-29 | ✅ fixed — 2026-05-29 — `Game.updateExitCollisions` now overrides south wall to passable when `exitsLocked && playerHasNoItems()`; called per-frame from `updateExploreState` so picking up / dropping items toggles the wall in sync with the renderer. |
| 72 | **Dungeon regenerates on every re-entry within the same D-room visit** — `DungeonSystem._exitDungeon` wiped `game.dungeonFloors = []` and `game.dungeonCurrentFloor = -1` on every ∩-door exit. The `if (!game.dungeonFloors[0])` guard in `_enterDungeon` therefore never hit, so floor 0 was re-rolled (fresh enemies, fresh layout, fresh reward) the moment the player walked back in. Designed behavior: a D-room's floor stack should persist until the player leaves the D-room itself; main.js already wipes `dungeonFloors` on REST entry, room transitions, and respawn. | user 2026-05-29 | ✅ fixed — 2026-05-29 — `_exitDungeon` now snapshots the current floor's items/ingredients (mirroring `_activateFloor`'s save block) and clears only `hutInterior` + `dungeonCurrentFloor = -1`; the floor stack persists. Re-entry via `_enterDungeon` finds `dungeonFloors[0]` cached and calls `_activateFloor(0, spawn)` which re-pushes the floor's preserved items and re-adds its alive enemies to physics. |
| 71 | **Dungeon walls didn't block the player (layer-honoring failure)** — Two compounding issues. (1) `HutInteriorOverlay` rendered solid collisionMap cells with wall fill `#1a1512` against panel bg `#111108` (~5-channel delta) and glyph `#2e2218` (~13-channel delta from fill), so existing border walls also read as void. (2) **Root cause of the walk-through:** `ExitSystem.updateExitCollisions` (called every frame from `updateExploreState`) guarded its `player.setCollisionMap(room.collisionMap)` line with `if (!player.inMaze && !player.inHut)` — `inDungeon` was missing. Result: while in a dungeon, every frame reverted `player.collisionMap` back to the exterior room's map, so template walls drew (`game.hutInterior.collisionMap` was correct) but physics read the exterior map and let the player walk through. Maze worked because its flag was honored; dungeon was the odd one out. | user 2026-05-29 | ✅ fixed — 2026-05-29 — (a) bumped wall fill to `#3a2a1c` and glyph to `#6a4830` in `HutInteriorOverlay.js`; (b) added `!player.inDungeon` to the `ExitSystem.updateExitCollisions` guard so the per-frame collision-map overwrite skips the dungeon plane the same way it already did for maze and hut. |
| 70 | **Errand traveler in huts had no requested-item indicator or hop animation** — `HutInteriorOverlay` drew interior NPCs manually via `drawTextWithAlpha`, bypassing `npc.render()`. In E-rooms, `ExploreRenderer` delegates to `npc.render()`, which lets `ErrandCharacter` paint its stage-colored requested-item glyph above its head and run the parabolic hop when the player is far. Inside a hut the traveler appeared as a bare `e` with no signal of what to give. The give-on-SPACE logic itself was already wired for huts (`main.js:5049` selects `hutInterior.npcs`), so the bug was purely visual but made the errand feel non-functional. | user 2026-05-29 | ✅ fixed — 2026-05-29 — `HutInteriorOverlay` now calls `npc.render(ctx, gridToPixel)` for each interior NPC (same pattern as `ExploreRenderer` and the camp companion path already in the overlay), then re-applies the overlay font/baseline before the WiseFellow hint-text branch. Polymorphic — also benefits any future NPC that overrides `render()`. |
| 69 | **Goblin boss followers crashed on attack** — `CombatSystem.update` threw `TypeError: undefined is not an object (evaluating 'proj.velocity.vx')` at line 993 when a Goblin Brawler swung an equipped weapon. Root cause: player weapons return `type: 'melee'`, but `Enemy.convertToEnemyAttack` only stamped `owner`; `CombatSystem.createEnemyAttack` then fell through to its projectile branch and pushed the melee attack (no `velocity`) into `enemyProjectiles`. | user 2026-05-29 | ✅ fixed — 2026-05-29 — `convertToEnemyAttack` remaps `type: 'melee'` → `type: 'enemy_melee'`; `createEnemyAttack` now dispatches array entries by type so multi-hit arcs land in the melee queue as well. |
| 68 | **Regular 'G' Goblin still bow-only** — follow-up to boss-pool work: per plan, `'G'` should pick from a weighted weapon pool on spawn (bow + sword/axe/spear/club/dagger via `equippedWeapon`) so every goblin encounter feels different. | plan follow-up 2026-05-28 | ✅ fixed — 2026-05-29 — added `spawnEquipment` to `'G'` data (55% chance to roll one of †/⊤/↑/↾/⫯/bow); Enemy constructor honors it; `pickupItem` now swaps to strictly stronger weapons and drops the displaced one back into the world; `equipWeapon` switches movement archetype to `chaser` for melee loadouts. Goblin Brute boss encounter now spawns G followers (J Goblin Brawler removed) and B renders at 1.8× scale. |
| 66 | **Player walks into closed exits during combat** — `updateExitCollisions` only ever set exit cells to `false` (passable); when a room loaded with enemies the exits were visually locked but the collision cells stayed open, so the player could walk through and render inside the wall. | user 2026-05-27 | ✅ fixed — 2026-05-27 — `updateExitCollisions` now reads `room.exitsLocked` and sets cells to `true` when locked, `false` when open |
| 66 | **Crystal maul shockwave throws IndexSizeError** — `ExploreRenderer.js:801` inner trailing ring computed `sw.radius - GRID.CELL_SIZE * 0.6`, but the guard above only required `sw.radius > GRID.CELL_SIZE * 0.5`. Radii in the 0.5–0.6 cell-size window produced a negative arc radius → `IndexSizeError`. Reproduced when crystal maul shockwave first spawns. | user 2026-05-27 | ✅ fixed — 2026-05-27 — raised guard threshold to `> GRID.CELL_SIZE * 0.6` so the subtraction can never go negative. |
| 64 | **Venom Lance drops too frequently from weak beasts** — `beast.weapons.UNCOMMON` in `items.js:1993` contains only `['↑', '↟']` (Spear, Venom Lance). The `weak` rarity profile sets RARE weight to 0.02, which `Math.round(10 * 0.02) = 0` zeros out entirely in `getWeightedRandomFromPool` — so weak beasts (Rat 'r', Bat '^') can NEVER drop RARE beast weapons (Bone Axe, Trident, War Spear) and instead split weapon-category drops 50/50 between Spear and the recently-added Venom Lance. Since Venom Lance is a crafted tier-up (Spear + Venom = ↟), it should be a discovery reward, not a starter-zone drop. Slimes ('o') are `goo` affinity and only drop Flail '○' as a weapon — user's "slime drops it" is likely visual confusion between `○` and `↟` (both can appear in same green L1-2 room). | user 2026-05-27 | ✅ fixed — 2026-05-27 — comprehensive AFFINITY_POOLS weapon rework: beast and goo no longer drop weapons (goo drops Fishing Pole UNCOMMON only); humanoid becomes primary weapon source with COMMON/UNCOMMON/RARE tiers; elemental weapons + matching gem wands isolated to their themed pools (fire/ice/electric/venom/dragon/undead/nature). Venom Lance now lives only in humanoid UNCOMMON. Follow-up bug #65 logged for weak-tier RARE wipeout (`round(10*0.02)=0`). |
| 61 | **Trident disappears when thrown into south (or any border) wall** — `_spearHitsWall` uses `collisionMap[gridY][gridX]`; when the spear tunnels past the border wall in a single frame (`gridY ≥ 30`), the index is out-of-bounds → returns false → spear snaps to `targetY > 480` → `_landThrownWeapon` places item inside wall → `enforceGridBounds` clamps it to y=464 (south wall cell) → item unreachable. Root cause: no out-of-bounds guard in `_spearHitsWall`. | user 2026-05-25 | ✅ fixed — added `if (t.x < 0 \|\| t.y < 0 \|\| t.x >= GRID.WIDTH \|\| t.y >= GRID.HEIGHT) return true` guard at top of `_spearHitsWall`. |
| 60 | **Enemy pinned by trident never escapes (permanently stuck inside wall)** — Two sub-bugs: (1) In the movement `else` branch of `updateInFlightTraps`, when `_spearHitsWall` fires while the spear is carrying an enemy, `t.carriedEnemy` is not cleared and `carriedBySpear` is never reset — physics permanently skips the enemy. (2) In the carry block, the enemy is dragged to `t.x - C/2` BEFORE the wall check runs, so the enemy ends up inside the wall cell before being pinned; `resolveCollisionMapOverlap` then picks the wrong ejection axis (horizontal instead of vertical) and loops. | user 2026-05-25 | ✅ fixed — (1) added carry-enemy pin-and-release to the movement-step wall-hit path; (2) swapped carry block order so wall is checked before enemy drag — enemy stays at safe previous-frame position when pinned. |
| 56 | **Whip triggered frog's "weak to shock" 2x damage bonus** — frog's `elementalAffinity.weakness` used the key `'stun'` (matching weapons' `onHit: 'stun'`), but its `spellDescription` says "WEAK TO SHOCK." Whip is a blunt stunner (no `electric: true`), so any whip hit on a frog incorrectly got 2x. | user 2026-05-25 | ✅ fixed — renamed frog weakness key `'stun'` → `'shock'` in `enemies.js`; CombatSystem (both projectile and melee paths) now multiplies `getElementalModifier('shock')` into the elemental mod when the source weapon has `electric: true`. Whip no longer gets the bonus; lightning/stun-gun/chain-bow/electric-axe still do. Existing electric-affinity enemies' `immunity: ['stun']` continues to zero out lightning damage (immunity * shock = 0). |
| 54 | **Thrown weapons (SHIFT) pass through room walls and land off-screen** — `updateInFlightTraps()` had `_spearHitsWall` only wired for pinning spears carrying an enemy. Non-pinning weapons flew through the collisionMap border and solid background objects, landing at `targetX/Y` which could be outside room bounds and unreachable. | user 2026-05-15 | ✅ fixed — added per-frame `_spearHitsWall` check in the `else` (still-flying) branch for `kind === 'weapon'`; weapon reverts to its previous-frame position and lands there. Also guards the natural-stop `targetX/Y` snap with the same check. |
| 53 | **Bridge worker donation has no arc animation** — `RidgeSystem.donateAvailable()` silently removed materials from inventory with no visual feedback; the arc animation used by the Well ritual and CampNPC hire was never applied. Root cause: `_arcAnim` state and `_startDonationArc()` were absent from `RidgeSystem`. | user 2026-05-15 | ✅ fixed — added `_arcAnim` lifecycle to `RidgeSystem`; `donateAvailable()` calls `_startDonationArc(firstDonatedChar)`; arc rendered in `ExploreRenderer._renderDonationArc()` using the donated material's char |
| 52 | **Floating Boots / Rubber Boots fire on every room entry and re-fire on cooldown expiry** — both boots had `cooldown` instead of `oneShot: true`, and `InventorySystem._checkTriggerCondition` returned `true` unconditionally for `float` and `waterImmunity` without checking whether the player was actually on liquid terrain. | user 2026-05-15 | ✅ fixed — removed `cooldown`, added `oneShot: true` to both boots in `items.js`; added `if (!player.inLiquid && !player.inDamagingLiquid) return false` guards in `InventorySystem`; added `player.inDamagingLiquid` per-frame tracking in `main.js` |
| 51 | **Armor-mitigated player damage shows raw damage number** — `Player.takeDamage()` computed `actualDamage` (after defense/absorption) but did not include it in the return value; CombatSystem used raw `proj.damage`, `attack.damage`, `sapData.damage`, and `tongue.damage` for the floating number instead. | user 2026-05-15 | ✅ fixed — `takeDamage` now returns `{ damaged: true, actualDamage }` (and same for reflect path); all four CombatSystem display sites use `result.actualDamage ?? rawDamage` |
| 50 | **Dropped (SHIFT) one-shot traps become pickable floor items** — `TrapSystem.dropOrPlaceTrap()` sent one-shot traps to `game.items` (pickable) instead of `game.placedTraps` (not pickable), so a SHIFT-dropped trap could be walked over and picked back up. Thrown traps via SPACE already correctly arm to `placedTraps`. | user 2026-05-15 | ✅ fixed — added `else if (trapData.type === 'TRAP' && trapData.oneShot)` branch in `dropOrPlaceTrap()` that arms the trap in `game.placedTraps` with particle burst, matching the thrown-trap path |
| 47 | **Three unreachable crafting recipes** — `findRecipe` returns the first match; three ingredient combos had two recipes each: `b`+`b` (Bone Armor shadowed Bone Dust), `f`+`~` (Rope shadowed Leather Armor), `M`+`~` (Flail shadowed Chain Mail). Also, Lava Sword and Stun Baton both used char `╪`, making them indistinguishable. | gap analysis 2026-05-15 | ✅ fixed — Bone Dust moved to `b`+`a` (Bone+Ash), Leather Armor to `f`+`b` (Fur+Bone), Flail to `j`+`~` (Jaw+String, so Chain Mail keeps `M`+`~`), Stun Baton recharred to `⌁` |
| 46 | **U room pickaxe mining non-functional on plane 1** — `objectOnPlane()` in `PlaneSystem.js` only checked the `tunnelWall` data flag to identify plane-1 objects; it defaulted everything else to plane 0. Glittering rocks (`'2'`) have `renderOnlyOnPlane: 1` but `tunnelWall: false`, so when the player was underground (`shooterPlane = 1`), the combat system's plane gate skipped all glittering rocks — making pickaxe swings silently do nothing. | user 2026-05-10 | ✅ fixed — `objectOnPlane` now checks `renderOnlyOnPlane` before `tunnelWall`, so objects with that flag are correctly classified on their intended plane |
| 45 | **Hut/dungeon interior projectiles escape walls and render outside overlay** — `CombatSystem.isOutOfBounds` used full-canvas bounds (480×480 px). Interior rooms are 160×160 (hut) or 384×384 (dungeon), so projectiles flew past interior walls without being stopped, then rendered at translated canvas coordinates outside the PiP panel. Root cause: neither player nor enemy projectile loops checked `room.collisionMap` for solid wall cells. | user 2026-05-09 | ✅ fixed — added `_hitsWall(proj, room)` helper in `CombatSystem.js` that consults `room.collisionMap`; called in both player and enemy projectile loops after the plane-update step. Interior projectiles stop at solid border cells. |
| 44 | **Main-room arrows escape room border walls** — same root cause as #45: the collisionMap's solid border cells (col 0, col 29, row 0, row 29) were never consulted; `isOutOfBounds` only triggered at pixel 481+ so fast arrows could reach wall-cell positions without being removed. Arrows should stick to walls like they stick to objects. | user 2026-05-09 | ✅ fixed — `_hitsWall` now applies to both interior and main rooms; arrows in the main room create a `stuckType: 'wall'` stuck-arrow entry on wall collision instead of being silently removed. |
| 43 | **W room — dual coin offerings only complete one ritual** — `room.well.consumed = true` is set the moment the first SPACE press is detected, permanently locking the well. A player carrying both `¤` (Infused Coin) and `★` (Lucky Coin) could only ever deliver the infused coin (higher priority); the lucky coin offering was silently blocked. | user 2026-05-09 | ✅ fixed — `_completeRitual` now checks for a remaining valid offering after the first arc lands and resets `anim.room.well.consumed = false` when one exists, so the player can press SPACE again to sequence the second ritual |
| 42 | **U room plane-0 rocks not generating** — by design, the U room's surface (plane 0) is supposed to be a wall of rocks everywhere outside the 4 exit clearings, forcing the player to descend through a cave entrance and traverse on plane 1 to cross the room. That rock fill was missing — the surface was an empty walkable field, letting the player skip the cave entirely. | user 2026-05-07 | ✅ fixed — `generateUndergroundRoom` now scatters `'0'` rocks at every interior cell outside the 4 clearings before cave generation. Rocks default to plane 0 only (no `tunnelWall` flag), so they block surface navigation but don't interfere with the plane-1 cave |
| 41 | **Dungeon weapon sacrifice was reversible** — `DungeonSystem.handleSpacePress` had a slot-retrieval branch that returned the sacrificed weapon to the player's first empty quick slot and re-locked the stairs. Made the unlock trivially reversible. | user 2026-05-06 | ✅ fixed — removed the retrieval branch in `DungeonSystem.handleSpacePress`; sacrificed weapons are now permanent for the duration of the dungeon visit |
| 40 | **Quick-slot UI doesn't update when sacrificing weapon to dungeon platform** — same root cause as #39: `handleShiftPress` cleared `quickSlots[i]` and set `uc.slotItem` but never called `updateUI()`, and the per-frame `updateExploreState` UI call was being skipped via the `inHut` early return. The slot stayed visually filled until the next SPACE event. | user 2026-05-06 | ✅ fixed by #39 — UI now refreshes every frame inside dungeons, so the slot clears as soon as the weapon is placed |
| 39 | **HP / quick-slot UI freezes inside dungeon** — `Game.updateExploreState` calls `this.updateUI()` at its tail (~line 4332), but a guard ~50 lines earlier (`if (player.inMaze \|\| player.inHut) return;`) early-returned before reaching it. Dungeons reuse the `inHut` flag, so HP, slot contents, and depth never refreshed visually while the player was inside one — even though the underlying state was changing (you could die from invisible damage). Hut & maze interiors normally have no combat so the bug stayed latent. | user 2026-05-06 | ✅ fixed — early-return path in `main.js updateExploreState` now calls `this.updateUI()` before returning |
| 38 | **Glacier Crab ice trail did not slow the player** — same root cause as #37: `TrapSystem._applyIcePuddle` called `game.player.applyStatusEffect('slow', 0.3)`, but `Player.statusEffects` has no `slow` key — it has `freeze` (slowAmount 0.5). The call silently no-op'd at the early-return guard, so ice trails (and any other future fire-puddle/ice-puddle source) had no effect on the player. | self-spotted while fixing #37 2026-05-06 | ✅ fixed — switched to `applyStatusEffect('freeze', 0.3)`; player movement now slows on contact via the existing `getStatusSpeedMultiplier` path |
| 37 | **Magma Slug fire trail did not burn the player** — `TrapSystem._applyFirePuddle` called `game.player.applyStatusEffect('burn', 0.5)`, but `Player.applyStatusEffect` only handles `goo`/`freeze`/`slimeBoost` and silently early-returns for any other key (it has no `statusEffects.burn` entry — player burn lives on `burnDuration` via `applyBurn`). On top of that, 0.5s was shorter than the 1.5s burn tick rate, so even if the path had hit the right field nothing would have ticked. Enemy burn duration of 0.3s had the same too-short-to-tick issue. | user 2026-05-06 | ✅ fixed — `_applyFirePuddle` now calls `game.player.applyBurn(2.0)` (gated on `fireImmune`) and applies `burn` to enemies for 3.0s so a 2.5s tick reliably fires |
| 36 | **Hut interior regenerates on every re-entry** — `HutSystem._enterHut` always called `generateHutInterior()`, so barrels/decor/enemies were rebuilt fresh each time the player walked back into the same `H` room. The `hut.interiorGenerated` flag was set but never read. | user 2026-05-06 | ✅ fixed — interior is cached on `hut.interiorState`; subsequent entries reuse the cached object so destroyed barrels, defeated enemies, and NPC dialogue state persist within a single room visit |
| 35 | **Player/enemies can walk straight through the hut exit door** — `generateHutInterior()` set `collisionMap[exitRow][exitCol] = false` to "let the player stand next to the door," but combined with the door background object's `solid: false`, the south-center cell was fully passable, letting the player escape the 10×10 interior bounds. Enemies suffered the same. | user 2026-05-06 | ✅ fixed — removed the collision opening; the door cell stays solid and `nearInteriorExit()`'s 2-cell proximity radius lets the player press SPACE from one cell north without standing on the door |
| 34 | **Enemy melee windup hitbox doesn't track knockback** — `Enemy.createWindupAttackVisual()` snapshotted the hitbox position at the moment the windup started, then `CombatSystem` only animated its alpha. When the enemy got knocked back during windup (especially `windupImmune` enemies like the Slime), the telegraphed hit zone stayed in the original location while the enemy moved away. | user 2026-05-06 | ✅ fixed — windup visual now stores `ownerOffsetX/Y` (the directional offset to the enemy); `CombatSystem.update` recomputes `attack.position = owner.position + offset` each frame while `windupPhase` is true. Active strike (post-windup) still locks in place for its 0.15s window so the snap remains committed |
| 33 | **REST north exit triggers across entire top edge** — `updateRestState` only gated the north warp on `player.position.y < exitThreshold` with no x-check, so any movement above row 2 (e.g. interacting with the gravestone, which sits at row 2 with a 3-cell interaction radius) immediately fired `animateExitWarp('north')`. EXPLORE-mode's analogous check requires `gridPos.x === centerX`. | user 2026-05-03 | ✅ fixed — added `inWarpColumn` (`gridX` within `centerX ± 1`, matching the rendered warp arrows) to the REST north-exit guard in `main.js`'s `updateRestState` |
| 32 | **Boar dash deflected by soft entity separation** — `PhysicsSystem.resolveEntityContacts` applied a 1.2-cell push-apart impulse between player and any non-boss enemy, repelling the boar's dash velocity before it could overlap with the player. The dash hit detection in `main.js` (`Math.abs(px - ex) < CELL_SIZE`) never fired because the boar was nudged off-axis as it closed in. | user 2026-05-03 | ✅ fixed — added `enemy.chargeState === 'charging'` exclusion in `resolveEntityContacts` so charging enemies plow through the soft separation. Wall-stun on collisionMap walls and the dash contact-damage path remain intact |
| 31 | **Polymorphed (frog) player cannot exit witch's hut** — when the witch polymorphs the player inside her hut, SPACE near the interior exit door fired the frog tongue attack instead of triggering `hutSystem.handleSpacePress()`, because the polymorph branch in `handleSpacePress` returned early before any interior exit handler ran | user 2026-05-02 | ✅ fixed — polymorph branch in `main.js handleSpacePress` now routes through `dungeonSystem` / `hutSystem` (when `player.inHut`) and `mazeSystem` (when `player.inMaze`) before falling through to `createTongueAttack`, so a frogged player can leave any interior |
| 30 | **Pickup/notification message renders behind hut & maze overlays** — `ExploreRenderer` drew the large "spell-style" pickup message before the hut/maze PiP overlays, so the interior panel painted over it. The text was visible only outside interiors. | user 2026-05-02 | ✅ fixed — moved the `pickupMessage` block in `ExploreRenderer.js` to after `hutInteriorOverlay` and `mazeInteriorOverlay` so the message is always the topmost foreground layer |
| 29 | **Hidden mimics block exits** — the `'m'` Mimic stays disguised as a floor item until the player approaches within `revealRadius`, but the room-clear check in `main.js` (`enemies.length === 0`) counted unrevealed mimics, leaving exits locked indefinitely while the player searched fruitlessly | user 2026-05-02 | ✅ fixed — added `_isHiddenEnemy` / `_countedEnemies` helpers in `main.js`; the three room-clear checks (initial unlock, layer-2 mute, post-update clear) now ignore unrevealed mimics. Mimics remain in `enemies` so they fight normally on reveal — they just don't gate progression while disguised |
| 28 | **Boar charge mechanic never triggered** — feature was wired up in `Enemy.js` (`chargeMechanic` block) but several issues prevented it from ever firing in play: (a) `chargeTimer` initialized to full `cooldown` (3.5 s) so the boar always reached melee range before the timer expired; (b) `chargeTimer` only decremented during `state === 'chase'`, so once the boar was locked in standard melee windup/attack the cooldown never ticked down; (c) launching the charge wrote to `targetVelocity` only, so the acceleration ramp consumed almost the entire 0.5 s charge duration before reaching `chargeSpeed`; (d) no charge contact damage path — the boar would have phased through the player even if the dash had run; (e) no telegraph rendering | user 2026-05-02 | ✅ fixed — short initial delay (0.6 s); cooldown ticks down whenever a target is engaged; charge windup interrupts standard melee state; launch sets `velocity` directly to bypass acceleration; main.js handles charge contact (creates a `createMeleeAttack` with 1.5× knockback then ends the charge) and detects wall-blocks via cached previous-frame velocity; ExploreRenderer draws a red telegraph line during windup that intensifies as the windup completes, with a faded line during the dash |
| 27 | **U room projectiles/melee collide across both planes** — `CombatSystem` iterated `backgroundObjects` in its projectile, melee, dodge-roll, ice-stream, and fire-spread loops without a plane filter; `PhysicsSystem` and `Enemy` both filter by `surfaceOnly` / `tunnelWall`, but combat did not. Surface rocks blocked underground bullets and tunnel walls blocked surface bullets. | user 2026-05-02 | ✅ fixed — added `_isObjectOnPlane(obj, plane)` helper in `CombatSystem.js` mirroring the Physics rule (`surfaceOnly` → plane 0, `tunnelWall` → plane 1) and applied it in all five bg-object loops |
| 26 | **Depth exceeds boss cap (15) once zone boss is defeated** — `incrementZoneDepth` had no cap, so after defeating a zone boss (or in any post-warp scenario where the pre-boss gate at depth 14 is suppressed by `defeatedBosses.has(zone)`), each subsequent room transition kept advancing depth past `ZONES[zone].bossDepth` (15). Organic `B`-letter exits (10% weight) hit the same path. | user 2026-05-02 | ✅ fixed — `ZoneSystem.incrementZoneDepth` now caps at `ZONES[zone].bossDepth` when defined; gray zone (no `bossDepth`) is unaffected |
| 24 | **Reflect damage crashes on fire/magic projectiles** — `result.attacker.takeDamage is not a function` when player has a reflect armor and is hit by `createMagicAttack`, `createFireBreath`, `createMiniFireBreath`, or an item-wielding enemy's attack. Root cause: those methods set `owner: 'enemy'` (string) instead of `owner: this` (enemy instance); `takeDamage` doesn't exist on a string | 2026-04-23 | ✅ fixed — changed all `owner: 'enemy'` to `owner: this` in Enemy.js; also simplified `convertToEnemyAttack` (dropped redundant `enemyOwner` field, unused outside that method) |
| 25 | **Green ranger action cooldown blocked gun auto-fire** — GUN-type weapons were incorrectly triggering `actionCooldown` (the ranger's stamina bar) after each shot, preventing auto-fire while holding space. Only melee and bow weapons should consume the ranger's stamina. | 2026-04-23 | ✅ fixed — `triggerGreenActionCooldown()` in CharacterSystem now returns early when `heldItem.data.weaponType === 'GUN'` |
| 23 | **Memory mark — enemy follows old direction instead of mark** — when `aggroMemoryActive` was set by the noise-source redirect in `CombatSystem.js`, `currentDirection` was not reset; combined with the `isPursuingStaticMark` suppression of `decisionTimer` recalc in `updateVectorNavigation`, the enemy kept its old direction indefinitely until it hit a wall | 2026-04-19 | ✅ fixed — reset `currentDirection` and `memoryMoveDelayTimer` in the CombatSystem noise redirect; added `memoryMarkMisaligned` check (dot < -0.5 = >120° off) to `needsRecalc` as a safety net for any future external setters |
| 22 | **U room gemstone drops only on first rock** — `destroyObject:spawnGemstone` set `miningGemDropped = true` on first mine and skipped all subsequent rocks; intended behavior is a 25% chance per rock with a guarantee on the last unmined rock if none found yet | 2026-04-11 | ✅ fixed — replaced "first-only" flag with per-rock 25% roll + `rocksRemaining === 0` failsafe |
| 21 | **U room cave walls block entrance access** — clearings (forced-open zones in the cave cellular automata) ended exactly at the entrance row/col, so the automata could place `}` wall objects in the cell immediately inside the cave adjacent to `^`/`v`/`>`/`<` markers, making entrances unreachable from underground | 2026-04-11 | ✅ fixed — extended each clearing by 1 cell toward the cave interior |
| 20 | **U room enemies blind to player underground** — `hasVision()` and `getVisionObstructionPoint()` in `Enemy.js` iterated all `backgroundObjects` without skipping `surfaceOnly` ones; the dense surface automata layer (boulders/rocks with `surfaceOnly = true`) blocked every vision ray cast by plane-1 enemies, preventing aggro entirely | 2026-04-11 | ✅ fixed — added `if (obj.surfaceOnly && this.plane === 1) continue` guard in both functions |
| 19 | **Minable rocks drop no stone** — `destroyObject:spawnGemstone` only dropped the once-per-room rare gem; subsequent rocks dropped nothing, blocking the bridge quest that requires rock `'0'` donations | 2026-03-29 | ✅ fixed — always drop 1–2 stone `'0'` ingredients on mine; gem logic unchanged |
| 18 | **U Room surface layer blocks exits** — plane-0 surface automata placed solid boulders/rocks in the one-cell gap between exit clearings and room borders (row 28 for south, row 1 for north, col 28 for east, col 1 for west), making exits unreachable when aboveground | 2026-03-29 | ✅ fixed — extended each exit clearing by 1 cell toward the border in `generateUndergroundRoom` |
| 17 | **Plane-switch memory mark bugs** — When an aggroed enemy is chasing the player and the player switches planes (via `^` or equivalent), the system creates a memory mark at the player's *current* position instead of at the *destination* entry point; the enemy stops at the portal rather than following through. Conversely, if a memory mark already exists when the player switches planes, it should expire quickly (brief timer) since the player has left that plane — currently it persists at full duration, keeping the enemy searching the wrong plane indefinitely. | devlog 2026-03-13 | ✅ fixed — (1) lost-vision mark projected 3 cells past the entrance when player switched planes, so the enemy follows through; (2) `memoryMarkPlane` tracked at mark creation; stale timer ticks when player's plane diverges and expires the mark after 2 s |
| 16 | **Wall tiles do not block enemy vision** — `hasVision()` in `Enemy.js` uses `hasLineOfSight()` which only samples the collision map (room walls), but `getVisionObstructionPoint()` also checks background objects (trees, boulders, etc.). The aggro / memory system calls `hasVision()` so solid wall tiles between enemy and player do NOT prevent detection. Background objects block vision correctly. Fix: unify `hasVision` ray-sampling to use the same background-object check as `getVisionObstructionPoint`. | devlog 2026-03-09 | ✅ fixed — `hasVision()` now checks both collision map and background objects |
| 14 | **Goo blobs rendered twice per frame in EXPLORE** — `ExploreRenderer.renderForeground()` has two identical loops over `game.gooBlobs`; same char/color/alpha/scale drawn twice, making goo appear at near-double opacity | parity review 2026-03-09 | ✅ fixed — removed duplicate loop after particles; kept ground-layer render before enemies |
| 13 | **Entering REST does not clear EXPLORE entities** — goo blobs, particles, and other EXPLORE-spawned entities are not flushed on REST entry; they can persist visually or linger in physics system | parity review 2026-03-09 | ✅ fixed — added `particles`, `gooBlobs`, `steamClouds`, `debris`, `captives`, `neutralCharacters` clears to `enterRestState()` |
| 6 | **Consumable particles show "UNDEFINED"** — when consumables trigger, particle text renders the string "UNDEFINED" instead of the effect name or value | devlog v0.3→v0.4 | ✅ fixed — `_triggerConsumable`, `_createExplosion`, `_createSparkBurst` in `InventorySystem.js` all pushed plain particle objects without a `.char` property; `ctx.fillText(undefined, ...)` rendered the string "undefined" |
| 5 | **Tunnel room rendering artifacts** — duplicate rendering and wrong-scale tunnel walls caused by `bgCtx.drawImage(fgCanvas, 0, 0)` during background pass: (a) fgCanvas.width is in physical pixels so the DPR scale transform doubles the drawn size, (b) fgCanvas still holds the previous frame's foreground content at that point, ghosting entities onto the bg layer | devlog v0.3→v0.4 | ✅ fixed — moved tunnel wall rendering entirely to foreground pass using `drawEntityDithered`; removed cross-canvas copy hack |
| 4 | **Gap/collision regression from canvas resize** — player could no longer squeeze through previously passable gaps; likely collision/hitbox scaling broke during recent resize | devlog v0.3→v0.4 | ✅ fixed |
| 3 | **Inventory parity display bug** — fishing pole and bow both appeared simultaneously in the equipment display after weapon swapping | devlog v0.3→v0.4 | ✅ fixed — chest retrieve and chest store in `MenuSystem.js` mutated `quickSlots` without calling `markBackgroundDirty()`; background canvas cached the old item while foreground drew the new one |
| 2 | **Slime suit protection not applying** — player was inhibited by goo while slime suit was equipped; protection effect not being checked on movement/dodge | devlog v0.3→v0.4 | ✅ fixed — `applyEquipmentEffectsToPlayer` was reading all armor props from `item` instead of `item.data`; defense, immunities, speed mods were all silently failing |
| 1 | **REST area "SPACE or SHIFT" indicator missing** — hover text over player in REST hub was accidentally removed; should prompt space/shift for attack/drop | devlog v0.3→v0.4 | ✅ fixed |

---

## P2 — Balance / Missing Implementations (Resolved)

| # | Issue | Source | Resolution |
|---|-------|--------|------------|
| 215 | **Only the Centipede head actually receives the boss loot bonus — every other Miniboss silently drops normal-tier loot and a normal mana chance** — the two consumers of the flag read it off the shared data object (`LootSystem.js:132` `enemy.data.isBoss ? 3 : null` for base drop count; `main.js:3315` `enemy.data?.isBoss ? 1.0 : 0.75` for mana drop chance), and a repo-wide sweep shows `data.isBoss: true` is set in exactly one live place: `CentipedeSystem.js:412`. Everything else misses it two different ways. (a) The generic fallback Miniboss — the only kind a zone with no `bossPool` can get, which today means **yellow** — is built by `createBossEnemy()` (`enemies.js:2463`, which does put `isBoss: true` in its returned plain object) and then applied via `Object.assign(enemy, {..., isBoss: true})` at `roomFeatures.js:1238`, i.e. onto the **instance**, not onto `enemy.data`. `enemy.data` is still the shared `ENEMIES[char]` entry, which has no `isBoss` key, so both readers see `undefined`. (b) Authored `BOSS_ENCOUNTERS` Minibosses (`giant_slime`, `sniper`, `goblin_army`, `bone_legion`, `grave_tyrant`) set the flag on neither instance nor data — they carry `tier: 'boss'` and nothing more. `CentipedeUnit.js:18` explicitly sets `isBoss: false` on body segments with a comment citing LootSystem, which confirms `data.isBoss` is the intended read side. `GLOSSARY.md:524-526` documents "Bosses have enhanced drops (guaranteed Mana)" via `enemy.data.isBoss` as though it were general. Fix is a design call on which side is canonical: promote the readers to accept an instance-level flag (`enemy.isBoss || enemy.data?.isBoss`), or give every Miniboss a per-instance `data` clone carrying the flag — assigning `isBoss` onto `enemy.data` in place must **not** be the fix, since `data` is the shared `ENEMIES` object and it would tag every ordinary spawn of that char for the rest of the run. | self-spotted 2026-08-23 (boss/sub-boss survey) | ✅ fixed — 2026-08-24 — instance-level flag won the design call (user ruling: bosses are one-off encounters that don't repeat, so the clone machinery buys nothing). Both readers promoted to `enemy.isBoss || enemy.data?.isBoss` (`LootSystem.js` base drop count, `main.js` mana chance); `spawnBossEncounter` now stamps `isBoss` on the boss/leader instance only — followers (goblin wall, skeleton ring) stay regular mobs. This matches the generic fallback's existing instance assign and `Enemy.getNearDeathBlinkColor`'s established instance read (authored bosses also gain the near-death blink, same as the fallback). Centipede head keeps its own data object — unchanged. In-place `enemy.data` mutation remains forbidden. Build + `check:arch` green. |
| 220 | **Pyramid's Legend of Three Slots broke the game's Slot shape — a bare `□` where every other Slot in the game is `[` + glyph + `]`** — `DungeonFloorGenerator._generatePyramid` built each of the three registers as a single flat-gray (`#555555`) `□` BackgroundObject. Every other Slot in the game is a 3-cell bracket frame: REST equipment (`EquipmentSlots.js`, family-colored brackets), the crafting station (`CraftingStation.js`), and — the direct parity target, since it's the other in-world dungeon Slot — the Puzzle Room's weapon pedestal (`HutInteriorOverlay.js`, stone `#554f3d` brackets with the wanted ingredient glyph grayed at `#8a8470` inside). The Pyramid also never showed *which* item each register wanted until after it was already deposited, so the three registers were mute: nothing on the floor said a weapon goes in the bottom two and a consumable in the top one, even though `pyramidSlots[].itemType` had that fact all along. | user-reported 2026-08-24 ("slots in pyramid room are not in parity with Rest Mode slots, critical visual symmetry needed across game for 'slots' taxonomy") | ✅ fixed — 2026-08-24 — extracted the world-Slot vocabulary into `src/data/slotChrome.js` (`SLOT_CHROME`: bracket chars + `STONE`/`PENDING` pair) and built the Pyramid's registers from it: `[★]` / `[⌖]` / `[⌬]` in green, each showing its wanted glyph dimmed in `PENDING` from the moment the floor generates, repainted to the item's own color on deposit (`DungeonPuzzleSystem._fillPyramidSlot` is now a pure repaint — the glyph was already correct). The Puzzle Room pedestal was migrated onto the same constants so the two in-world Slots are literally the same values, not two copies of the same hex. Unauthored zones (no `legendOfThree.js` entry) render three empty `[ ]` frames — dormant masonry rather than a missing edifice. Verified headlessly through the real generator via Vite SSR: frames land at cols 7-9 / 11-13 / 15-17 (no collisions), every cell non-solid so the walk-up-and-offer verb is unchanged, and the three interaction anchors stay at (8,12) / (14,8) / (14,16). Build + `check:arch` green. |
| 219 | **No cheat-menu shortcut to the dungeon boss floor** — `CheatMenu`'s DUNGEON WARP folder listed ENTRANCE/CORRIDOR/BRANCH/PYRAMID (floors 0-3), the Trap Room, and every Puzzle Room template, but stopped short of floor 4 (the Vault) — the one floor that hosts the dungeon boss encounter. Reaching a dungeon boss for testing therefore meant playing the whole delve: skull key → Corridor's West gate → Branch's companion-switch puzzle → the Pyramid's full Legend of Three offering. | user-reported 2026-08-24 ("no shortcut in cheat menu to dungeon boss floor") | ✅ fixed — 2026-08-24 — added a `VAULT (BOSS)` entry pointing at `{kind:'numbered', floorIndex:4}`. Purely a missing menu row: every path it needs already existed and was verified before adding — `DungeonFloorGenerator.generateFloor` already routes `case 4` to `_generateVault`, `DungeonSystem._debugEntryIdFor` already maps `4: 'north'`, and `_activateFloor` already fires `dungeonBossSystem.onFloorActivated(floor)`, which keys off `floor.isVault`. So the jump lands at the correct up-stairs footprint and starts the boss encounter without walking the offering first. Build + `check:arch` green. |
| 183 | **Equipping an oil on a dagger that already has an elemental `onHit` silently discards the blade's own effect instead of stacking or being skipped** — `Item.createMeleeMultistab` (`Item.js:1109-1111,1134`) computes `onHit: oilOnHit \|\| this.data.onHit` for any `weaponSubtype: 'dagger'`; an equipped oil's `onHit` always won when present, with no check for whether the dagger already carried its own (Acid Blade → `'poison'`, `items.js:685`). Confirmed case: Acid Blade + equipped Drowse Oil applied `'sleep'` on every swing and poison never fired, while `CombatSystem.js`'s charge-depletion logic still spent one of Acid Blade's 3 per-room charges for that swing regardless of which `onHit` won. | user-reported 2026-08-15 (Acid Blade + Drowse Oil) | ✅ fixed — 2026-08-15 — Split `onHit` into a singular primary (the weapon's own effect if it has one, else the first equipped oil) plus a new `extraOnHit` array carrying every other equipped oil's effect (`Item.js`'s `_readEquippedOilEffect`, `createMeleeMultistab`, `createSingleArrow`); extras resolve through a new `ExtraOnHitEffects.js` (respects i-frames, independent of Acid Blade's charge gate) called alongside the primary in both `CombatSystem.js` damage paths — per the standing design rule that spending a consumable oil slot must never cost the weapon's own effect. Landed as part of a broader generic stacking system: all 9 blink-capable status effects (burn/poison/zap/stun/sleep/charm/freeze/wet/dizzy) now carry a `stacks` field (capped at 3, `EnemyStatusEffects.js`) incremented on every fresh application regardless of source; poison's DOT tick rate and every effect's blink frequency scale with stacks; Drowse Oil became a 3-tier effect (mild slow → severe slow → full sleep, pink blink) instead of instant sleep. The enemy glyph's status blink now round-robins across every simultaneously-active effect's color (`StatusEffectVisuals.js`), and a new stack-count pip indicator renders above the enemy per active effect (`StatusPipEffects.js`). `Enemy.js`'s write-side logic was extracted to `EnemyStatusEffects.js` and a 3×-duplicated freeze-escalation branch to a shared `applyOnHitStatusEffect` helper to keep `Enemy.js`/`CombatSystem.js` under their architecture budgets. **Follow-up same-day fix**: user playtest immediately found sleep never accumulated past 1 stack (confirmed even with a plain, non-elemental dagger + Drowse Oil alone) — root cause was `Enemy.takeDamage()`'s pre-existing "sleep breaks on damage" rule (~line 2162), which unconditionally reset `sleep.stacks` to 0 on every damage instance, including the very hit that was about to reapply/increment sleep one line of code later in `CombatSystem.js` (`takeDamage()` always runs before the onHit block that re-applies status, gated on `damaged !== false`). Fixed by threading a `{ reapplyingSleep }` hint (computed from `attack.onHit`/`attack.extraOnHit` at both `CombatSystem.js` damage call sites) into a new third `takeDamage(amount, attackId, opts)` parameter; the wake-clear now skips when this same hit is about to reapply sleep, letting the subsequent onHit increment land on top instead of on a freshly-zeroed counter. All other ~35 `takeDamage()` call sites are unaffected (`opts` defaults to `{}`). Build + `check:arch` green (`CombatSystem.js` +112 chars, within tolerance); tier-progression playtest (mild → severe → full sleep across 3 consecutive Drowse Oil hits) still the user's to confirm. **Second follow-up, same day**: further playtest found (1) full sleep (3 stacks) still expired too quickly and (2) poison dropped all 3 stacks simultaneously the instant its timer hit zero instead of tapering off. Root cause of (1): `applyStatusEffect`'s `status.duration = Math.max(status.duration, duration)` is last-hit-wins, not additive — since every Drowse Oil hit passes the same flat per-hit duration, reaching 3 stacks didn't extend the duration past what one hit alone grants. Fixed by scaling sleep's duration by stack count (`duration * status.stacks`, `EnemyStatusEffects.js`), mirroring the precedent poison already set for stack-dependent behavior (its DOT tick-rate formula) — full sleep now lasts ~3× a single hit's duration instead of ~1×. Root cause of (2): poison's expiry was a single shared timer for the whole effect with no per-stack decay logic. Redesigned as an explicit user-requested behavior change (not a bug fix): when poison's duration hits zero with `stacks > 1`, it now loses one stack, recomputes `tickRate` for the new (weaker) stack count, and restarts the timer for a fixed `POISON_DECAY_INTERVAL` (3.0s) rather than clearing the whole effect — so a 3-stack poison steps down 3→2→1→gone, visibly slowing its tick rate at each step, instead of cutting off all at once. Burn (poison's DOT sibling) intentionally keeps the original all-at-once cliff — the user's request was poison-specific. Build + `check:arch` green. |
| 175 | **Purified/Unstable Potions were unreachable from the Cauldron — Water always brewed Base regardless of ingredient** — the class's own docstring (`AlchemySystem.js:16-21`) says the Water path brews "Base / Purified / Unstable" depending on the ingredient used, but `_commitStarter`'s liquid branch (`liquidToStarter`) unconditionally mapped Water → Base (`G`) regardless of ingredient; the ingredient-category branch that would produce Purified (`🜅`)/Unstable (`🜆`) was the `else` arm, reachable only when `cauldronInputType` was something other than `'liquid'`/`'starter'` — which never happens from the live Cauldron UI, so Purified/Unstable Potions could previously only be obtained as loot, never brewed. Reachability half of the original combined #172 report (mechanical `getPotionEffectParams()` fallthrough half renumbered and still open, see #174). | self-spotted 2026-08-08 (Alchemy Hut status check; fixed as a prerequisite for the Quagmire 1st-ingredients drop feature — Purified/Unstable starters are pointless if the Cauldron can never resolve to them) | ✅ fixed — 2026-08-08 — `_commitStarter` now branches on `cauldronLiquidType`: plain Water (`🜉`) calls `starterPotionForIngredient(ingredientChar)` (category-aware: Base/Purified/Unstable by ingredient), the other three liquids keep their existing `liquidToStarter` map (Electrified/Magma/Mud → Charged/Burning/Primal, unchanged). Landed alongside `applyPotionModifierColor()` (a starter potion's color/modifier now persists to the true potion it becomes, at both the Cauldron and the REST Combine Station) as part of The Alchemist's Path feature. Build + `check:arch` green. |
| 164 | **A treasure offered to the fairy fountain resolved in total silence — the coin's luck blessing was indistinguishable from nothing happening** — `FountainSystem._resolveOffering` applied the gift and stopped there. Three of the four non-elemental blessings write a flag with no readout anywhere on screen (`luckBlessed`, `fountainSprintBlessed`, and `fountainArmorBonus`, which only surfaces as a number the player never sees), the pool is unchanged by design for all four, and the fairy has already flown off by the time the flag lands — so a coin offering consumed the one offering per visit and produced no observable effect at all. The four elemental gems were better off (the water visibly re-tints) but still said nothing. The room's own precedent was already the other way: `WellSystem._grantCoinBlessing` answers every coin toss with a narrator line, including the ones whose effect is visible. Offering a treasure whose boolean blessing was already held was the worst case — it spent the offering, changed nothing, and reported nothing. | user 2026-07-26 (playtest) | ✅ fixed — 2026-07-26 — `_applyBlessing` and `_attune` now return the line the narrator says and `_resolveOffering` shows it through `menuSystem.showPickupMessage`, matching the well's phrasing: `FEELING LUCKY?` / `FEELING HEARTY?` / `FEELING SWIFT?` / `FEELING TOUGH?` for the four blessings, `THE WATER TURNS.` for an attunement. A redundant boolean blessing now returns `THE WATER IS STILL.` instead of silently pocketing the gem — the offering is still spent (the "doesn't stack" rule is unchanged), but the answer is honest. Deliberately not a popup: this is narrator voice, the same channel the well already uses, so no key hints or explanatory text enter the offering overlay. Build + `check:arch` green; the messages themselves need eyes on them in play. |
| 145 | **No floating damage number for any player heal, and Red Zone rolling rocks (BoulderSystem) dealt damage with zero visual feedback** — `Player.heal()` (`Player.js:955`) is a pure setter with no callers ever notified; grepped all 3 call sites (hot-spring tick `PhysicsSystem.js`, consumable heal `ConsumableTriggerSystem.js:347`, passive regen `Player.js:611`) and none spawned a `createDamageNumber`. Separately, `BoulderSystem.js`'s player/enemy hit resolution (`~line 200/214`) called `takeDamage` directly with no `createDamageNumber` call at all — the sole gap among the combat system's ~70 other damage call sites, all of which already follow that manual "call takeDamage, then call createDamageNumber" convention. | user 2026-07-10 | ✅ fixed — 2026-07-10 — added `COLORS.HEAL` (`#33ff66`, `GameConfig.js`) and a `CombatSystem.showHeal(amount, x, y)` helper (`+N` label, green) as the single call every heal source should use; wired it into the hot-spring tick (`PhysicsSystem.js`) and consumable heal effect (`ConsumableTriggerSystem.js`). Passive regen left silent (per-tick popups would be visual noise, not requested). Added the missing `createDamageNumber` calls to `BoulderSystem.js` for both player and enemy rock hits, matching the existing red damage-number convention (`#ff4400`) used elsewhere (e.g. lava). Did not centralize inside `Player.takeDamage()`/`heal()` itself — with ~70 existing call sites already deliberately picking their own label/color (crit, dodge, block, reflect, DoT, …), that would be a large, risky rewrite of an established pattern for the sake of 2-3 gaps; the `showHeal()` helper gives heal the same "always-called, one true color" guarantee without touching the damage side. Build + `check:arch` green; manual verification pending. |
| 103 | **Zero zone-boosts in `exitLetters.js` were silently ignored** — `ExitSystem.getLetterWeightsForZone` checked `data.zoneBoosts[zoneType]` truthily, so a boost of `0` (intended as a hard "never in this zone" gate) fell through to base weight. The existing 'G' letter's `yellow/red/cyan/gray: 0` boosts did nothing — G spawned in every zone. | self-spotted 2026-06-12 (P-room puzzle build) | ✅ fixed — 2026-06-12 — check is now `!== undefined`. Side effect: G is gated to green as originally intended, and the new Puzzle 'P' letter can be zone-gated to green+yellow. Distribution balance worth a glance in play. |
| 59 | **Red zone boss (Ancient Shell) phase 2 lacked meaningful escalation + Tortoises woke doors** — (1) Phase 2 only had a periodic boulder rain every 8s, which was too predictable and stackable with new ricochet rocks. (2) No 75% HP wave existed. (3) Unactivated Tortoises blocked room-clear before this session's shell-hidden fix. | user 2026-05-24 | ✅ fixed — removed periodic boulder rain (P2_BOULDER_INTERVAL) entirely; replaced with ricochet rock projectiles (3× '0' chars, ±30° fan into room) on every wall bounce in phase 2; added 75% HP tortoise wave (5× passive 't' enemies spawned in passive shell state using `_spawnTortoiseWave`); `tortoiseWaveSpawned` flag prevents re-trigger. |
| 58 | **Unactivated Tortoise ('t') blocked room-clear** — `_countedEnemies` only excluded hidden mimics; Tortoises in their initial shell-form (never woken) counted as active enemies, preventing exits from unlocking. | user 2026-05-24 | ✅ fixed — added `shellCamouflage && inShellForm && !hasBeenActivated` check to `_isHiddenEnemy`; `hasBeenActivated` is set in Enemy.js when the ambusher first wakes. |
| 57 | **deathExplosion enemies had no counterplay warning** — fire/freeze/rock/magic burst projectiles fired instantly on hp=0 with no signal to the player. | user 2026-05-24 | ✅ fixed — added `deathDelay: 0.8` to all 8 `deathExplosion` blocks in enemies.js; added dying-shake phase in main.js (enemy freezes, shakes 3px rapidly, flashes to burst color, no AI); `_countedEnemies` excludes dying enemies so doors open on kill; `isDying` enemies skipped in AI loop. |
| 56b | **Dungeon interior not fully separating player/companion from layer 0** — (1) `DungeonSystem.generateFloor` opened `collisionMap[EXIT_ROW][STAIRS_COL] = false`, creating a physical hole in the south wall of floor 0 that let the player walk out without pressing SPACE. (2) `CampNPCSystem._updateCompanion` pulled enemies from `currentRoom.enemies` (exterior, all frozen) so the companion never aggroed dungeon enemies. (3) On companion death, `startFleeing(currentRoom.exits)` passed exterior exit positions (meaningless in interior coords) as flee targets. (4) Companion had no interior bounds clamping (self-managed position, no physics collision map), letting it drift outside the 384px PiP panel. (5) Dungeon border cells (row 0/23, col 0/23) had no visual representation — player hit invisible walls. (6) High-velocity knockback could skip the 24-cell collision map boundary (cell ≥24 = undefined = no collision), letting the player escape the 384px interior. (7) `InteractionSystem.findNearbyBackgroundObject` checked `inHut` but not `inDungeon`, routing to exterior `currentRoom.backgroundObjects` during dungeon play. (8) `_isValidBlinkPosition` (Yellow Mage blink) used exterior `currentRoom.backgroundObjects` without interior guard. | user 2026-05-23 | ✅ fixed — 2026-05-23 (session 1): removed collisionMap opening; companion enemy targeting, flee path, and bounds clamped to interior. ✅ fixed — 2026-05-23 (session 2): added stone wall tile rendering for solid collision cells in `HutInteriorOverlay`; added hard player bounds clamp with velocity zero in `DungeonSystem.update`; fixed `InteractionSystem` and `_isValidBlinkPosition` to use interior bg objects when `inDungeon`. |
| 49 | **Dungeon PiP overlay never rendered** — `ExploreRenderer` only checked `game.player.inHut` to trigger `hutInteriorOverlay.render()`; `DungeonSystem` sets `player.inDungeon` (not `inHut`) but reuses `game.hutInterior` for the interior state, so the dungeon view never appeared. All `!inHut && !inMaze` guards also incorrectly passed through while the player was inside a dungeon, ghosting exterior entities onto the screen. | user 2026-05-15 | ✅ fixed — 2026-05-15 — added `inDungeon` to ExploreRenderer; all exterior-skip guards now check `!inHut && !inMaze && !inDungeon`; hutInteriorOverlay renders on `inHut || inDungeon` |
| 48 | **Rockwarden armorMechanic is a stub** — `armorMechanic` block in `enemies.js` defines `armorChunks`, `damageReduction`, `debrisOnBreak`, and `rageOnUnarmored`, but `Enemy.js` only stores `this.armorChunks = data.armorMechanic.armorChunks` and nothing reads it — no damage reduction, no chunk-breaking, no debris spray, no rage mode. The Rockwarden (`R`) is effectively a plain heavy melee enemy. | self-spotted 2026-05-15 | ✅ fixed — 2026-05-15 — removed entire `armorMechanic` block; replaced with working `chargeMechanic` (200 speed, 1.8s windup, wall-stun) + `deathExplosion` (8 rock projectiles) + `shellCamouflage:true`. Same "armored bruiser that rages" identity, fully implemented with existing systems. |
| 12 | **Consumable slot unlock conditions undefined** — additional equipment slots are visible but no mechanism exists for earning them | devlog v0.3→v0.4 | ✅ fixed — B-A-T secret room (Bat Belfry) clears to unlock slot 3; `InventorySystem.maxConsumableSlots` drives the rest |
| 7 | **Slime entity count not capped** — goo puddles are capped but spawned slime entities are not; large slimes can flood the room | devlog v0.3→v0.4 | ✅ fixed — spawn requests are skipped when `currentRoom.enemies.length >= 10` |
