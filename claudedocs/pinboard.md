# Pinboard

Open todos that aren't bugs. Bugs go in `known-bugs.md`; this is for follow-ups, deferred work, and ideas waiting for a session. Remove items when done (move nothing — just delete; git history is the archive).

Format: `| N | item — context/acceptance | added | status |` — numbers never reused.

| # | Item | Added | Status |
|---|------|-------|--------|
| 1 | **Smoke-test sfx-editor template UI** — `npm start` in `tools/sfx-editor/`, verify SAVE to a folder path (e.g. `enemy/magic/test`), LOAD, DEL, and that the dropdown groups by folder. CLI path is verified; GUI wiring was only code-reviewed. | 2026-06-10 | open |
| 2 | **Grow the SFX template library** — save sounds under category paths (`enemy/<class>/`, `ui/`, `pickup/`, `ambient/`). Goal: robust enough that new audio work starts from a template + `sfx vary`, not from scratch. | 2026-06-10 | open |
| 3 | **CLI lever control for `sfx vary`** — only `--factor` exists; add freq-range/pitch-sweep/lock flags mirroring the GUI randomizer levers. Wait until the basic vary loop has been used a few times. | 2026-06-10 | open |
| 4 | **Shared harness for `tools/debug/` repro scripts** — canvas stubs + tick-runner lib to cut per-repro boilerplate. Deferred until the pattern recurs a few more times (decided 2026-06-10). | 2026-06-10 | open |
| 5 | **Playtest Bat / Rubber Bat + knocked-away chain** — verify: windup glyph rotates CW from facing and blinks white at 270°; release sweep launches non-heavy enemies (mass < 2) along contact angle; heavy enemies take damage but don't fly; Rubber Bat (Goo+Stick) launches with no damage numbers; launched enemies bowl over others (velocity transfer chains, interrupts windups/charges). Tune first-pass values: Bat damage 1–2, Metal Bat (Bat+Metal) 1–4, launchForce 1100, chargeTime 2.4 (double-seconds), chain MIN_CHAIN_SPEED 80. | 2026-06-11 | open |
| 6 | **Playtest per-character dodge-roll i-frames** (first-pass values from resolved bug #152) — Gold `rollIframes: 0.1`, Gray Assassin `0.08`, Green Ranger none (`rollInvulnerable: false`). Acceptance: each roll still reads as an *evade* against a telegraphed enemy swing (not so tight that a well-timed roll gets clipped), Gold no longer feels immune while mashing, Green's sprint reads as repositioning and punishes rolling *into* an attack, and the Cyan Rogue's hide still feels distinctly more protective than every other roll. Retune `rollIframes` per character — the field exists so they can differ. | 2026-07-24 | open |
| 7 | **Playtest the Troll's windup blink at its true speed** (resolved bugs #150/#151) — the 4-phase telegraph blink now plays in full for the first time, and every telegraph pulse delay now fires at half its former real-time delay. Acceptance: the Troll's windup reads as a legible wind-and-swing rather than a flicker, and `attackWindup` values across the roster still telegraph clearly now that they land where the data says. Retune `attackWindup` per enemy if any now feel too fast to react to. | 2026-07-24 | open |
| 8 | **Playtest the depth-1 telegraph trio — Rat / Slime / Boar on the shared small `box` Area** (first adopters, 2026-07-24) — all three now resolve damage against the small box hit shape (1 cell x 1 cell starting 0.6 cells out, so a 1.6-cell far edge) instead of the legacy melee box (~1.6–1.75 reach x 0.5 wide). Reach is at parity; **lateral width is 2x**, so standing slightly off-axis no longer whiffs them. Acceptance: the Slime's still `blink` reads as the clearest possible "this area, now" at depth 1 and its `windupImmune` swing feels dodgeable-by-position rather than unfair; the Rat's `vertical` reads as a lunge lining up and a rat pack's overlapping telegraphs stay legible rather than becoming visual soup; the Boar's `slap` reads as distinct from its charge; and none of the three feels like a difficulty spike in the first two minutes of a run. If the width is too punishing, narrow `AREA_PRESETS.box.sizes.small.hitShape.width` — it is shared by all three on purpose, so one edit retunes the whole lesson. | 2026-07-24 | open |
| 9 | **Playtest the Telegraph render rewrite — no outline, tell-then-strike, glyph Attack Shapes** (2026-07-24) — the warned Area is now fill-only (blinking between 40% and 15%, no edge stroke) and **disappears the instant the attack goes live**, leaving only the strike; nothing at all draws between the beats of a multi-hit attack. Acceptance: an unoutlined Area still reads as bounded ground to leave, on grass and on stone both; losing the Area at release reads as "the swing is happening" rather than as a flicker or a dropped frame; and `doubleSweep`'s silent pause still lands its second hit fairly on a player who stopped moving (if the pause reads as "it's over", the fix is to re-show the tell between beats — a one-branch change in `drawTelegraph`). Also unexercised by any shipped enemy: `trapezoid`, `circle`, the `big` Size, and `telegraph.attackShape`. Author one enemy onto each before trusting them — the trapezoid especially, since its whole point is that backing straight up is the wrong dodge. | 2026-07-24 | open |
| 10 | **Playtest the single ingredient pile** (resolved bug #165, 2026-07-26) — every ingredient now lives in one array across REST and EXPLORE; `Player` holds none and returning to REST transfers nothing. Acceptance: the cauldron and the press in REST list ingredients gathered on the run that just ended *and* ones held from earlier runs (they previously saw only the carried half); the Tab overlay's counts match what those menus offer; the bridge worker on the ridge accepts stored materials; the wise fellow and the errand traveler still take an Artifact; mana conversion still spends goo; and a character swap at REST leaves the pile intact while a full game over still wipes it. Watch for the loop this changes: there is no longer any reason to walk back to REST to secure ingredients, only weapons/armor/consumables — see the ADR backlog row. | 2026-07-26 | open |
| 11 | **Playtest thrown tripline ends + the Electric Tripline** (2026-07-31) — SPACE away from any anchor now charges a throw of one wire end (128px at full charge, stops on walls) instead of flashing the red X, and either end can be thrown, so a wire can be strung with the player never touching an anchor. Acceptance: the charge reticule reads as a throw and not as a trap deploy; a thrown end reliably lands somewhere the wire is worth having (rather than mid-floor where nothing walks); the green origin bead makes it obvious which end is anchored and which is still in hand; and the throw doesn't make walking a wire to two anchors feel pointless. Electric Tripline (`⌇ + ◇`) tuning: contact routes through `ElectricitySystem.shockEntity`, so an enemy on the line is zap-locked (1.5s, refreshed every frame of contact) while taking 1 damage per 0.3s of enemy iframes — verify that isn't a free kill zone, and that the player crossing their own electric wire (damage, no stun — see bug #166) reads as fair. Also eyeball the `⏦` glyph in-game: it is the first U+23E6 in the project, so confirm Unifont renders it rather than tofu. | 2026-07-31 | open |
| 20 | **Re-author Frost Wolf (`w`) and Sniper (`1`) with custom `states` blocks** — the two remaining enemies whose current behavior is bespoke enough that `stateDefaults.js`'s archetype-derived defaults don't capture it (pack-readiness hesitation, aimed ranged windup). Trap Goblin and Giant Slime are done — Trap Goblin re-authored around the new `flee` wildcard state instead of Strike (see row 21); Giant Slime authored a real `strike.bands` entry (`within`/`attack`/`windup`), which is also what surfaced that bands had no `damage`/`knockback`/`duration` override (now fixed — see ADR backlog). Authoring a `states` block replaces the default wholesale (no merge — see `statesFor()`), so each needs its full States set written, not just the one State that differs. Acceptance note: if Frost Wolf's pack hesitation is authored via Anticipate's `requirePack` gate, be aware `anticipate.js` has no timeout on that gate today — an unsatisfiable pack-readiness condition hangs the wolf in Anticipate forever; add a `duration` fallback alongside it rather than relying on `requirePack` alone. | 2026-08-01 | open |
| 21 | **Enemy State spine / mechanic-file coexistence cleanup** — several `enemyMechanics/*.js` files duplicate logic the spine now owns, in three tiers. Tier 1 (collapse into the spine outright): `SniperMechanic`, `FlockMechanic`, `MimicMechanic`. Tier 2 (convert to spine primitives): `JumpMechanic` → a `hop` movement modifier, `PatrolMechanic` → `alert.movement`. `TrapLayerMechanic` is done, but landed differently than originally scoped here — not a Strike band; it's now a ~30-line reactive Mechanic that watches the new `flee` State's flags (`fleeReachedBarrier`/`fleeBarrierPauseTimer`) and drops one trap, since only a Mechanic's `{suspend,result}` return can reach `TrapSystem`. Tier 3 (spine needs new declarations to fully subsume the rest): add `priority`/`claims`/`overlay` concepts so remaining mechanics can declare precedence/exclusivity against States instead of hand-checking `machine.current` themselves. Sequence matters — do Tier 1 first since it needs no new spine features. | 2026-08-01 | open |
| 22 | **Playtest Flee's new `cornered` reaction** (2026-08-03) — a fleeing enemy can now opt into a forced response when the player closes real distance on it (not just when it breaks line of sight, which is what `lookback`/`useTrap` already covered). Rat/Plague Rat (via `ThiefMechanic`'s coward flip, `cornerRange` defaults to their own `attackRange`) bite back once cornered instead of just absorbing hits while fleeing. Trap Goblin (`enemies.js` `'n'`) now force-lays a trap at `cornerRange: GRID.CELL_SIZE * 1.5` — a first-pass value, untested — instead of taking free hits when a player corners it without ever breaking its sightline. Acceptance: the rat's counter-bite reads as "cornered, so it turns and fights" rather than as a state-machine glitch (it should still flee immediately afterward, not resume hunting); Trap Goblin's forced trap shouldn't fire so early that casual pursuit trips it constantly, nor so late that a fast player still gets free hits before it triggers — retune `cornerRange` in either direction if so. | 2026-08-03 | open |
